# 配線シミュレーター データ層スキーマ v0.3（段取り2＝L1エンジン完成・画面あり）

作成: 2026-08-18。HANDOFF §2-2「netlistデータ層を先に作れば両方が生える」の具体化。
このファイルは設計の正本。確定事項は HANDOFF §2／不変条件は HANDOFF §6 が上位。

---

## §A 中心の設計判断＝**ノードは部品ではなく端子**

`origin/wiring-viewer` の試作 `wiring/data/f.json` は**部品単位**（49ノード・13エッジ・`{"from":"battery","to":"ignition_sw"}`）。
**この粒度では到達性計算ができない**＝スイッチは「どの位置でどの端子とどの端子が繋がるか」が本体で、部品を1点に潰すと内部が消える。

→ **ノード＝端子（`部品ID.端子名`）**とする。これは HANDOFF §2-7「測定点は部品側の端子に置く」と同じ粒度＝**シミュレーションの単位と測定の単位が一致する**（L3・UCM-01連携がデータ変換なしで乗る）。

さらに好都合なことに、**端子ノードの集合は既に原典照合済みで存在する**＝`wiring-terminals.json`（F52・L39・R54点・DIN名・図座標・台帳付き）。ノード集合を新規に発明しない。

## §B ファイル構成（静的配信・ビルド無し＝HANDOFF §6-7）

| ファイル | 内容 | 大きさ | 版管理 |
|---|---|---|---|
| `wiring-terminals.json`（既存） | 端子の**図座標**。ここが座標の唯一の正本 | 13KB | `TERM_VERSION`（既存） |
| `wiring-net.json`（新規） | **トポロジー**＝parts・wires・部品の内部挙動。座標を持たない | 小 | `NET_VERSION` |
| `wiring-geom-{f,l,r}.json`（新規・後回し） | 線1本ごとのSVGパス | 大 | `GEOM_VERSION` |
| `wiring-patches.json`（新規） | 仕様パッチ＋出典台帳 | 小 | `PATCH_VERSION` |
| `wiring-sim.js`（新規） | **L1エンジン**（到達性計算のみ・DOMを触らない） | 8KB | — |
| `wiring-sim.html`（新規） | 作業場ページ（⛔導線なし・`noindex`） | 12KB | — |

**トポロジーと幾何を分ける理由**: HANDOFF §5 が「SVGパス化は総工数の過半」と見積もっている。分けておけば**幾何ゼロの状態でL1のロジックを完成・検証できる**。さらに——

> 💡**幾何が無くても動くL1が作れる**: 既存の端子座標に点を打つだけで「いまどの端子が生きているか」は表示できる。線が光る前に**端子が光る**版が成立する。「線は1本あれば役に立つ」の系＝最初の可視化は既存資産だけで届く。

## §C エンティティ定義

### C-1 parts（部品）
```json
{
  "id": "regulator",
  "label": "レギュレータ",
  "kind": "regulator",
  "sys": 11,
  "terms": ["30", "67", "51", "31"],
  "types": "FLRD",
  "model": { "kind": "regulator_dynamo" },
  "src": "terminals_ledger.md 系統11"
}
```
- `terms` は**DIN名の羅列のみ＝座標を持たない**。座標は `wiring-terminals.json` 側から解決する（重複を作らない）。
- `types`: この部品が存在する型式（F/L/R/D）。**型式差をファイル分割ではなくフラグで持つ**＝端子JSONが既にF/L/Rを1ファイルで扱っている流儀に合わせる。

### C-2 wires（配線＝原典。静的）
```json
{ "id":"w11-03", "a":"regulator.51", "b":"dynamo.51", "color":"MARRONE",
  "sys":11, "types":"FLRD", "v":"orig", "src":"f_netlist §11" }
```
- `v`＝照合レベル: `orig`（原典照合済）／`auto`（機械抽出のまま）／`inf`（推定）。
  **`auto` と `inf` を事実として見せない**＝f_netlist は**ウインカー系以外すべて機械抽出**（HANDOFF §5）。この旗が公開前検証の進捗管理そのものになる。
- 色は**OTSUKA原典の色名**（GIALLO/MARRONE…）を持ち、描画RGBは持たない（パレット表は f_netlist にある）。

### C-3 部品の内部挙動（状態で変わる接続）
```json
"model": { "kind":"switch",
  "positions": { "OFF":[], "ON":[["30","15/54"]], "START":[["30","15/54"],["30","50"]] } }
```
- **wires＝常に繋がっている／model＝状態で繋がる**、この2層の和がその瞬間のグラフ。
- 【2026-08-18 v0.2】`model` は共通で `kind` ／ `v`（この内部モデルの照合レベル）／ `default`（初期状態）／ `note`（何をモデル化し何をしていないか）を持つ。**簡易モデルには `v:"inf"` を必ず付ける**＝原典に描かれていない内部接点を事実として見せないため。
- 【同】ランプ等の負荷は配線ではなく**部品の内部要素**として持つ（`model.kind:"lamps"` の `lamps[]`＝`a`/`b` は同一部品の端子名）。wires は原典に線として描かれているものだけに保つ。
- スイッチの接点構成は**端子台帳作業でかなり判明済**（HANDOFF §5）。レギュレータ・フラッシャーは簡易モデルで足りる。
- 【2026-08-18 v0.3】**画面の操作レバー→部品位置の対応表も `wiring-net.json` が持つ**（`controls`）。「キーを回すと ign_sw が動く」「エンジンが回ると dynamo と regulator が動く」は車の事実であってUIの都合ではない＝JS側に書くと正本が二重になる。`role:"input"`（通常操作）と `role:"fault"`（故障注入＝L2の先取り）を分けてある。**配列の順番＝適用の順番**で、後ろの項目が前を上書きする（故障は入力より後ろに置く）。

### C-4 L1 到達性の定義（物理演算なし）【2026-08-18 v0.3 で確定・実装済】

**エッジを2種類に分けるのが要**。これをしないと、点いているランプを通り抜けた先まで「通電」になる。

| 種類 | 中身 | 扱い |
|---|---|---|
| 導体エッジ | `wires` ＋ いま閉じている `model.positions[位置]` のペア（スイッチ接点・ヒューズ・カットアウト・巻線） | 抵抗ゼロ＝同電位 |
| 負荷エッジ | `model.kind:"load"` の `pair` ／ `model.kind:"lamps"` の `lamps[]` | ここで電圧が落ちる |

- **導体エッジだけで連結成分に切る**（＝同じ成分は同じ電位）。電源は `kind:"battery"` の `pos`、アースは `kind:"ground"` の端子＝**どちらもデータから見つける**（ノードIDをJSに直書きしない）。
- **負荷が働く = 片側が B+ 成分 ∧ 反対側がアース成分**。
- 端子の状態は**4段**。3段（B+/アース/無電）では足りないことが実装で確定した：

| 状態 | 意味 | テスターを当てると |
|---|---|---|
| `hot` | B+ に導体だけで到達 | 約12V・電流を流せる |
| `post` | **負荷の先**＝働いていない負荷を跨いだ向こう側 | **電圧は出るが電流は流れない** |
| `gnd` | アースに導体だけで到達 | 0V |
| `off` | どちらにも通じない（孤立・断線の先） | — |

- `post` の伝播は**働いていない負荷だけを跨ぐ**（働いている負荷の先は電位が落ちているので伝えない）。
- **B+ 成分とアース成分が同一になったら短絡**（`shorted`）＝L2の故障判定にそのまま使える。
- ⚠️**ダイナモの電機子巻線（`STOP` の `51↔−`）は導体扱い**にしている。実際は巻線＝抵抗だが、チャージ灯（数十Ω）に対して十分小さいので L1 では 0Ω と見なす。**これが「キーONでチャージ灯が点く」の成立条件**なので、負荷側に移すと挙動が壊れる。
- ⚠️アースに負荷経由でしか繋がらない成分は `off`（0V）に落としている。導通試験の意味では「アースに繋がっている」だが、L1は電圧しか見ないので区別しない。
- これだけ。電流も電圧降下も持たない（HANDOFF §2-5「L4はやらない」）。

### C-5 期待値テーブルは**持たずに導出する**（L3・UCM-01の要）
測定点の期待値は L1 の到達性から自動的に出る：`hot`＝約12V／`post`＝**電圧は出るが電流は流れない**（テスターは振れるのに直らない、の正体）／`gnd`＝0V／`off`＝—。
**authoredで書くのは物理が要る例外だけ**（例: 走行中のオルタB+＝13.8〜14.5V、ダイナモ車は挙動が別）。
→ HANDOFF §3 の「期待値も仕様パッチで書き換わる」が、**パッチでグラフが変われば期待値も自動で変わる**という形で自然に満たされる。表を二重管理しない。

## §D 仕様パッチ

```json
{
  "id": "gen-alt",
  "label": "オルタネーター換装",
  "sys": [11],
  "when": { "generator": "オルタネーター" },
  "ops": [
    { "op":"removePart", "id":"regulator" },
    { "op":"removeWire", "id":["w11-02","w11-03"] },
    { "op":"addPart", "part": { "id":"alternator", "terms":["B+","D+"], "model":{"kind":"alternator"} } },
    { "op":"addWire", "wire": { "a":"alternator.B+", "b":"starter.+", "v":"inf" } }
  ],
  "ledger": "出典: （施工実例の典拠をここに書く。terminals_ledger の流儀）",
  "types": "FLRD"
}
```
- `when` は**DB列の値そのまま**（§DB実勢調査で確定した `generator`/`ignition_type`/`fuel_pump` の実値）＝ HANDOFF §2-3「登録データを自動適用・手数ゼロ」がマッピング表なしで成立。
- **op は6種**: `removePart`/`addPart`/`removeWire`/`addWire`/`setModel`/`setControl`。自由描画は入れない（HANDOFF §6-1）。**2026-08-18 追記**＝当初5種の見積もりだったが、`gen-alt` パッチを実装して「同じ操作レバー（`engine`等）が入れ替わった部品を動かせない」漏れが分かり `setControl` を足した（詳細は §G-4）。
- 該当パッチ無し → **その系統だけ純正表示＋注記**（§2-6）。パッチは系統単位で当たるので、他系統は自分仕様のまま。
- **実装済み**: `wiring-patches.json`（`PATCH_VERSION` 1・`gen-alt` パッチ1本）＋ `wiring-sim.js` の `applyPatchOps`/`applyPatches`（netlistのクローンに ops を当てて新しい netlist を返す純粋関数。既存の `solve` は無改修）。

## §E 段取り（この順なら途中でも役に立つ）

1. ~~`wiring-net.json` に**発電系（系統11）だけ**を書く~~ ＝**2026-08-18 完了**（parts 9・wires 13）。内訳は系統11が parts 7・wires 11、これに系統10から `ign_sw` と2本（`f1.30↔ign_sw.30`・`ign_sw.15/54↔quadro.INTER`）を足した。**キーONが無いとチャージ灯の点灯が再現できない**＝段取り2「キーON/OFFで端子の色が変わる」に必要な最小の足し算。詳細は §G
2. ~~L1ロジック（BFS）＋スイッチ内部モデル。キーON/OFFで端子の色が変わる~~ ＝**2026-08-18 完了**（`wiring-sim.js`＋`wiring-sim.html`）。決着した宿題は §G-2
3. ~~`gen-alt` パッチ1本~~ ＝**2026-08-18 完了**（登録車の約半数・オルタ69台に効く見込み）。5ケースの検算はパッチ適用前後で1件も変わらないことを確認済み。詳細は §G-4
4. 系統11のSVGパス化＝ここで初めて「線が光る」。工数実測の材料になる
5. 以降、系統を1本ずつ（各段階で公開可能＝100%カバーは成立条件ではない）

## §F 既存資産との接続で要る作業（新規発明ではない）

- ~~**端子JSONに部品IDを足す**~~ ＝**2026-08-18 完了（40点・`TERM_VERSION` 8→9）**。`{"x":301,"y":1712,"t":"30","p":"regulator"}` の形で、系統11・系統10・系統15(30/4)・系統1(レギュレータの67/31) の該当端子にだけ付けた。
  - **`p` はビューアが読まないキー**（`renderTerm()` が使うのは x/y/t/dx/dy だけ）＝表示は一切変わらない。それでもデータが増えたので3言語とも `TERM_VERSION` を上げた（親テーマの生きたルール）。
  - 付ける対象は **`wiring-net.json` に部品が在るものだけ**。部品の無い端子に `p` を付けると宙に浮いた参照になる＝系統を書くたびに一緒に増やす。
  - 内訳: F=16点／L=11点／R=13点（L型は台帳39点の範囲にヒューズ箱と計器盤が無く、R型は計器盤が無い＝**部品はFLRD共通でも座標は型式ごとに欠ける**。トポロジーと幾何を分けてあるので欠けても計算は成立する）。
- **f_netlist の系統別ネットを wires へ転記**（色・端点は既にある。`v` を正直に付ける）。
- **`ignition_type`/`fuel_pump` のパッチ設計**は §DB実勢調査の結果を前提にする（点火は3値で基本分岐・ポンプは電気式51台のみ対象・「15直結/リレー式」の列がDBに無い＝要追加）。

---

## §G 実装状況（2026-08-18・段取り3 完了時点）

> 【2026-08-21 更新】その後 **NET_VERSION 3 で系統7（油圧・燃料の警告灯）／NET_VERSION 5 で系統9（点火・一次側）**を追加した。系統9の定義と「持たないもの」は末尾の **§G-5** を読むこと。持ち越し3件（下記）にはそれぞれ**期限となる旅**が決まった＝`JOURNEY-INDEX.md` §6。
> 【2026-08-22 更新】**NET_VERSION 7 で系統5（ホーン）・系統8（ルームランプ）**を追加＝**§G-6**。これで持ち越し3件のうち **3 が決着**（残り＝1・2）。現在 parts 17・wires 26・controls 9。
> 【2026-08-28 更新】**NET_VERSION 9 で系統2（ヘッドライト）**を追加＝**§G-8**。これで持ち越し3件のうち **1 も決着**（残り＝2＝計器盤 `30` の給電元）。現在 parts 30・wires 48・controls 15。

### 入っているもの（`wiring-net.json` NET_VERSION 2）

parts 9 = `body` / `battery` / `starter_sw` / `starter` / `regulator` / `dynamo` / `f1` / `ign_sw` / `quadro`、wires 13（w11-01〜11・w10-01〜02）。【NET_VERSION 2 で `controls` 5項目を追加＝操作レバーと故障注入。データの結線そのものは段取り1から変えていない。】**すべて `v:"orig"`**＝原典（D型取説p40の目視転記 `d_netlist.md` と、F原典照合済の `terminals_ledger.md`）から読めた結線だけ。f_netlist の機械抽出は補強にしか使っていない。

回路としては **バッテリー＋ → スタータSW節点 →（MARRONE太）→ レギュレータ30 →（ROSSO太）→ F1の30バス → イグニッションSW30** という幹に、レギュレータ51⇔ダイナモ51、レギュレータ51⇔計器盤51（チャージ灯）、各アースがぶら下がる形。

### 内部モデル（`v:"inf"`＝原典に描かれていない＝そう見せない）

- `regulator`: カットアウトリレー（`CHARGE` で 30↔51）だけ。界磁制御はL1に効かないので持たない
- `dynamo`: `STOP` で 51↔− が導通（電機子巻線）。**これがチャージ灯の点灯理由**。`RUN` で切れ、代わりにレギュレータが 30↔51 を閉じて灯が消える＝キー・エンジンの2状態だけで正しい挙動になる
- `quadro`: チャージ灯を `51`↔`INTER` として持つ。**⚠️反対側が未確定**（原典の計器盤は `30`←BIANCO も持ち、`30` は色疑義で転記保留＝台帳の⛔項目）。キーOFFで点きっぱなしにならない方を採った＝**これは推定なので、計器盤の内部を原典で読み切ったときに必ず見直す**

### 持ち越した疑問（系統を足すときに必ず決着させる）

1. ~~**イグニッションSWの `30/4`×2（NERO）の内部接点**。行き先は外部照明SW21と計器盤で決着済（`d_netlist.md` 未決④）だが、SW内部で30と常時繋がっているのかが未照合。500は**キーを抜いても外部灯が点く**のでおそらく常時30系だが、推定でモデルに入れていない。**系統2（ヘッドライト）を書くときが期限**~~ → **2026-08-28 決着（§G-8）＝`30/4` は常時電源のタップとして扱う。** `ign_sw` の positions を `OFF:[[30,30/4]]` / `ON:[[30,15/54],[30,30/4]]` に改めた（従来 OFF は空だった）。根拠＝外部照明SWの入りは NERO（`30/4`）しかなく（HANDOFF その69①②④）、**キーを抜いてもヘッドライトが点く**という実車の挙動は `30/4` が常時 `30` に繋がっていなければ再現できない。⚠️**原典が内部接点を描いているわけではない**＝回路として成立する唯一の形として採った。⛔記事で「原典で確認した」と書かない
2. **計器盤 `30` の給電元**（上記チャージ灯の反対側と同じ問題）。`f_netlist` は F2 側（15/54）から来ていると読み、D原典は BIANCO（30系）と書いていて割れている＝**原典の拡大で決着させる**
3. ~~**ヒューズの負荷側の端子名**が原典に無い（合成端子 `_out`）。F1の負荷側はホーンとルームランプ＝系統5/8を書くときに繋ぐ~~ → **2026-08-22 決着（§G-6）＝合成端子 `_out` のまま維持する**。理由＝D型図のヒューズ箱の行に DIN 名「30」が振られてはいるが、それは**その線がつながっている「ネット（電位）の名前」であって、接点1つ1つの名前ではない**（F1 の電源側も同じ 30 になり区別が付かない）。原典が接点を名指ししていない以上、名指ししたことにしない＝合成端子を残す方が原典に忠実。⚠️**この判断は他のヒューズにもそのまま適用する**（F2〜F4 の負荷側も `_out`）

### §G-4 段取り3＝`gen-alt` パッチの実装（2026-08-18）

**見込みどおり `WiringSim.solve` は無改修**。足したのは `wiring-sim.js` に `applyPatchOps`（1パッチのops列をnetlistのクローンに当てる）と `applyPatches`（`when`/`types` が合うパッチだけ順に当てる）の2関数だけ＝エンジンは netlist を受け取るだけで、それがどう作られたかを知らない、という見立てどおりになった。

**`gen-alt` パッチの中身**（`wiring-patches.json`）:
- `regulator`・`dynamo` を外し、`alternator`（`terms`: `B+`/`D+`/`_gnd`）を追加。`_gnd` は原典に無い合成端子（`starter_sw._in`/`_out` と同じ流儀）＝オルタネーター本体のブラケット接地を表す。
- `model.positions`: `STOP`＝`D+↔_gnd`（停止中はD+がアース近傍→警告灯が点く）、`RUN`＝`B+↔D+`（充電中はD+がB+近傍→灯が消える）。**ダイナモの`51↔−`（STOP）＋レギュレータのカットアウト`30↔51`（RUN）という2部品2接点の役割を、D+端子1本の2接点に統合した**。原典に無い改造の再構成なので `v:"inf"`。
- 配線4本（`pga-01`〜`04`）は原典の `w11-03/04/07/08,09` を1:1で置き換える形（`starter_sw._in`→`B+`／`B+`→`f1.30`／`D+`→`quadro.51`／`_gnd`→`body.31`）。
- **`setControl` を新設した経緯**: `engine` コントロールは元々 `{STOP:{dynamo:"STOP",regulator:"OFF"}, RUN:{dynamo:"RUN",regulator:"CHARGE"}}` という「部品ID→状態」の対応表を持つ。`gen-alt` が `dynamo`/`regulator` を消して `alternator` を足しても、**このコントロール自体は書き換わらない**ので、キーを回してもエンジンを回してもオルタネーターは動かない（`positionsFrom` が存在しない部品IDへの `set` を黙って無視する実装のため、エラーにはならず「何も起きない」という気づきにくい形で壊れる）。これが実装して初めて分かった漏れ＝ op を5→6種に拡張し、`{ "op":"setControl", "id":"engine", "merge":{ "STOP":{"alternator":"STOP"}, "RUN":{"alternator":"RUN"} } }` で `engine` コントロールの両選択肢に `alternator` の対応を追記する形にした。
- **検算**: 原典（ダイナモ+レギュレータ）の5ケース（HANDOFF §G 検算表と同じ入力）を、`gen-alt` 適用後のnetlistでも流し、**チャージ灯・スターターの点灯条件が1件も変わらないことをNode実行とページ内の両方で確認済み**（`wiring-sim.html` の「検算（gen-altパッチ適用時）」）。B+/負荷の先の**件数**はトポロジーが変わるため一致しない（比較対象外）。
- ⚠️**「レギュレータ故障」の故障注入は`gen-alt`適用時は無効**（`reg_fail`コントロールが動かす `regulator` パーツがそもそも存在しない）。オルタネーター車向けの故障モデル（例: 内蔵レギュレータ不良）はスコープ外＝L2着手時の課題として持ち越す。

### 次の一手（段取り4）

系統11のSVGパス化＝ここで初めて「線が光る」。表現方式は §G-3-1 で決着済み（流れる点＝SMIL `animateMotion`）。工数実測の材料になる（HANDOFF §5「総工数の過半になる見込み」の検証）。

### 段取り1のデータで検算した結果（2026-08-18・段取り2の実装で数え方を改めた）

⚠️**この表の数字は一度書き換えている**。段取り1では使い捨てBFSの**純粋な到達性**で数えていた（キーON時 16点）。段取り2で導体エッジと負荷エッジを分けたので、**負荷の先を B+ に数えなくなった**（同 8点）。**灯が点くかどうかの判定は両方の数え方で一致**＝結論は変わっていない。

| 状態 | チャージ灯 | スターター | B+ | 負荷の先 |
|---|---|---|---|---|
| キーOFF・エンジン停止 | 消 | — | 6（常時30系だけ） | 0 |
| キーON・エンジン停止 | **点** | — | 8 | 0 |
| キーON・エンジン回転 | 消 | — | 11 | 0 |
| キーOFF・スタータ引き | 消 | **回る** | 8 | 0 |
| F1切れ・キーON | **点** | — | 7 | 0 |

- **F1が切れてもチャージ灯は点く**＝イグニッションSWの30がヒューズの電源側から分岐しているため。原典どおりの挙動がデータだけで再現された（作り込んだ結果ではない）
- ⚠️**期待値（点/消・回る）は原典と実車から書いたもので、計算結果を写していない**。写すと回帰の見張りにならなくなる。この5件は `wiring-sim.html` の「検算」欄で読み込みのたびに自動実行される＝**系統を足したときにここが崩れたら、足したデータのほうが間違っている**

### §G-2 段取り2で決着した宿題（負荷の先）

段取り1が持ち越した「純粋な到達性だと負荷を通り抜けた先まで B+ になる」問題は、**端子の状態を4段にする**ことで決着（定義は §C-4）。**L4（電圧計算）をやらない方針は一切変えていない**＝グラフの成分を数え直しただけ。

**この段が要る理由は画面で確認できる**: エンジン回転中に「レギュレータのカットアウトが閉じない」故障を入れると、`regulator.51`・`dynamo.51`・`quadro.51` の3点が `post`（負荷の先）になり、チャージ灯は消えたまま。**テスターを当てれば電圧は出るのに灯が点かない**＝球切れかレギュレータか迷う実際の場面がそのまま出る。3段（B+/アース/無電）だとこの3点が B+ と同じ色になり、**診断ツールとして逆に人を誤らせる**。

### §G-3 段取り2で新しく見えた宿題

1. ~~**通電の色と原典の配線色がぶつかる**。原典の線は ROSSO（赤）・VERDE（緑）で描かれていて、いま端子の状態にも赤（B+）を使っている。点だけなら判別できるが、段取り4で「線が光る」ようにすると、その色が配線色なのか通電状態なのか分からなくなる。~~ → **2026-08-18 決着**: 色そのものでは決着させない（点滅を強くしても常時30系の既存の赤と紛れて判別できない、というユーザー実機検証で判明）。**線の地色（原典のROSSO/VERDE等）はそのまま識別用に残し、通電時だけ「動き」を別レイヤーとして重ねる**＝端子側で採用した dim（他を沈めて浮かせる）と同じ「色に頼らずレイヤーで分ける」原則を線にも適用。方式は**流れる点**（Falstad系回路シミュレータの定番表現。SMIL `animateMotion` で宣言的に書け、JSのフレーム処理が要らない・動く向き＝実際の電流方向をそのまま表せて診断的にも意味を持つ）。次点は流れる破線（`stroke-dasharray`＋`stroke-dashoffset`・より軽量だが直感性は点に劣る）。**実装は段取り4（線のSVGパス化）着手時**＝それまでは端子の点滅のみで運用（意味は「見るところ」欄の文章とdimで補う）。
2. **画面の作りで分かったこと（公開版の設計にそのまま効く）**。最初に作った版は操作パネルと図が縦に並んでいて、**操作すると図が画面外に出て変化を追えなかった**（ユーザー指摘）。直した内容＝①操作は図の直上に `position:sticky` で貼る・長い説明は図の**下**へ ②**図を `<img>` と重ねるのをやめて SVG の中に `<image>` として取り込み**、`viewBox` を動かすだけで図と端子が一緒に拡大するようにした ③**変わった端子のまわりに自動で寄る**（375px に図の全体を出すと端子番号が読めない＝A4縦の配線図をスマホで見せる以上、寄せる仕組みは必須） ④場面ごとに**「見てほしい端子」を持たせて寄り先にする**（変わった端子だけだと図全体に散って寄る意味が無くなる）。⚠️**線が光るようになっても同じ問題が出る**＝段取り4でも「どこを見ればいいか」を先に決めてから描く
3. **`quadro.30`・`ign_sw.30/4`・`quadro.57`・`dynamo.67`・`regulator.67` は常に「無電」に見える**＝まだ相手側の系統を書いていないだけで、故障ではない。系統が増えれば自然に解消するが、**公開する画面では「この系統の範囲外」と「本当に電気が来ていない」を区別して見せる必要がある**（いまは作業場なので区別していない）

### §G-5 系統9＝点火系（2026-08-21・`NET_VERSION` 4→5・旅ページ第6号 `no_spark` と同時に投入）

**追加したのは2部品・3電線・コントロール1個だけ**（見積りは3〜4部品だった）。少なく済んだのは、下の「持たないもの」を意識して切ったから。

| 種別 | id | 中身 |
|---|---|---|
| parts | `coil` | terms `+` / `D`。`model.kind:"load"`・`pair:["+","D"]`＝**一次巻線を1本の負荷エッジ**として持つ |
| parts | `distributor` | terms `_in` / `_gnd`。switch＝`CLOSED`（カムの谷・接点閉）／`OPEN`（カムの山・接点開） |
| wires | `w09-01` | `ign_sw.15/54` → `coil.+`（AZZURRO） |
| wires | `w09-02` | `coil.D` → `distributor._in`（NERO） |
| wires | `w09-03` | `distributor._gnd` → `body.31`（NERO） |
| controls | `points` | `CLOSED` / `OPEN`（既定 `CLOSED`） |

- ⚠️**コイルはヒューズ F2 を通らない**＝キースイッチ `15/54` の**電源側**から分岐する。根拠3つ＝`d_netlist.md` 部品22（15/54←AZZURRO＋AZZURRO＝→ヒューズ・→コイル+）／同 部品28（+←AZZURRO・D←NERO）／`terminals_ledger.md` 系統9 (471,486)「15/54＝F2左接点(AZZURRO→コイル)✔」。`f_netlist.md` §9 の機械抽出は「F2→IGN_COIL+」と読めるが、**電源側の左右はヒューズごとに違う**（F3 は右が電源側）ので、これ単独を根拠にしないこと。すべて `v:"orig"`。
- `distributor._gnd` は原典に端子番号が無い合成端子（`starter_sw._in`/`_out` と同じ流儀）＝デスビ本体のブラケット接地。

**⛔持たないもの（データ側にも `_sys9_scope` として明記した・消さない）**

1. **二次側（コイル `B` → HT → デスビ中心 → プラグ×2）を電線として持たない。** 高圧は一次電流の遮断による**誘導**で生まれる輪で、L1（到達性）では原理的に解けない。入れると「つながっている＝火花が出る」と誤読される＝**診断の道具として人を誤らせる**（`JOURNEY-INDEX.md` §4 と同じ理由）。旅ページでは**「この絵の外」の帯**として絵にだけ描いた。原典 p40 の部品30（プラグ）は**絵と文章にだけ出る**扱い。
2. **コンデンサ（部品29の一部）を持たない。** L1 は導通/非導通しか表せないので、入れるとポイントを**常時短絡**する（実車は交流的にしか効かない）。
3. **点火時期・ドエル角を持たない。** 角度と時間の話＝L1 の外。`points` コントロールの note にも書いた。**測る側＝UCM-1 の担当**（ドエル角カード4枚と直結する＝旅ページ側は測る場所の定義まで、良否の値は持たない）。

**この系統を足しても既存の絵は1文字も変わらない**ことを `check_journeys.js --diff` で実測（一致62項目・変化0項目）。検算は6ページ合計89件・不一致0。

### §G-6 系統5＝ホーン／系統8＝ルームランプ（2026-08-22・`NET_VERSION` 5→6→7・旅ページ第7号 `horn_silent`／第8号 `room_lamp_dark` と同時に投入）

**この2系統は分けて書けない**。どちらも **F1 の負荷側に着く、ただ2つの負荷**で、「両方が同時に死んだらヒューズ1本」という車上で最も確度の高い切り分けが、2つ揃って初めて成立するため。旅ページも互いを参照し合う形にした（第7号→第8号／第8号→第7号）。

| 種別 | id | 中身 |
|---|---|---|
| parts | `horn` | terms `+` / `-`。`kind:"load"`・`pair:["+","-"]` |
| parts | `horn_sw` | terms `_in` / `_gnd`。switch＝`OFF`／`PRESSED`（押している間だけ閉じるモーメンタリ） |
| parts | `room_lamp` | terms `+` / `-`。`kind:"load"`・`pair:["+","-"]` |
| parts | `room_sw` | terms `_in` / `_gnd`。switch＝`OFF`／`ON`。**`v:"inf"`**（下記⚠️） |
| parts | `door_sw` | terms `_in` / `_gnd`。switch＝`DOOR_CLOSED`（接点**開**）／`DOOR_OPEN`（接点**閉**） |
| wires | `w05-01` | `f1._out` → `horn.+`（BIANCO・`v:"orig"`） |
| wires | `w05-02` | `horn.-` → `horn_sw._in`（GIALLO E NERO・`v:"orig"`） |
| wires | `w05-03` | `horn_sw._gnd` → `body.31`（NERO・`v:"inf"`＝原典はアース記号だけ） |
| wires | `w08-01` | `f1._out` → `room_lamp.+`（BIANCO・`v:"orig"`） |
| wires | `w08-02` | `room_lamp.-` → `room_sw._in`（NERO・`v:"inf"`＝部品26を2つに分けたことで生じた内部の渡り） |
| wires | `w08-03` | `room_sw._gnd` → `body.31`（NERO・`v:"orig"`） |
| wires | `w08-04` | `room_lamp.-` → `door_sw._in`（NERO・`v:"orig"`） |
| wires | `w08-05` | `door_sw._gnd` → `body.31`（NERO・`v:"orig"`） |
| controls | `horn_btn` / `door` / `room_btn` | `OFF`/`PRESSED`・`CLOSED`/`OPEN`・`OFF`/`ON` |

**この2系統が持ち込んだ、構造として新しいもの2つ**

1. **スイッチが【アース側】に入る初めての系統**＝ボタンは電気を送るのではなく**帰り道をつなぐ**。副作用として **`key` の位置と一切無関係に働く**（実車で「鍵を抜いてもホーンは鳴る」が正常なのはこれ）。⚠️**UCM-1 側で「キーONで測る」前提の判定を作るとき、この2系統だけは前提が違う**（`ucm1-interop/journeys.json` の `note_for_ucm1` にも書いた）。
2. **帰り道が2本ある初めての負荷**＝ルームランプの `-` から `room_sw` と `door_sw` に**並列**で分かれ、どちらか一方でもアースに落ちれば点く。**片方を「試験用の道」として使える**＝部品を外さずに故障箇所が割れる、という診断上の強みがそのまま絵になる（第8号の骨）。

**⚠️原典側で「既に決着済みだった読み」を2件拾い直した（古い記述を根拠にしない）**

- **F1 負荷側の給電色は F/D とも `BIANCO`＝色差はゼロ。** `df_diff.md` 論点1 で決着済み（「F型＝AZZURRO／D型＝BIANCO の型式差」は撤回された古い読み）。⛔**色差説を復活させない。**
- **ホーンの線は F/D とも `GIALLO E NERO`（黄と黒）＝一致。** ⛔**「F図では無地に見える」を色差の根拠にしない**（描き分けの粗さであって色ではない・`df_diff.md` 冒頭の訂正）。
- 本数も一致＝`df_diff.md` 論点3 が「F1 の負荷側に着くのは2本」と照合済み。**だから「ホーンとルームランプだけが道連れになる」と言い切ってよい。**

**⛔持たないもの**

1. **ホーン本体の機械（振動板の調整ねじ・内部接点の荒れ）**＝「通電しても鳴らない／音がかすれる」は L1 の外。旅ページは**両端子の測定が全部正常なら残りはここ**と文章で手放している。
2. **球切れ**＝L1 は線がつながっているかしか見ない。旅ページはテスターの読み方（＋だけ12Vで − が0V＝球切れ）で補っている。
3. **ドア柱スイッチは実車では左右2個が並列**だが、L1 では結果が同じなので**1個で代表**させた。「片側のドアだけ効かない」はこの1個を2個と読み替えること。
4. ⚠️**`room_sw` の位置数は推定（`v:"inf"`）**。原典は「内蔵SW→アース」と書くだけで接点の位置を描かない。**切／入の2位置**と解釈した（3位置＝ドア連動そのものを切れる形とは解釈していない）。原典の拡大で読めたら見直す。

**既存の絵は1文字も変わらない**ことを `check_journeys.js --diff` で実測。検算は**8ページ合計119件・不一致0**。

---

### §G-7 系統6＝ブレーキ灯（2026-08-22・`NET_VERSION` 7→8・旅ページ第9号 `brake_no_light` と同時に投入）

**シリーズで初めて「キーON側のヒューズ F2（15/54）」が入った系統。** F1（常時電源）とちょうど裏返しの関係になる。

| 種別 | id | 中身 |
|---|---|---|
| parts | `f2` | fuse。terms `15/54`（電源側）／`_out`（合成端子・§G-6 の決着に従う） |
| parts | `brake_sw` | terms `_in` / `_out`。switch＝`UP`（開）／`PRESSED`（閉）。**`v:"inf"`**（下記⚠️） |
| parts | `stop_l` / `stop_r` | terms `+` / `-`。`kind:"load"`・`pair:["+","-"]`・`v:"orig"` |
| wires | `w06-01` | `ign_sw.15/54` → `f2.15/54`（AZZURRO・`v:"orig"`） |
| wires | `w06-02` | `f2._out` → `brake_sw._in`（GIALLO E NERO・`v:"orig"`） |
| wires | `w06-03` | `brake_sw._out` → `stop_l.+`（ROSSO・`v:"orig"`） |
| wires | `w06-04` | `stop_l.+` → `stop_r.+`（ROSSO・`v:"orig"`＝**車体の底を横断する渡り**） |
| wires | `w06-05` / `w06-06` | `stop_l.-` / `stop_r.-` → `body.31`（NERO・`v:"inf"`＝原典はアース記号だけ） |
| controls | `brake` / `f2` | `UP`/`PRESSED`・`OK`/`BLOWN` |

**この系統が持ち込んだ、構造として新しいもの**

1. **キーON側ヒューズ**＝`f2` の電源側は `ign_sw.15/54`。⚠️**F2 が切れて死ぬのはブレーキ灯・ウインカー・ワイパーだけで、点火と警告灯は生き残る**＝コイル `+` と計器盤 INTER への線は**イグニッションSWの 15/54 端子＝ヒューズの手前**で分かれるため。⛔**`f_netlist` の一本だけを見て「この負荷はヒューズを通る」と決めない**（電源側の左右はヒューズごとに違う）。
2. **負荷が2つ並び、一方から他方へ渡る初めての系統**＝`w06-04`。**どちらの灯が消えたかで切れた場所が割れる**（右だけ消＝渡りか右のアース／左だけ消＝左のアース／両方消＝スイッチより上）。この非対称がそのまま第9号の骨になった。
3. **「点きっぱなし」を扱う初めての系統**＝スイッチ固着。旅ページでは `override:{brake_sw:'PRESSED'}`（コントロールの入力を無視して部品の位置を直接与える）で作った。

**⚠️ストップスイッチは電気のスイッチではない**＝ブレーキ配管に付く**油圧スイッチ**で、踏むと油圧で**閉じる**。**第2号の油圧センダとは向きが逆**（センダは油圧がかかると**開いて**警告灯を消す）で、**油も別物**（ブレーキフルード／エンジンオイル）。`controls.brake` の `note` にも書いた。位置数は原典に記号があるだけなので `v:"inf"`。

**⛔持たないもの**

1. **球切れ**（L1 は線しか見ない）／**灯の光り方の弱さ**（電圧降下＝L1 の外・UCM-1 の担当）。
2. **F2 の相棒＝ウインカーとワイパー**。原典上は同じ F2 にぶら下がるが、**まだ netlist に入っていない**（#12・#15 の実装時に投入する）。第9号の本文では文章でのみ触れ、footer の「持っていないもの」に明記した。⚠️**「ブレーキ灯とウインカーが同時に死んだら F2」という切り分けは、#12/#15 が入って初めてシミュレーター上でも成立する**（§G-6 の「対で数える」と同じ形）。
3. **ハイマウント・ナンバー灯・尾灯（車幅灯側）**＝別系統。この旅の絵には出さない。

**既存の絵は1文字も変わらない**ことを `check_journeys.js --diff` で実測（**変化0項目**）。検算は**10ページ合計142件・不一致0**。

### §G-8 系統2＝ヘッドライト（2026-08-28・`NET_VERSION` 8→9・旅ページ第8回 `headlight` と同時に投入）

**シリーズで最大の追加**（parts 21→30・wires 32→48・controls 11→15）。**外部照明の入口（元栓＝部品24）とコラムレバー（コミュテータ＝部品14）が初めて入った系統**で、ヒューズも F3〜F6 の4本が一度に増えた。

| 種別 | id | 中身 |
|---|---|---|
| parts | `light_sw` | 外部照明スイッチ（F原典 部品24）。terms `30/4`（入り・NERO）／`_pos`（合成端子・VERDE）／`C`（BIANCO）。positions **`OFF`／`ON`（`30/4`↔`_pos` と `30/4`↔`C` の両方＝入／切の2位置）**。**`v:"inf"`**。⚠️⚠️**2026-08-28 訂正**＝以前は `OFF`/`POS`/`HEAD` の3段だったが誤り（→ その73） |
| parts | `commut` | コミュテータ（F原典 部品14）。terms `C` `30/2` `30/3` `56b` `56/a1` `56/a2`。positions **`POS`（I＝何も閉じない）／`LOW`（II＝`C`↔`56b`）／`HIGH`（III＝`C`↔`56/a1` と `56/a2`）＝⭐3段はこちら**。default `POS`。**`v:"inf"`**（→ その73） |
| parts | `f5` / `f6` | fuse。`_in`（合成端子・箱の中で連結）／`30/3`・`30/2` |
| parts | `f3` / `f4` | fuse。`f3` は `56b`（入り）／`56/b2`（右ロー）、`f4` は `_in`（合成端子）／`56/b1`（左ロー） |
| parts | `head_l` / `head_r` | **`kind:"lamps"`**＝1部品に2本のフィラメント（`lo`＝`56b`↔`-`／`hi`＝`56a`↔`-`）。アースは共通の `-` |
| parts | `hi_ind` | ハイビーム表示灯（F原典 部品21）。`kind:"load"`・`pair:["+","-"]` |
| wires | `w02-01`〜`w02-16` | 16本。骨は `ign_sw.30/4`→`light_sw`→（VERDE）F6・F5／（BIANCO）`commut.C`→（GRIGIO-ROSSO）F3・F4→左右のロー／（VERDE系）左右のハイ＋表示灯 |
| controls | `lights` / `beam` / `f3` / `f4` | 元栓3段（`OFF`/`POS`/`HEAD`）・ロー/ハイ・ヒューズ2本 |

**この系統が持ち込んだ、構造として新しいもの**

1. ⭐⭐**1つの部品に2つの負荷が入る初めての形**＝`kind:"lamps"`。ヘッドライトは**電球1個の中にロー用とハイ用の2本のフィラメント**が入っていて、**アースは1本を共用**する。だから「片側だけ、ローもハイも両方消えた」＝その側のアースか電球そのもの、という切り分けが図の構造として言える。
2. ⭐⭐**1本が箱の中で2本のヒューズに分かれる初めての形**＝`w02-08`（F3⇔F4 の入り側連結）と `w02-03`（F6⇔F5）。⭐**これが「片目だけロービームが消える」の理由**＝ヒューズより手前は1本なので左右が一蓮托生、ヒューズから先だけが独立する。
3. ⭐**ヒューズを1つも通らない負荷が初めて入った**＝ハイビーム（`w02-11`・`w02-12`）。⇒ **「ハイだけ両方消えた＝ヒューズより手前」**が言える。⚠️**「ヘッドライトが消えたらまずヒューズ」という一般論をこの系統に持ち込まない。**
4. ⭐**`30/4` を常時電源のタップとして確定**＝上の「持ち越した疑問1」の決着。`ign_sw` の `OFF` に `[["30","30/4"]]` を足した。**既存9ページの絵は1文字も変わらないことを `check_journeys.js --diff` で実測（変化0項目）**＝`30/4` に繋がる部品が今まで1つも無かったため。

**⚠️`v:"inf"` を付けた2つ（原典で読めていない）**

- **`light_sw`（元栓）の内部**＝3端子・破線2組までは 14倍拡大で読めたが、「どの位置でどちらが閉じるか」は原典に無い。**NERO が入り・VERDE と BIANCO が出**としたのは、VERDE が F5/F6 の入り側に着く以上、電源になりうるのが NERO しかないため（HANDOFF その69④）。
- **`commut`（コラムレバー）の内部**＝原典・再作図物とも印刷解像度で読めない（8倍まで確認）。**`C` をロー/ハイの共通電源**としたのは、`30/2`・`30/3` を共通電源にすると**レバーが I（車幅灯）の位置でもヘッドライトが点いてしまう**＝実車と違うため。⭐**I／II／III の3位置そのものは推定ではなく純正取説に明記**（500D p12-12・500L p10-12）＝その73。

⛔**どちらも「原典で確認した」と書かない。** 記事側は `v:"inf"` と note のとおり断ってある。

**⛔持たないもの**

1. **`commut` の `INT` 端子**（`30/2`・`30/3` から先＝⭐**計器盤照明で確定**＝取説 500L p9-5 が元栓の役目に「ed alla lampada di illuminazione del quadro di controllo」を挙げている＝その73）。端子だけ持ち、内部接点は持たない。⛔**だから「F5・F6 が切れたらヘッドライトはどうなるか」をこの絵に答えさせない**（#10 メーター照明の領分）。`f5`・`f6` に control を作っていないのも同じ理由。
2. **球切れ**（L1 は線しか見ない）／**光量の弱さ**（電圧降下＝UCM-1 の担当。⚠️「ヘッドライトが暗い」は L1 では作れない＝JOURNEY-INDEX §4）。
3. **パッシング**（コミュテータ 部品14 は原典名に "lampi luce anabbaglianti" を含むが、内部接点が読めない以上モデルにしない）。
4. **車幅灯・ナンバー灯の本体**（#14 の領分）。VERDE の枝は `f5`・`f6` の負荷側で止めてある。

**色の出どころ**（⚠️当会F図の彩色を根拠にしない＝HANDOFF その66）

- `30/3` の線は**純正原典で GIALLO**（F原典 p42・L原典 p59 とも）。当会F図と 500line 再作図物は MARRONE で描いている。
- `56/b1`（左ロー）は**再作図 `fd_F1.pdf` のヒューズ箱ラベルが GRIGIO E NERO**。当会F図は無地のグレーに見えるだけ（二色線の黒破線を描き分けていない）。
- 新色4つ（`GRIGIO-ROSSO`・`GRIGIO E NERO`・`VERDE E NERO`・`ROSA`）を `wiring-journey.js` の `WC` に追加した。

**検算**＝11ページ合計 **164件・不一致0**（headlight 22件）。系統2 単体の到達性テストは16件・不一致0。
