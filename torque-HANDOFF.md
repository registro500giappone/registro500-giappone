# 締め付けトルク早見帳 引き継ぎ書

最終更新: 2026-08-14

## 1. これは何か

`torque.html`（1ページ完結の検索型トルク早見帳）と `torque-data.json`（データ本体）の設計・判断の正本。

- **本番デプロイ済みだが導線なし＋`noindex`＝限定公開の作業場**（`https://www.registro500.com/torque` を直接開けば見える）。正式公開の判断はユーザーが出す。`<meta name="robots" content="noindex">` を入れてある（公開時に1行外す）。
- N·m は**データに持たず表示時に計算**する（`kgm × 9.80665`）。二重管理を避けるため。
- 表示は「値」より**手順を優先**（`procedure` があるとそちらを主役にする）。

## 2. ⚠️ 最重要の設計判断：適合はエンジン「系統」で分ける

> **【2026-08-14 更新】エンジン軸は形式（110F.000 など）ではなく「系統」2つ（`e500` / `e126`）に畳んだ。** 経緯・実測値は §6.10。以下の「車種で切らない」という判断そのものは変わっていない。

**2026-08-13 ユーザー確定。** 車種（500 / 126）で分けてはいけない。

> 古いクルマなので車両によってエンジンを載せ替えているケースが多数。よって判断は車両よりエンジン形式にした方がいい。

これは載せ替え対策であると同時に、**メーカーの素の仕様からして車種で切ると破綻する**。

- **500 R（1972-75）は 126 のエンジン（126.A・594cc）を積んでいる。**
- したがって「対象: クラシック FIAT 500（Nuova / D / F / L / R）」という現在のページ表記は**誤り**。R のエンジンは既存データの対象外。**要修正**。

### エンジン形式の一覧（データ上は `engines[].forms` に残っている。選択肢には出さない）

| 系統 | コード | 排気量 | 搭載車 |
|---|---|---|---|
| `e500` | 110.000 | 479cc | Nuova500 N |
| `e500` | 110D.000 | 499cc | Nuova500 D |
| `e500` | 110F.000 | 499cc | Nuova500 F / L |
| `e500` | 120.000 | 499cc（横倒し） | Nuova500 Giardiniera / Autobianchi |
| `e126` | 126 A.000 | 594cc | Fiat 126 前期 |
| `e126` | **126 A5.000** | 594cc | **Nuova500 R**（§6.8 結果6） |
| `e126` | 126 A1.000 / A1.048 | 652cc | Fiat 126 後期 / Bambino |

### 二軸で持つ

エンジンだけでは足りない。サス・ブレーキ・ステアリング・ボディは車体側の値なので、**エンジン系と車体系を別の軸で持つ**。

- エンジン系（ヘッド・コンロッド・フライホイール・プーリー・プラグ・ロッカー・オイルフィルター・ジェネレーター）→ **エンジン系統**で適合（28項目）
- それ以外（ミッション・デフ・サス・ブレーキ・ステアリング・ボディ）→ **ベース車体**で適合（82項目）

これで「Nuova500 F の車体に 126 エンジンを載せた車」を正しく引ける。**実データで 500車体に126系エンジンが35台（21.6%）**あり、この2軸を1本に畳むことはできない（§6.8.1）。

## 3. 資料と、その性格（★ここを読まずに数値を足さない）

現物は **`Documents\registro500-notes\manuals\`**（手持ち）と **`manuals\FD Ricambi\`**（2026-08-13 に取得）。

> ⚠️ **このリポジトリの中に置かないこと（2026-08-13 ユーザー確定）。** 当初は repo 直下の `マニュアル/` に置いていたが `registro500-notes` へ移した。理由＝①**push すると Cloudflare Pages が自動デプロイするので、入れた瞬間に `registro500.com/...` で誰でも落とせるようになる**。Autobooks・Fiat 純正・Bucheli はいずれも著作権のある商業出版物で、再配布は権利侵害 ②5ファイルが Cloudflare Pages の1ファイル 25MiB 上限を超えていてビルドが通らない ③一度コミットすると 338MB が `.git` に残り、履歴の書き換え無しには消せない。

### 使ってよい資料

| 資料 | 対象 | 単位 | 性格 |
|---|---|---|---|
| `FD Ricambi/Fiat 500 Workshop Manual 1957-1973 (EN).pdf` 128p | 110 / 110D / 110F / 120 | lb·ft | Autobooks。**lb·ft の原値**。テキスト層あり |
| `FD Ricambi/Fiat 500 D Reparaturanleitung (DE).pdf` 336p | 110 / 110D | mmkg | **Fiat 純正**。**kgf·m の原値**。トルク一覧表は**章ごとに7つ**＝p57（エンジン）/ p128（ミッション・デフ）/ p146（フロントサス）/ p161（リアサス）/ p179（ステアリング）/ p193（ブレーキ）/ p205-206（シャシー総括）。テキスト層あり |
| `FD Ricambi/Fiat 500 Giardiniera Reparaturanleitung (DE).pdf` 39p | **120（ジャルディニエラ）** | mmkg | **Fiat 純正の補遺**。全ページ**スキャン画像**（テキスト層なし）。トルク表は PDF p16（本 p18・エンジン）/ p28（ミッション・デフ）/ p38（サス・駆動・ホイール）/ p39（ステアリング）。**エンジン表は 500 D の p57 と同一構成・同一部品番号**＝120 に固有のトルク値は無い。したがって現状ここを原値にした項目は無く、**「120 にも同じ値でよいか」の裏取りに使う** |
| `FD Ricambi/Fiat 126 Reparaturhandbuch (DE).pdf` 94p | **126.A1（652cc）のみ** | **N·m** | Verlag Bucheli。純正ではない。トルク表は本 p91-92（PDF p93-94）。スキャン画像。**単位は本 p91 に明記**＝「Angaben in Nm; um mkp-Werte zu erhalten, sind die Nm-Werte durch 10 zu dividieren」 |
| `FD Ricambi/Fiat 126 Workshop Manual Autobook 853 1972-1976 (EN).pdf` 131p | **126.A（594cc）** | lb·ft（kgm 併記） | Autobooks。**500 R のエンジン値を得る唯一の資料**。**巻末 本 p112（PDF p113）に締付トルクの一覧表がある**＝126 の値はここを原値にした |
| `Manuale uso e manutenzione - Fiat 500 R.pdf` 13p | **500 R（車体・エンジン識別）** | — | **Fiat 純正『Fiat 500 VARIANTI al libretto uso e manutenzione』**（奥付 `FIAT SERVIZIO NORME E PUBBLICAZIONI / 2ª Edizione / Stampato N. 603.91.687 - XI-1972`）。**トルク値は載っていないが、「L に対して R で何が変わったか」の Fiat 自身による網羅リスト**＝§6.8 の R 問題を解いた資料。スキャン画像・低解像度（464×650）だが `pypdf` の `page.images` で取り出せば読める。中身は 本 p2-p3（諸元差分）／p4-5（配線図）／p6（電装）／p7（ボディ・整備）。p8-9 は R 専用ではない Fiat 共通の点検計画（`603.01.755 - VI-1973`）、p10-13 は広告 |

### ⛔ 使ってはいけない / 注意が要る資料

- **FD Ricambi のサイト上のトルク表**（`/en/tech-center/.../tightening-torques-for-classic-fiat-500/`）
  - 「500用」と題しているが、**中身は 126 Bucheli 整備書 p91-92 の値**（ヘッドナット40・主軸受30・コンロッド35・プーリー150・プラグ30 …が完全一致）。
  - **ページに単位の表記がない**。Bucheli 側に「Angaben in Nm」とあるので N·m。
  - 500 のヘッドナットは実値 3.3 kgf·m＝32 N·m。**この表の 40 を 500 に使うと締めすぎになる。**
  - 脚注 `*` `**` `***` の説明文がページに存在しない（`Cylinder head nuts 40*`、`Hub nuts see ***`）。
  - 一部は Bucheli とも食い違う（Wheel bolts 50 / Bucheli 70）。**出所不明の混成。数値の出典にしない。**
- **atwiki「fiat500-onlinemanual」の締め付けトルク2020**（`w.atwiki.jp/fiat500-onlinemanual/pages/285.html`）— 日本語で読める一覧なので見つかりやすいが、**出典の記載が無く、原典と食い違う行が混ざっている。数値の出典にしない。**（2026-08-13 に全27行を照合）
  - 大半は当ページと同じ原典の転記だが、**プーリーのオイルフィルターカバー `8.0 kgf·m` は原典 15.0（15000 mmkg／英語版 108.5 lb·ft）のほぼ半分**。この値で締めると緩む。**カムチェーンスプロケット `0.9` も原典 1.1（1100 mmkg）と違う。**
  - ⚠️ **「エキマニ<>シリンダヘッド 2.5 kgf·m」の1行だけ、当方が全数走査した5資料のどこにも対応する記述が無い。** 出所不明なので §5.3 の判断（エキマニは原典に指定なし）は変えない。**この行を見つけても採らない。**
  - ⚠️ WebFetch は 403 で読めない。中身を見るならブラウザ（Playwright）で開く。
- **`manuals/FIAT500マニュアル.pdf`（128p）** — 上記 Autobooks 英語版の機械翻訳。原典を入手済みなので**役目を終えている**。訳で数値が崩れている（`1 08.5 Ibft`、`lb`→`Ib`、「プラグ」と「pins」の取り違え）。**これを出典にしない。**
- **イタリア語の取説4冊**（`Fiat 500 D uso e manutenzione` 他）— スキャン画像・テキスト層なし。取説なので整備トルクは期待薄。
  - ⚠️ **ただし「取説だから」で束にして切ってはいけない。** 当初この束を5冊として一括で「期待薄」と切り捨てていたが、**うち1冊（`Manuale uso e manutenzione - Fiat 500 R.pdf`）は取説ではなく Fiat 純正の差分補遺**で、§6.8 の「500 R の車体」問題を解く鍵だった（2026-08-13 に取りこぼしが判明）。**トルク値が無い資料でも「何が変わったか／変わっていないか」の一次証拠になる。**

### 資料そのものの誤り（判明済み・再調査しないこと）

- **Autobooks 英語版はリレーレバーのピン締付を二重に記載している。** p94「39 to 43 lb ft」／p95「78 to 85 lb ft」（約2倍）。Fiat 純正独語 p179 が `5500—6000 mmkg`＝39.8-43.4 lb·ft なので、**p94 側が正しい**。データは 5.5-6.0 kgf·m を採用済み。
- Autobooks は 120.000 エンジンについて「**シリンダーヘッドナットの締付順序が違う**」と明記（FIG 1:44 と FIG 1:46）。トルク値は共通だが順序が別。
- **Bucheli（126・652cc）のピットマンアームは桁を落とした誤植。** `Lenkhebelbefestigungsmutter 10` は N·m として読むと 1.0 kgf·m だが、594cc の Autobook は **10 kgf·m（72 lb·ft）**、500 も 10〜11 kgf·m。**Bucheli 側が誤り**と判断してデータは 10.0 kgf·m を採った。FD Ricambi のサイトの表もこの「10」をそのまま転載している。**再調査しないこと。**
- **Bucheli は同じ箇所を二重に載せて値が食い違う行がある。** ①ブレーキバックプレート→ナックル＝足回りの表 3.0／ブレーキの表 2.0（Autobook と一致する **2.0** を採用）②クラッチハウジング→エンジン＝一方が 2.5、他方が 2.5〜3.5（Autobook と一致する **2.5** を採用）。

## 4. データの持ち方

- `kgm` / `kgm_max` … **Fiat 純正独語版の記載値**（mmkg ÷ 1000）
- `lbft` / `lbft_max` … **Autobooks 英語版の記載値**
- どちらも原典の記載をそのまま入れる。⚠️**原典に書かれていない値を換算で作らない**（2026-08-13 是正済み。片側しか記載が無ければ片側だけ入れ、もう片側は null にする）

### 英語版WSMの lb·ft について（誤解しやすい）

⚠️ **英語版WSM の lb·ft が「kgf·m × 7.23301」と小数1桁までぴったり一致するのは正常。** 元がイタリア語の kg m なので、換算した数字をそのまま印刷しているため。**「換算値だから原典に無い」と早合点しないこと**（2026-08-13 に一度これで誤診した）。

- **章によって書き方が違う**。エンジン〜駆動系は小数1桁（`18.1 Ibft` / `23.9 Ib ft` / `108.5`）、ステアリング章は整数（`14 to 18` / `39 to 43` / `72`）。どちらも原典の記載。
- ⚠️**抽出するときは表記ゆれに注意**。`lb ft` / `Ib ft` / `Ibft` / **`Ib/ft`（スラッシュ）** が混在する。スラッシュ形を落とすとステアリング章がまるごと漏れる。正規表現は `(?:lb|Ib)\s*[/. ]?\s*ft`。
- **全項目を原典と突き合わせ済み**（2026-08-13）。換算で埋まっていたのは4箇所だけで、すべて是正した＝pitman-arm-nut と front-swingarm-hub-nut の lb·ft 上限（原典は単一値）／brake-backplate-front・rear の lb·ft（出典は独語版で kgf·m のみ、英語版に対応記述なし）。

### 単位の書き方（2026-08-13 ユーザー確定）

**注記・手順・差分説明の中でトルク値に触れるときは、必ず N·m を先頭に書く。**

- 理由＝**日本で流通するトルクレンチは N·m が主流**（ユーザー本人のレンチも N·m）。注記の中だけ kgf·m や lb·ft しか無いと、N·m のレンチしか持たない人がその値を使えない。
- ⚠️**併記する N·m は kgf·m × 9.80665 の計算値に統一し、原典が書いている N·m をそのまま写さない。** カード上部の大きい N·m と食い違うため。
- **Bucheli と FD Ricambi の「N·m」は kgf·m を10倍しただけの簡便表記**で、正確な換算ではない（Bucheli 自身が「10で割れば mkp」と書いている）。だから紙面の 70 は当ページでは 69 になる。この説明は `sources.buch126.note` に入れてある。
- 手順項目（`procedure`）は `kgm` が null で**カード上部に単位が何も出ない**ので、手順の文中に lb·ft も残す。値のある項目の `notes` はカードに3単位とも出ているので「N·m（kgf·m）」でよい。
- ⚠️`diff-rolling-torque-126` の **kgf·cm は kgf·m に直さない**（0.13 kgf·m のような読みにくい数字になる）。N·m は併記する。
- `cross` … 2つ目以降の出典と、値が一致したか（`agree`）。画面に「✓ 2資料で一致」バッジが出る。不一致なら橙の「⚠ 資料間で値に差あり」
- `cross[].kgm` / `kgm_max` / `note` … **不一致のときだけ入れる**。相手の資料の値と、なぜ差があるかの説明。画面に「別の資料では ○○ N·m」の枠で出る。**どちらを採るかは読む人が決められるようにし、片方を伏せない**
- `procedure` … 角度戻し等、単純なトルク値で表せない箇所。値より優先表示
- `critical` … 重要保安部位（ブレーキ・ハブ・ホイール・ステアリング）。カード左端に赤帯

## 5. 現状（2026-08-13 時点）

- **110項目**（うち 126 向けが59項目）。値あり98・値なし12＝**この12項目はすべて §5.3 の通り「原典に指定なし」と確定済みで、未調査の項目は1つも無い**。**2資料で照合済みが合計68項目**。
- **【2026-08-13 追記】原典の再照合で未収録だった9項目を拾った（§5.2）。**
- **126 の投入が完了**（下記 §5.1）。これで **500 R もカバーできた**＝**【2026-08-14 更新】126系エンジン ＋ ベース車体 FIAT 500 を選ぶと 49項目・値37件**が出る（ヘッドは126の 4.0 kgf·m、ホイールボルトは500の 4.5 kgf·m と正しく混ざる）。**エンジンを選んで値が0件になる組み合わせは無い。**
- 42項目（値あり28・手順2・値未登録12）だった時点の経緯は以下。
- 2026-08-13 に Fiat 純正独語版と全項目を突き合わせ、**5件を純正の原値に修正**（ステアリングボックス 2.0-2.5／タイロッド 2.5-3.0／リレーレバー 5.5-6.0／ピットマン 10.0-11.0／フロントスイングアーム 5.5-6.0）、**項目名の誤り1件を訂正**（「ミッションとボディの結合」→「クラッチハウジングとエンジンの結合ナット」＝独語 `Kupplungsgehäuse am Motor`）、**4項目を追加**（ステアリングホイール 4.0-5.0／クラウンホイール 3.2／ブレーキバックプレート 前2.0・後6.0）。
- 値が無い12項目は**消さない**。「何を締めるか」の索引になり、ねじサイズ表へ誘導できるため。指定が無いことを確認済みだと画面で伝える（§5.3）。
- **【2026-08-13 追記】画面まわりの手当てを2件（§6.1）と、ログイン済みオーナー向けのプリセットを実装（§6.2）。** ページの構成は `torque.html` ＋ `torque-data.json` のままだが、**`config.js` と supabase-js（CDN 2.94.0）を読むようになった**点だけ変わっている。

### 5.1 126 の投入（2026-08-13）

**原値＝126 Autobook 本 p112 の一覧表**（lb·ft 主・kgf·m 括弧併記）。**照合＝Bucheli 本 p91-92**（N·m）。

- **594cc（Autobook）と 652cc（Bucheli）はエンジン・ミッション・デフの全項目で完全一致した。** よってエンジン系10項目は `fits.engine = ["126A","126A1"]` とした。
- **同じ部位でも 500 と 126 で値が違う**ので、項目は分けて持つ（id は `-126` サフィックス）。例＝ヘッドナット 500 は 3.3・126 は 4.0／クラウンホイール 500 は 3.2・126 は 4.5 kgf·m。
- 名前が重複して取り違えるのを防ぐため、**カードに「対象エンジン」「対象車体」を必ず表示する**ようにした（`fitsLabel()`）。
- Bucheli にしか無い項目（パワーユニットマウント5点・レリーズフォーク・ホイールシリンダー・ブレーキホースユニオン・ハンドブレーキ等）は **652cc でしか裏が取れていない旨を `notes` に書いて**投入した。

#### 資料間で食い違った5項目（画面に両方出している）

| 項目 | 594cc Autobook | 652cc Bucheli | 判断 |
|---|---|---|---|
| **ピットマンアーム** | 10.0 kgf·m | 1.0 kgf·m | **Bucheli の誤植**（§3参照）。10.0 を採用 |
| **ホイールボルト** | 5.0 kgf·m | 7.0 kgf·m | 判別つかず。FD Ricambi の表は 50 N·m＝Autobook 側。**低い方を主にして両方表示** |
| 板バネ ゴムマウント | 3.0 | 5.0 | 年式差か誤植か不明。両方表示 |
| ロアアームシャフト→ボディ | 3.0 | 5.0 | 同上 |
| ロアアーム→キングピンハウジング | 6.0 | 7.0 | Bucheli は自己ロックナット指定。ナットの種類が違う可能性。両方表示 |

#### 手順で管理する箇所（数値を主役にしない）

- **フロントハブナット**＝0.7 kgf·m まで締めて約30度戻す。Bucheli も 0.5 kgf·m と極端に低く、**両資料とも「締めてから戻す」手順で一致**。
- **リアホイールベアリング**＝回転抵抗で管理。スペーサーは必ず新品・指定グリス Fiat MR 3。
- **デフのプリロード**＝シム調整。単体 13〜15 kgf·cm、クラウンホイール組付け後 20〜50 kgf·cm。**締付トルクと取り違えないこと。**
- **ミッションのメイン／レイシャフトナット**＝5.0 kgf·m の後、割ピン穴が合うまで**増し締め方向にのみ**追い込む。

### 5.2 未収録項目の拾い直し（2026-08-13）

**きっかけ＝純正独語版 p57 の `ANZUGSDREHMOMENTE DES MOTORS`（エンジン締付一覧）を、前のセッションで見落としていた。** 一覧表は p179 / p193 / p205-206 の3つだと思い込んでいたが、**エンジンは p57、ギアボックス／デフは p128 に別の表がある**。⚠️ **「一覧表は全部見た」と判断する前に、章ごとの表が他に無いか確認すること。**

拾った9項目：

| 項目 | 値 | 出典 |
|---|---|---|
| 主軸受ボディ 固定ボルト | 2.1 kgf·m | 独語 p57 のみ |
| カムシャフトギア 固定ボルト | 1.1 kgf·m | 独語 p57（英語版と不一致） |
| 冷却ファン→ジェネレーター ナット | 3.5 kgf·m | 独語 p57（英語版と不一致） |
| デフ ベアリングハウジング 取付ナット | 1.8 kgf·m / 13 lb·ft | 独語 p128 ＋ 英語 本 p71（一致） |
| スターターモーター 取付ナット | 18.1〜21.7 lb·ft | 英語 本 p74 のみ |
| パワーユニット 下側マウントナット | 18.1〜21.7 lb·ft | 英語 本 p74 のみ |
| リレーレバー サポート→ボディ | 14〜18 lb·ft | 英語 本 p101 のみ |
| ジェネレーター ベアリング外輪ストッパー | 0.80 lb·ft | 英語 本 p114 のみ |
| デフ プリロード（回転抵抗・手順） | 1.01 ± .07 lb·ft | 英語 本 p74 のみ |

- ⚠️ **英語版WSMにしか無い項目が4つ出たため、`kgm` が null で `lbft` だけの項目が初めて発生した。** 表示ロジックが `kgm` の有無で「値あり」を判定していたので、`itemHasValue()` に集約して lb·ft だけの項目も値ありとして扱うようにした（カードの大きい N·m・並び順・件数・「値がまだ無い」警告の4箇所）。`cross` の差分表示も `lbft` を受け付ける。
- ⚠️ **ファンとプーリーは英語版と独語版で扱いが違う。** 英語版 本 p114 は「The pulley and fan-to-generator armature shaft self-locking nuts … 14.5 Ib ft」＝**プーリーとファンを1行にまとめている**。純正独語版はプーリー 2.0（p223）・ファン 3.5（p57）と**別値**。データは独語版に従い項目を分け、プーリー側の `notes` にも「ファンは別項目」と書いた。**再調査しないこと。**
- ⚠️ **英語版WSMの本のページ番号は PDF のページ番号＋8。** 出典を `本 pXX` で書くときは PDF のインデックスをそのまま使わない（今回、下書きの5箇所が全部ずれていて是正した）。印字されたノンブルで確認する。
- ⚠️ **英語版 本 p74 には 18.1〜21.7 lb·ft が2箇所ある。** 片方はパワーユニット下側マウント（4本）、もう片方はスターターモーター取付（2本）。**同じ値なので1つと早合点しない。**

### 5.3 ⛔ 原典に指定が無いと確定した12項目（2026-08-13）＝再調査しないこと

**オイルドレン・ロッカーカバー・エキマニ／マフラー・キャブ・インマニの5項目は、手持ちの全資料に締付トルクの指定が無い。** §6 の残タスク3としてもう一度探した結果なので、**次のセッションで3度目を始めないこと。**

探した範囲（すべて全数走査。「表だけ見た」ではない）：

| 資料 | 調べ方 | 結果 |
|---|---|---|
| 英語版WSM（128p・テキスト層あり） | `(?:lb\|Ib)\s*[/. ]?\s*ft` で**全ページのトルク記述を1件残らず列挙**し、別途 `kg m` / `kgmm` も走査。`drain plug` / `rocker cover` / `exhaust manifold` / `carburett` / `manifold` の全出現も文脈付きで確認 | **5項目とも指定なし。** 組付け手順は詳しく書いてあるが（例 p24「Secure the carburetter using two copper washers and two self-locking nuts」）トルクは付いていない。**巻末に技術データ／トルク一覧の付録は無い**（本文は第12章ボディで終わる）。空白ページ4枚は本当に白紙 |
| 純正独語版（336p・テキスト層あり） | `mmkg` / `mkg` / `kgm` を**全ページ文脈付きで列挙**し、見出し `ANZUGS(DREH)MOMENTE` も全件確認。トルク表は **p57 / p128 / p146 / p161 / p179 / p193 / p205-206 の7つで確定**（p57 と p205-206 は画像で目視） | **5項目とも指定なし。** p57 のエンジン表は11行で、主軸受・フライホイール・コンロッド・ロッカー台座・ヘッド・カム・プーリー・ファン系のみ |
| 純正独語版 ジャルディニエラ補遺（39p・スキャン） | 全39ページのサムネイル一覧を作って表組みページを特定 → PDF p16（本 p18）`ANZUGSDREHMOMENTE FÜR MOTORTEILE` を目視 | **5項目とも指定なし。** 120.000 のエンジン表は **p57 と同一構成・同一部品番号**（1/11003・1/47500・1/25664・1/17016・1/21647・1/40549・1/09794・987109・1/42904・1/25756） |
| 126 Autobook 本 p112 | 一覧表を目視で再確認 | **5項目とも指定なし** |
| 126 Bucheli 本 p91-92 | 一覧表を目視で再確認 | **5項目とも指定なし** |

- ⚠️ **メーカーが指定していないだけで、締めなくていいという意味ではない。** ページ側は「値は未登録です」ではなく **「整備書に締め付けトルクの指定がありません」＋探した範囲**を出し、ねじサイズからの一般値へ誘導している。データ側は `no_spec: true` で持つ（`_readme` に定義あり）。**未調査（`no_spec` なし）と区別するための印**でもある。
- `no_spec` の項目は **「未確認」バッジを出さない**（調べていないのではないため）。
- 副産物＝`sources` の `oem.note` にトルク表のページを **7つ全部**書き直した（従来は p179 / p193 / p205-206 の3つしか書いておらず、§5.2 で見つけた p57・p128 が反映されていなかった）。

#### 残り7項目も同日に確定（＝値が無い12項目すべてが `no_spec` になった）

同じ手順で残りも当たり、**全12項目が「原典に指定なし」で確定**した。**未調査の項目はもう1つも無い。**

| 項目 | 判定の要点 |
|---|---|
| ブレーキパイプ フレアナット | 独語版のブレーキ表（p193）は**2行だけ**（バックプレート→ナックル 2000／→トレーリングアーム 6000）で配管系は無い。英語版はホースの外し方（本 p101）を書いているがトルクは付いていない |
| ホイールシリンダー（500） | 同上。**126 は Bucheli に 9.8 N·m（1.0 kgf·m）があり別項目で登録済み**＝500 も同じ値かは裏が取れていない |
| マスターシリンダー（500） | 同上。**126 は 14.7 N·m（1.5 kgf·m）**。独語版の油圧ブレーキ章（p186-188）はマスター／ホイールシリンダーの構造を詳しく書いているが、取付トルクは一切無い |
| シートベルト アンカー | **当時の純正装備ではない**。英語版 本 p102 に「シートベルトの取付部を床から外す」という手順が出てくるだけ。独語版の `Gurt` ヒットは OCR の誤り（板バネ表の `Belastung`） |
| シートレール／ドアヒンジ | ボディ章（英語 本 p112-120／独語 p284-297）にトルク記述が1件も無い。`Scharnier` の6ページはいずれも mmkg を含まない |
| エンジンマウント（500） | **「マウント全体」の指定が無いだけで、値そのものは既にある**＝英語版 本 p74 の下側4本 24.5〜29.4 N·m（18.1〜21.7 lb·ft）を `powerplant-mount-lower` として登録済み。独語版の `TRIEBWERKSLAGERUNG` 章（p89-90）にトルクの記載は無い（mmkg を含まないページ） |

- ⚠️ **「指定が無い」と「どこにも値が無い」は違う。** ホイールシリンダー・マスターシリンダー・エンジンマウントは**別項目に値がある**ので、`no_spec_note` で行き先を書いた（`_readme` に定義あり）。カードには「指定がありません」の下に補足として出る。
- ⚠️ **英語版WSMのブレーキ章（本 p104-111）とボディ章には、トルク値が1つも無い。** lb·ft の全数走査で該当ページが1件も出てこないので確認済み。**次に「ブレーキのトルクが載っているはず」と思っても開き直さないこと。**

## 6. 残タスク

1. ~~**適合をエンジン形式ベースに作り替える**~~ **＝2026-08-13 実装済み。** `models` を廃止し `fits`（`engine` / `chassis`）に置き換え、`engines`（6形式）と `chassis`（2種）の定義をデータに追加。UI に2つのプルダウンを付けた。**選んだエンジン形式の値をまだ持っていない場合は、黙って空にせず橙の警告を出す**（「黙って0件」だと「この形式は締めなくていい」と読まれかねないため）。エンジン系14項目・車体系28項目。
2. ~~**126.A（594cc）のトルク値を 126 Autobook から抽出する。**~~ **＝2026-08-13 完了。** 巻末 本 p112 の一覧表を目視で全項目確認した。
3. ~~126.A1（652cc）のデータ投入。~~ **＝2026-08-13 完了。** 懸念していた低い値は裏が取れた＝`Radlagermutter 5` は**手順項目で正しい**（Autobook も 0.7 kgf·m で締めて30度戻す）、`Lenkhebelbefestigungsmutter 10` は**Bucheli の誤植**（§3）。
4. ~~出典表記を公開ページに出してよいかのユーザー確認~~ **＝2026-08-13 ユーザー許可。書名をそのまま画面に出してよい。** 数値の出所を辿れることが早見帳の信頼性そのものなので、**出典表示を外さない**。
5. 公開の判断（noindex を外す・導線を付ける）。**どちらもユーザーの明示指示があるまでやらない。**
   - 【2026-08-14 ユーザー方針】**まずテストユーザーに検証してもらい、そのあと正式公開**。テスト中は現状のまま（本番デプロイ済み・`noindex`・導線なし）でURLを渡す。
   - 【2026-08-14 夕・ユーザー確定】**画面はこの状態でテスター依頼に出す。** これ以降は**テスターの反応が返るまで画面を触らない**（先回りで直さない）。導線を付けるときは §6.11（`?type=` で入口から車種を渡す）を同時に決める。
6. ~~**500 側で値が無い5項目**（オイルドレン・ロッカーカバー・エキマニ・キャブ・インマニ）を本文から拾う。~~ **＝2026-08-13 完了（結果は「原典に無い」）。** 全5資料を全数走査して指定が無いことを確定させ、`no_spec: true` として持った。**再調査しないこと**＝§5.3 に探索範囲を残した。
7. ~~`マニュアル/` を `.gitignore` に入れるかの判断~~ **＝2026-08-13 解決。`Documents\registro500-notes\manuals\` へ移動した**（§3 の警告を参照）。repo の外なので gitignore は不要。
8. ~~画面のさわり心地・見栄えの点検~~ **＝2026-08-13 実施。**指摘3点のうち2点を修正（§6.1）。**未対応で残したのは「対象エンジンの行が3行になる」1点**＝全形式に当てはまる12項目で `110.000 479cc ／ …／ 126.A1.000 652cc` を全部並べるため。畳むこと自体は可能だが、**適合はエンジン形式で判断するページなので、畳むなら全形式を開ける導線が必須**。優先度は低いと判断して見送った。

9. ~~**`126A5`（500 R のエンジン `126 A5.000`）を独立IDで追加する。**~~ **＝2026-08-14 完了。ただし独立IDではなく、§6.10 のエンジン系統化に吸収した。**
   - `126A5` は `engines` の `e126.forms` のメンバーとして持つ。値は 126系として自然に共有される。
   - カードごとの注記は出さない（110項目に同じ文が繰り返されるため）。**根拠と留保はエンジン選択欄の注記に一度だけ**出している（`tqEngineNote`／文面は `torque-data.json` の `engines[].note`）。§8 の「根拠と留保を画面に出したうえで共有する」は満たしている。
   - `engineIdFromCar()` は `engineFamilyFromCar()` になり、**判定順の問題そのものが消えた**（`126…`→`e126` / `110…`・`120…`→`e500` の2分岐）。
10. ~~**500 R の車体の出典を探す**~~ **＝2026-08-13 クローズ（§6.8 結果5）。** Fiat 純正の R 差分補遺に車体側の締結部の変更が1件も無く、**R の車体は F/L と同じ**と確定。**再調査しないこと。**

### 6.1 画面まわりの修正（2026-08-13）

| 直したもの | 何が起きていたか |
|---|---|
| **タブを折り返しに**（`.tq-tabs` の `overflow-x: auto` → `flex-wrap: wrap`） | 390px 幅で **7個中3個しか見えていなかった**（表示幅 347px に対し中身 680px）。`scrollbar-width: none` でスクロールバーも消していたため、**横に動かせること自体が分からない**状態だった。ちょうど「駆動系」で切れるので、そこで終わりに見えてしまう。折り返すと3段になるが操作が要らない |
| **「指定がありません」の説明を1行に** | 12項目すべて同じ4〜6行の説明を出しており、`no_spec` が連続する箇所（オイルドレン→ロッカーカバー→エキマニ）が文字の壁になっていた。**理由の説明は §5.3 相当の内容を「使う前に」へ一本化**し、カードには「整備書に指定がありません。下の『ねじサイズから目安を調べる』をご確認ください。」だけ残した。項目固有の `no_spec_note` はカードに残す |

⚠️ **ダークモードは既に手当て済み**＝`.tq-novalue` などは `var(--rg-*)` 経由で、ベタ塗り色は `@media (prefers-color-scheme: dark)` ブロックで全て上書きしてある。入力欄・セレクトにも `background` / `color` を明示済み。`viewport-fit=cover` は無いが、このページは `env(safe-area-inset-*)` も固定下部バーも使っていないので不具合にならない。

### 6.2 ログイン済みオーナーの諸元プリセット（2026-08-13）

ログインしているオーナーの**登録車両から、エンジン形式と車体の初期値を入れる**。`cars.owner_user_id` = `session.user.id` で自分の車を引く（`owner_email` では引かない＝SELECT 権限が無い前提の設計）。

- **閲覧にログインは要求しない。** 未ログイン・車両未登録・supabase-js 未読込のときは**帯ごと出さずに黙って終わる**（サイトの閲覧系ページの標準）。
- `engine_type_select`（`edit.html` のプルダウン14種）→ 当ページのエンジン系統の対応は **`engineFamilyFromCar()`** に集約。括弧書き・空白・ドットを落として頭で判定する。**【2026-08-14 更新】判定は2分岐だけ**＝`126…`→`e126` / `110…`・`120…`→`e500`。**形式まで見ていた頃の「`126A1` を `126A` より先に見ないと誤爆する」という順序依存は消えた。**
- ~~⛔ **`126A5.000` は対応づけない**~~ **＝2026-08-14 解消。`126A5.000` は `e126` に当たる**（500 R のエンジン・§6.8 結果6）。副産物として、誤った旧表記 `126A1.048（594cc・空冷）` も系統では正しく `e126` に落ちる。
- `BIS（704cc・水冷）` は **`'bis'` を返して絞り込みには使わない**（4資料のどれも対象にしていない）。帯には専用の文を出し、**2択も聞かない**。
- `その他` / `不明` / 未記入は **`null`**。ベース車体だけ合わせたうえで、**帯で「積んでいるエンジンはどちらですか？」の2択を聞く**（§6.10）。**推測で埋めない**＝§8 の不変条件そのもの。
- 帯には「すべて表示に戻す」／「この車に合わせる」を置き、**押した状態を保存しない**（次に開けば再びプリセットが効く）。複数台のオーナーはプルダウンで切り替えられ、**手放した車（`is_sold`）は後ろに回す**。
- ⚠️ `onAuthStateChange` のコールバック内で `await supabaseClient.xxx` を呼ぶと**認証ロックでデッドロックする**（サイト内の既知の落とし穴）。ここは `getSession().then(...)` の一本道にしてあるので、書き換えるときも同じ形を保つこと。

### 6.3 選んだ条件を端末に覚える（2026-08-13）

**ログインしていなくても効く。** 早見帳は同じ車の値を何度も見に来るページなので、開き直すたびに選び直させない。`localStorage` の **`torque.fits.v1`** に `{engine, chassis}` の**選択IDだけ**を持つ（個人情報は持たない）。

- ⚠️ **保存するのは本人が選んだときだけ**＝2つのプルダウン操作と、帯の「この車に合わせる」「すべて表示に戻す」「車の切り替え」。**登録車両からの自動プリセットは保存しない。** 自動で入っただけの値を「本人の選択」として固定してしまうため。`applyMyCar(on, byUser)` の第2引数がこの区別。
- ⚠️ **保存済みの選択があるときは、自動プリセットで上書きしない**（`loadMyCar()` の `hasSavedFits()` 分岐）。本人が明示的に選んだものが自動判定より優先。帯は出したまま「この車に合わせる」を押せば戻せる。
- 読めない値・データ側に無いID（区分を変えたとき）は**「保存なし」と同じ扱い**にして落とす。
- ⚠️ **帯の文言は保持フラグではなく `myCarMatches()`（いま実際にその車の条件になっているか）で決める。** フラグで持つと、プルダウンを手で動かしたときに表示が実態とズレる。
- `localStorage` が使えない環境（プライベートブラウズ等）でも例外を握りつぶして機能は落とさない。

### 6.4 作業中に使う画面への作り替え（2026-08-13）

**このページは「整備中に片手で見る」もの**というユーザーの用途確定を受けて、情報を削ぎ、部位名とトルク値を主役にした。

| 直したもの | 中身 |
|---|---|
| 対象行を1行に | `対象: クラシック FIAT 500 / FIAT 126` だけ。絞り込みの説明はプルダウンの注記、項目数はタブ下の件数表示にあるので繰り返さない |
| 登録車両の帯を1行＋展開に | `<details>` の `summary` は「🚗 車名 ／ エンジン形式 に合わせています」だけ（実測38px）。車の切替・注意書き・ボタンは中に畳む |
| タブを4列グリッドに | **`flex-wrap` ではなく `grid-template-columns: repeat(4, 1fr)`**。折り返しだと文字数しだいで3行になるが、グリッドなら幅に依存せず必ず2行。絵文字を外し `areas[].short`（エンジン／燃料点火／駆動系／足回り／ブレーキ／ボディ他）を使う |
| 部位名を大きく | `.tq-card-name` 1rem → **1.25rem**。「どこを締めるか」と「何 N·m か」が対で主役 |
| 文中の N·m を立たせる | `emphasizeNm()` が `24.5〜29.4 N·m` の形を `.tq-nm-inline`（1.25rem・800・アクセント色）で包む。**大きい数値を別に出しているカードでは掛けない**（どちらが規定値か紛れる）。対象＝`procedure`／`no_spec_note`／値なし項目の `notes` |

### 6.5 検索のゆれを受ける（2026-08-13）

**外した実例＝「ホイールボルト」**。項目名が「ホイール取付ボルト」で、間に「取付」が挟まるため部分一致で落ちていた。人は正式名では打たない。2段構えにした。

1. **言い換え表 `SYNONYMS`**（`torque.html` 内）＝別の語で呼ばれるものを結び付ける。タペット↔ロッカー、デフ↔クラウンホイール、ハンドル↔ステアリング、ボルト↔ナット↔ねじ など。
2. **飛び石照合**（`isSubsequence`）＝1段目が0件のときだけ、文字が順番どおり現れるかで拾う。当てすぎるので**画面に「一致する項目はありません。字の並びが近いものを出しています」と必ず断る**。黙って別物を出さない。

⚠️ **やってはいけない書き方**（実測で踏んだ）：
- **言い換え語そのものを検索語に足さない。** 語の該当部分だけを置き換える。足すと「ホイールボルト」が『ナット』を含む**全36項目**に当たった。
- **語を短くする言い換えを入れない**（例「シートベルト→ベルト」）。置き換え後の語がファンベルト等の別項目に当たる。
- **`normalize()` は長音を落とす**ので「プーリー」→`ぷり`となり「スプリング」にも当たる。避けられないため、**名前に当たったものを上に並べる**（`item.__hit` 0=名前 / 1=別名・言い換え / 2=飛び石）ことで実害を抑えている。
- 空振り時の例に出す語は、**実際にその語で当たるものだけ**にする（「デスビ」は該当項目自体が無いので例から外した）。

### 6.6 締め付け順序の図（2026-08-13）

カード内の `<details>`「締め付け順序の図を見る」。**既定は畳む**（作業中の画面で常時場所を取らせない）。

- ⛔ **原典の図は転載しない。番号の並びだけを取り、SVG は当ページで描き起こす**（マニュアルPDFを repo に置かない方針と同じ理由）。図ごとに出典と「描き起こし」である旨を出す。
- 並び（原典の図と同じ左→右）：

| 対象 | 上段 | 下段 | 出典 |
|---|---|---|---|
| 110.000（Nuova 500） | 5・1・4・8 | 7・3・2・6 | 英語版WSM **FIG 1:44**（PDF p23） |
| 110D / 110F / 120 | 8・2・1・7 | 4・5・6・3 | 英語版WSM **FIG 1:46**（PDF p24） |
| 126.A / 126.A1 | 5・1・4・8 | 7・3・2・6 | 126 Autobook **FIG 1:5**（PDF p14） |

- ⚠️ **500 は形式で順序が違う**（Nuova と D 以降で別の図）。データの `sequences[].engines` で持ち、**形式を選んでいるときはその形式の図だけ出す**。
- ⚠️ **図の向きを示す目印は「下辺中央の吸気の合流口」**（2026-08-13 修正）。当初これを**上側に「点火プラグ穴」として描いていたのは誤り**。500 の点火プラグはヘッドの**左右側面に1本ずつ**入っており中央には無い。原典（FIG 1:44 / 1:46）では、左右から来た吸気の通路が「Λ」の形に合流して**下辺中央の張り出したフランジ**に入り、そこにキャブレターが載る。英語版WSM p2 の記述がそのまま根拠：*"The inlet passages merge into a single centralized flange onto which is mounted the carburetter."*（同 p23 の組立手順17でもこの穴を "intake duct hole" と呼んでいる）。**向きを間違えると番号が上下逆になって実害が出る**ので、図を触るときはここを必ず確認する。
- ⚠️ **ホイールボルトには図を付けていない。** 原典の指示は「対角順に締める」だけで番号が振られていないため、番号を振ると原典に無いものを作ることになる。
- 126 Autobook はテキスト層が無いスキャンなので、`pypdf` の `page.images` でページ画像を取り出して目視で読んだ（§7 の手順）。

### 6.7 実機（iPhone）確認からの手直し（2026-08-13）

ユーザーの実機スクリーンショットを起点にした6点。

1. **「クラシック」を外した**。`torque-data.json` の `target` と `chassis[].label`（→「FIAT 500」）。
2. **エンジン形式・車体の枠自体を畳んだ**。`<details class="tq-fit-wrap">` にし、閉じているときは `車: FIAT 500 ／ 110F.000` の**1行だけ**（`renderFitSummary()`）。登録車両と一致しているときだけ末尾に `🚗ご登録の車`。**一度選んだら滅多に変えない設定なので、作業中の画面から場所を取らせない**という判断。
3. **順序の図が出ない件の真因は Service Worker のキャッシュだった**（コードもデータも正しかった）。詳細は下の⚠️。
4. **`emphasizeNm()` が `±` を見ていなかった**。「1.37 ± 0.09 N·m」の **0.09 だけが大きくなり本体の 1.37 が小さいまま**だった。範囲（`〜`）と誤差（`±`）をどちらも省略可の形で並べた。データ中の N·m 全53箇所を新しい正規表現で総当たりして、拾うべきものが全部拾えることを確認済み。
5. **エンジン形式の選択肢を形式名だけにした**（排気量・車名を削除）。あわせて `engineName()` とカードの「対象エンジン」行からも排気量を外し、**全形式が対象なら「全形式共通」と1語で書く**ようにした（4形式並ぶと3行になり、肝心のトルク値が画面から押し出されるため）。
6. **タブに8個目「ねじサイズ」を新設**。絞り込みではなく `#tqSizeTool` へ飛ぶだけなので `is-jump`（破線）で見た目を分けてある。**4列グリッドなのでちょうど 4×2 に収まり、行は増えない**。飛び先には `scroll-margin-top: 184px` を入れてある（sticky な検索欄＋タブの実測高が175px。**ここを削ると見出しが sticky の下に隠れる**）。

⚠️ **JSON がキャッシュから返る問題（同じことを何度も踏む）**

`fetch()` で取る JSON は `request.destination` が空文字になるため、`sw.js` の分岐で**画像でもCSS/JSでもない「その他」に落ちて StaleWhileRevalidate になっていた**＝更新した内容が1回遅れて出る。順序の図もタブ名も、コードは正しいのに古い `torque-data.json` が返っていた。3か所で塞いだ。

- `sw.js`：`.json` を **NetworkFirst** に。オフライン時のフォールバックで `index.html` を返すと `JSON.parse` で落ちるだけなので、`.json` は素直に throw する。
- `_headers`：`/*.json` を `no-store` に（それまで JSON だけルールが無かった）。
- `torque.html`：`fetch('torque-data.json?v=' + DATA_VERSION)`。**`torque-data.json` を書き換えたら `DATA_VERSION` も必ず上げること。** 新しい SW は**ユーザーが更新バナーをタップするまで有効にならない**ので、SW 側の修正だけでは今いる人に届かない。クエリを変えればキャッシュのキーが変わるので、SW の世代に関係なく取り直せる。

⚠️ **車名は registro500 の表記に合わせる**（2026-08-13 ユーザー確定）。ページ本文に「Nuova 500」「500 D / F / L」「ジャルディニエラ」が混在していた。**サイトの車両登録（`edit.html`）が正**で、500系は車種名がすべて `Nuova500`、グレードが `D` / `F` / `L` / `R` / `Giardiniera` / `N Normale` / `Prima Serie`（実データ155台もこの表記のみ）。126側は `Fiat 126` / `Fiat 126E` / `Polski Fiat 126p` などの1階層。したがって本文では **`Nuova500 D` / `Nuova500 Giardiniera` / `Fiat 126`** と書く。**ページ最上部の「対象: FIAT 500 / FIAT 126」と車体の選択肢はこのままでよい**（車名ではなく車種の大分類なので、ユーザー了承済み）。

> 未対応で残しているゆれ＝**エンジン形式の書き方**。当ページは原典どおり `126.A.000` / `126.A1.000`、registro500 の登録は `126A.000` / `126A1.000`（`110`・`120` も `.000` 無し）。**照合は `engineIdFromCar()` が正規化するので機能上の実害は無い**が、画面上の見た目は揃っていない。揃えるかはユーザー判断。

⚠️ **濁点の打ち間違い**：「シリンダー**ベ**ッドナット」（ヘ→ベ）は1文字違うだけなので飛び石照合でも拾えず0件だった。検索の**3段目**として、濁点・半濁点を落として（`stripDakuten`）もう一度飛び石照合する。**1段目・2段目で当たったときは通らない**ので、通常の一致精度には影響しない。

### 6.8 車体3世代（N/D・F/L・R）で締付トルクが違うのかの調査（2026-08-13）

**ユーザーの前提**：500 と 126 は別の車格＝別の車。その 500 の中で**車体は3世代**＝ ①N/D ②F/L ③R。**エンジン以外（車体側）でこの3世代の値が違うのかを確定させたい**、というのがこの調査の問い。

**結果1：N/D と F/L の間で車体側トルクが変わる証拠は無い。**
500 の車体側で値のある **27項目のうち18項目が2資料照合済みで、差あり 0件**。英語版WSMは1957-73を1冊で扱い各箇所に**単一の値**しか出さず、純正独語版は**N/D 専用**。両者が一致するということは、**N/D の値がそのまま F/L 用の値としてWSMに載っている**ということ。これが今ある中で最も強い根拠。

**結果2（最重要）：4資料はどれも 500 R の車体をカバーしていない。**
英語版WSMの表紙 "Models Covered" は **Fiat New 500 / Fiat 500D / Fiat 500F / Fiat 500L / Fiat and Autobianchi Giardiniera の5つだけで、500 R が無い**。全ページを検索しても「500 R」「110R」「126A」「594」の記述は**0件**。126 の2冊は 126 の車体の本で、500R とはエンジン（126A.000）が共通なだけ。**＝ R 世代の車体側の値は、現在の出典では埋まっていない。** 登録車両では R が26台（500系で3番目に多い）。

**結果3：世代で部品が変わることはWSM自身が明記しているが、トルク値は分岐していない。**
lb·ft が出る26ページを全数確認したが、車体側の締付トルクに**グレードの限定が付いた記述は1件も無い**。一方で作りの違いは書いてある＝ 6:11 ギアシフト機構（110F セダンと後期ワゴン）／ 7:7 ドライブシャフトのフレキシブルジョイント（同）／ファイナルドライブ調整手順（500D と 500F セダン・初期ワゴン）／ブレーキの自動調整装置（新型セダンのみ・ワゴンには無い）／リアの車高寸法（500D と ワゴンで別値）。**部品が違うと書いてあってもトルクは分けていない**、という状態。

**結果4：値が実際に分かれている唯一の例＝フロントハブナット**（割ピン式 29 N·m ／ カシメ式 20 N·m）。これは**世代ではなくハブの型**で分かれており、当ページは既に両方載せている。

**結果5（2026-08-13 追記・結果2 の穴が埋まった）：Fiat 自身が「R で車体は変えていない」と書いている。**
`Manuale uso e manutenzione - Fiat 500 R.pdf` の正体は取説ではなく **Fiat 純正『Fiat 500 VARIANTI al libretto uso e manutenzione』**（`603.91.687 - XI-1972`＝R 発売時）だった。冒頭に目的が明記されている＝「500 は現在 *Berlina normale* のみで生産される。**以下に、取説に記載された対応版との差異を示す**」。つまり **L に対する R の差分の網羅リスト**。そこに挙がっている車体側の差は次の3つだけ。

| 章 | R での変更 |
|---|---|
| RUOTE E PNEUMATICI | ホイールが**新意匠・ホイールキャップ無し**（`Ruote a disco di nuovo disegno, prive di coppa`）。タイヤは慣用カーカス、ラジアルはオプション |
| FRENI | ブレーキ液を **DOT 3 指定**（`Liquido FIAT etichetta azzurra DOT 3`）。機構の変更記載なし |
| IMPIANTO ELETTRICO | ストップランプSWが**油圧式→ブレーキペダルによる機械式** |

**サス・ステアリング・ハブ・ブレーキ機構の変更は1件も挙がっていない。** この冊子はドア内ハンドルの形状・窓クランクの型・ジャッキの新型・リアシート背もたれの固定化まで書く粒度なので、締結部が変わっていれば載らない方が不自然。**＝結果1（N/D と F/L は同値）と合わせて、500 の車体側の値は N/D・F/L・R の3世代共通として扱ってよい。**

> ⚠️ **穴の性質が変わった。**「R 専用の出典が見つからない」ではなく「**R では車体が変わっていないと Fiat 自身が書いている**」。**500 R の車体の出典探しはクローズしてよい。**
>
> 残る留保は1点だけ＝**ホイールが新意匠**なので取付ボルト 4.5-5.5 kgf·m がそのまま当たるかは断定できない。ただし**補遺は固定方法の変更に触れていない**ので同一とみなすのが妥当。

**結果6（副産物・エンジン軸の話）：500 R のエンジン型式は `126 A5.000` であって `126 A.000` ではない。**
同補遺の識別データ欄に `Sigla d'identificazione ... **Versione II**` / `Tipo del motore ... **126 A5.000**` と明記（低解像度なので4倍に拡大して確認済み）。同ページの諸元＝594cm³・73.5×70mm・圧縮比 7.5・**18 CV DIN**・Weber 26 IMB（紙面に手書きで 24 の書き込みあり／外部資料は 24 IMB とする）。126 の `126 A.000` は **23 CV**・Weber 28・カム違い・**ダイナモではなくオルタネーター**。**＝両者の違いはカム・キャブ・出力・発電機で、腰下やヘッドの締結部に関わる差は挙がっていない。**

- これで §6.2 の「⛔ `126A5.000` は対応づけない（どれと同じか裏が取れていない）」の**裏が取れた**。`126A5.000` は 500 R のエンジンで、registro500 の `engine_type_select` にも同じ選択肢がある。
- ⚠️ 現在のデータは `126A` を `126.A.000`＝「Nuova500 R / Fiat 126（前期）」としており、**Fiat の型式表記としては不正確**。

**残っている穴**：**独語版だけが出典＝N/D 限定の値**が2項目ある。⚠️どちらも重要保安。
- ブレーキ バックプレート → ナックル（フロント） 2.0 kgf·m（oem のみ）
- ブレーキ バックプレート → トレーリングアーム（リア） 6.0 kgf·m（oem のみ）
- ⚠️ ただし**結果1＋結果5 により、この2項目も N/D 限定ではなく3世代に及ぶと考えてよい**（WSM が N/D と同値を 1957-73 通しで載せ、R で車体は変わっていない）。**照合先探しの優先度は下がった。**

**次にやること**：①~~500 R の車体の出典を探す~~**＝結果5 でクローズ**。②上の2項目の照合先を探す（優先度低）③この調査結果を画面の免責に反映するか判断（下の「保留中の実装案」④）。

### 6.8.1 ⛔ 車体軸を「エンジンのみ」に畳まないこと（2026-08-13 検討・却下）

結果5 を受けて「車体が共通ならタイプ分けはエンジンだけでよいのでは」という案が出たが、**却下**。ユーザーの意図は「**500 の中で世代分けをする必要は無いよね**」の確認であり、**FIAT 500 と FIAT 126 を分けること自体は必要**というのが結論（ユーザー確定）。畳めない理由を数字で残す。

- **110項目のうち車体軸で決まるのが 82項目**（500専用27／126専用49／両対応6）。エンジン軸は 28項目。**ページの中身の4分の3は車体側。**
- **同じ部位名で 500 と 126 の値が食い違うものが6件**（うち実値が違う4件）：

| 部位 | 500 | 126 |
|---|---|---|
| ホイール取付ボルト | 4.5–5.5 kgf·m | 5.0 |
| ステアリングホイール取付ナット | 4.0–5.0 | 5.0 |
| クラウンホイール（リングギア）取付ボルト | 3.2 | 4.5 |
| ブレーキ バックプレート → トレーリングアーム（リア） | 6.0 | 5.5 |

- **エンジンから車体は導けない。** `126A5`→500 R / `126A`→126前期 / `126A1`→126後期 と素の車なら一意に決まるが、**載せ替え車で崩れる**。「500F の車体に 126 エンジン」はまさに §2 が想定した定番で、エンジンだけで判断すると **82項目が126の値に化ける**（ホイールボルトとステアリングナットを126の値で締めることになる）。
- ⚠️ **車体軸の値は `500` と `126` の2つだけで、500 の中の世代分けは最初から持っていない。** したがって結果5 の「3世代共通でよい」という結論に対して、**データ・画面ともに変更は不要**（確認して終わり）。

**選択操作の見せ方**（1つのプルダウンで車を選び、載せ替えのときだけ2軸に展開する案）は提示したが**ユーザー判断保留**＝着手しない。

### 6.10 エンジン軸を「系統2択」に作り替えた（2026-08-14・ユーザー確定）

**きっかけ**＝ユーザーの整理「500 と 126 の区別は必要だが、500 の中でボディ形状の区別は不要になった。この前提でエンジン形式とセットになる部分の最適解を、現状に囚われずに構築したい」。

#### 作り替えを決めた根拠（全数検証・数字を残す）

| 検証 | 結果 |
|---|---|
| `fits.engine` を持つ28項目のパターン | **3通りだけ**＝500系4形式ちょうど12件／126系2形式ちょうど10件／全6形式6件。**家族の途中で切れている項目は0件** |
| `notes` / `cross` / `procedure` の本文に形式差の記述 | **0件** |
| 形式まで見ている唯一の箇所 | **ヘッド締付順序の図のみ**（`110`＝FIG 1:44／`110D`・`110F`・`120`＝FIG 1:46）。トルク値は共通 |
| 登録車168台の系統自動判定 | **124台（73.8%）が当たる**。形式で見ていた頃は `126A5.000`（6台）を取りこぼしていた |
| 未記入・不明・その他 | **44台（26.2%）** |
| 500車体に126系エンジン | **35台（21.6%）**＝2軸を1本に畳めない理由の実数 |

**＝エンジン形式を6つに分けても値は1つも増えない。** 細かい形式は「精度がある」という誤った印象を与えたうえ、形式を知らない26%を「すべて表示」に落としているだけだった。

#### 実装した形

- `engines` は **`e500`（500系エンジン・479〜499cc）／`e126`（126系エンジン・594〜652cc）の2つだけ**。`fits.engine` も系統IDに置き換えた（28項目）。
- **形式は消していない**＝`engines[].forms` にメタデータとして残し、①`engineFamilyFromCar()` の判定 ②順序図の出し分け ③将来の逃げ道 に使う。
- ⚠️ **逃げ道＝`fits.engine` には系統ID・形式IDのどちらも書ける**（`fitsSelection()` と `engineDef()` が両方受ける）。後日「110F だけ値が違う」資料が出たら、その項目だけ `fits.engine: ["110F"]` と書けば形式単位で分岐する。**この受け口を潰さないこと。**
- **順序図は選ばせずに該当分を全部出し、車名ラベルで見分けさせる**（500系なら2枚）。`sequences[].label` を車名先頭に書き換えた（`Nuova500 N（エンジン 110.000）` 等）。**エンジン形式は知らなくてもグレードは知っている**、という判断。
- 車体軸を **「ベース車体」に改名**（§6.9-4 を採用）。免責に「ベース車体側の値は N/D・F/L・R 共通。根拠＝Fiat 純正の R 差分補遺に足回り・ステアリング・ブレーキ機構の変更が1件も無いこと」を1文足した。
- **判定不能の車には帯で2択を聞く**（「積んでいるエンジンはどちらですか？ 500系（479〜499cc）／126系（594〜652cc）」）。押した答えは `myCarAnswer` にこの場かぎりで持ち、`saveFits()` で端末に残す。**DBの登録内容は書き換えない。**
- **保存済み設定の移行**＝`loadSavedFits()` が旧・形式ID（`110F` 等）を属する系統へ読み替える。`torque.fits.v1` のキーは据え置き。

#### ⚠️ 実装中に踏んだ不具合（同じ形を作らないため）

**`myCarMatches()` は `engineFamilyFromCar()` ではなく `usableEngineFamily()` で比べること。** BIS のように「判定はできるが絞り込みに使えない」値があると、`applyMyCar()` が入れる `'all'` と `'bis'` が永久に一致せず、**帯が「合わせています」にならず、ボタンを押しても状態が変わらない**（実測で踏んで修正済み）。

#### 検証済みの項目（2026-08-14・ローカルサーバ＋Playwright）

- 4通りの組み合わせで件数と値。**決定打＝`126系` × `FIAT 500`（500 R・載せ替え車）でヘッドが 4.0 kgf·m（126の値）、ホイールボルトが 4.5 kgf·m（500の値）**と正しく混ざること
- 順序図が 500系で2枚（車名ラベル付き）・126系で1枚
- 保存済み設定の移行（`110F`→`e500`／`126A5`→`e126`／未知ID→`all`で車体は保持）
- 帯の8ケース（`126A5.000`／`110F.000 （ABA205）`／未記入／その他／旧表記 `126A1.048（594cc・空冷）`／BIS／`120`／`110.004`）と、2択を押したあとの反映・保存・ボタンの消え方
- 390px 幅で「対象エンジン」行が1行に収まり、横はみ出しが無いこと

### 6.9 保留中の実装案

**2026-08-14 に1〜4すべてクローズ。保留中の実装案はゼロ。** 新たに立った項目は §6.11（入口で車種を渡す＝公開判断とセット）だけ。

1. ~~**エンジン形式の表記を registro500 に合わせる**~~ **＝2026-08-14 クローズ（現状維持で確定）。⛔再検討しない。**
   - §6.10 の系統化で形式が選択肢から消えたため、**画面に形式表記が出るのは注記と順序図のラベルの中だけ**になった。
   - 全箇所を突き合わせた結果、**ズレは `110.000` と `120.000` の2つだけ**（registro500 の登録値は `110` / `120`）。`110D.000` / `110F.000` / `126A.000` / `126A5.000` / `126A1.000` / `126A1.048` は一致している。
   - **残ったズレは原典（Fiat / Autobooks）の正式表記が当ページ側**で、登録プルダウンが略しているだけ。照合は `engineFamilyFromCar()` が正規化するので**機能上の実害はゼロ**。直す理由が無い。
2. ~~**判定の参照先を `engine_type_select` → `engine_display` に広げる**~~ **＝2026-08-14 クローズ（実装しない）。⛔再検討しない。**
   - 判定できない44台の `engine_display` を全数確認した結果、**手がかりがあるのは5台だけ**＝`126A.000` / `120000` / `126A1.076`×2 の**4台が自動で当たるようになる**だけ。`EN07` / `2003年式スバルサンバー` の2台は**スバルエンジンへの載せ替えで、当たらないのが正しい**。残り38台は空欄か「不明」で効果が無い。
   - その4台も**帯の2択を1回押せば同じ結果**になる（§6.10）。割に合わないので実装しない。
   - ⚠️ 記録＝**スバルエンジンの2台にも2択は出る**（「どちらでもない」の選択肢は無い）。押さなければベース車体の82項目は正しく当たるので実害は小さい。**2台のために3つ目のボタンを足さない。**
3. ~~**帯から車名を消す**~~ **＝2026-08-14 実装済み（`ed65f58`＋`7fdfcc7`）。最終形は「車ではなくエンジンの話だけをする」。**

   **ユーザーの整理（この設計の根拠・勝手に戻さない）**＝「500 の人は 500 の入口から、126 の人は 126 の入口から来ているので、**そもそも車は合っている前提**。オーナーの自覚では**エンジンだけが選択事項**。だから車については触れなくていい」。

   裏付け＝ベース車体は `cars.car_type` と1対1で**外れようがない**（判定精度100%）。外れうるのは**エンジンだけ**（登録値から判定できるのは74%）。**確認を求めるのは外れうる方だけにする。**

   | 状態 | 帯の1行目（`myCarHead()`） |
   |---|---|
   | 絞り込み中・エンジン判定済 | `⚙️ エンジンは 500系 に合わせています` |
   | 絞り込み中・判定できない（未記入/その他） | `⚙️ エンジンを選んでください` ＋ 2択ボタン |
   | 絞り込みを外した状態 | `⚙️ すべて表示中` ＋「ご登録の内容で絞り込む」 |
   | BIS（704cc・水冷） | `⚙️ エンジンは資料の対象外です`（2択を出さない） |

   - ⚠️ **「すべて表示中」を依頼文にしない。** 最初は `ご登録のエンジンに合わせる` と書いたが、**エンジン軸だけ見ると「判定できない」状態と同じ（どちらも絞られていない）**ため、ユーザーから「違いが分からない」と指摘された。**片方を状態、片方を依頼にすると必ず混ざる**＝どちらも状態として書く。
   - ⚠️ **BIS を「選んでください」に混ぜない。** 選択肢に無いものを選べと言うことになる。
   - ⚠️ **「エンジンを選んでください」を消さない。** 判定できない車が26%（44台）あり、黙って「合わせています」と出すと**エンジン側も絞れていると誤読される**。これが2択に答えてもらう入口。
   - ⚠️ ラベルは `engines[].label`（`500系エンジン`）から末尾の「エンジン」を落として使う（`エンジンは 500系 に…` の重複回避）。
   - ⚠️ **車名は絞り込みに一切使っていない**ことをコードで確認済み（`fitsSelection()` が見るのは `currentEngine` と `currentChassis` だけ。`model_display_a` の出現は表示用の2行のみ）。複数台オーナーのプルダウンは（エンジン形式, 車体）で重複排除する（3人しかおらず、うち1人は2台が完全に同条件＝選ばせる意味がない）。
   - ⚠️ 常時見える上の1行（`renderFitSummary()`）は `車: FIAT 500 ／ 500系エンジン` のまま**据え置き**。ベース車体がまだ選択軸として画面に出ている以上、そこは表示する必要がある（下の §6.11 が入れば見直す）。

### 6.11 【未実装・公開時にセットで決める】入口で車種を渡す

上の「車は入口で決まっている」という前提は、**いまはまだ成立していない**。`/torque` は導線が無くURL直打ちで来るため、**未ログインで開くとベース車体もエンジンも「すべて」＝110項目が全部並び、500 の人にも 126 のヘッドナット（4.0）が混ざる。**

- `edit.html` が既に `?type=126` を受ける作りになっている（`edit.html:673`）。同じ形で **500 側の導線から `/torque?type=500`、126 側（`/126/`）から `/torque?type=126`** を渡せば、入口で `currentChassis` が決まる。
- **導線そのものはユーザーの明示指示があるまで作らない**（§6-5）。これは**公開判断のときに導線とセットで決める項目**として置いてある。
- 実装するなら `loadSavedFits()` より**後**に読む（端末に保存済みの本人の選択を、URL パラメータで上書きしないため）か、保存が無いときだけ効かせる。
4. ~~**車体軸を「ベース車体」に改名**~~ **＝2026-08-14 実装済み（§6.10）。以下は判断の記録。** 免責に因果を1項目書いた。→ **どの車にも必ずベース車両がある**というユーザーの整理に合わせる。`cars.car_type`（500/126）が既に「ベース車体」を表しており（Steyr-Puch 650T も Giannini 126 GP も正しく入っている）、早見帳の2軸はそれと1対1。**選択肢に Abarth 等を足すのは却下**＝4資料のどこにも派生車の値が無く、選べる＝値がある と誤読されるため。注記で「ベース車体の値は素のまま残っている部品にしか当たらない」と断る
   - 【2026-08-13 追記】**§6.8 結果5 で「500 の車体側は N/D・F/L・R 共通」が裏付けられた**ので、500 の車体を1つで持つ現設計が正しいと確認できた。免責にはこの因果をそのまま書いてある。
   - ⚠️ **選択肢に Abarth 等を足さない判断は生きている**（改名しても選択肢は `FIAT 500` / `FIAT 126` の2つのまま）。

## 7. 作業環境のメモ

- **OCR 環境がない**。`tesseract` バイナリと `poppler`（pdftoppm）が未インストール。`pypdf` / `pytesseract` / `pdf2image` は入っている。
- スキャンPDFは **`pypdf` の `page.images` でページ画像を取り出せる**（ページ全体が1枚の JPEG）。取り出して画像として読めば内容は追える＝126 の表もこの方法で読んだ。全ページを機械処理したい場合のみ OCR が要る。
- **Windows 標準 OCR は管理者権限なしで使える**（`winocr` は winrt 3.x と非互換なので `winrt.windows.media.ocr` を直接叩く）。ただし**英語の言語パックは管理者権限が要るため入っておらず、日本語エンジンで欧文を読むと数値の列が崩れる**。
- ⚠️**OCR は「どのページにトルク記述があるか」の探索にだけ使う。数値そのものは、絞り込んだページを画像で目視して読む。** 126 の全項目もこの手順で確定させた。
- ローカルでの表示確認は `python -m http.server` を立ててから開く。**`file://` だと `fetch('torque-data.json')` が CORS で失敗して真っ白になる。**

## 8. 不変条件（勝手に変えない）

- **エンジン系統で適合を判断する**（§2・§6.10）。車種だけで切らない。**形式（110F.000 など）まで分けない**＝値が形式で分岐している項目は0件だと全数検証済み。
- **ただし形式単位で分岐させる受け口は残す。** `fits.engine` には系統ID・形式IDのどちらも書ける。新しい資料で「この形式だけ値が違う」と分かったら、その項目にだけ形式IDを書く。**この受け口を潰さないこと。**
- **N·m はデータに持たない**（表示時計算）。**ただし注記の文中には N·m を必ず併記する**（§4 の「単位の書き方」）。
- **DB化した原典の値を書き換えない**。資料間で違うときは片方を消さず、両方を出典付きで残して差を明示する。
- **原典に無い値を換算で作らない。** 片側しか記載が無ければ片側だけ入れて、もう片側は null にする。埋めると原典にある値のような顔をしてしまう。
- **値が無い項目を消さない**。
- **「締めっぱなしにしない箇所」は数値を大きく出さず手順を主役にする**（フロントハブナット・リアホイールベアリング・デフのプリロード）。数字だけ大きく出すと事故になる。
- **同じ部位でも 500 と 126 で値が違うものがある**（ヘッドナット・クラウンホイール等）。項目を1つにまとめず分けて持ち、**カードに対象（エンジン系統／ベース車体）を必ず出す**。名前だけで見分けさせない。
- **導線を付けない・noindex を外さない**（ユーザーの明示指示があるまで）。

## 9. 【2026-08-13 追記】新資料 Haynes 2冊の照合（⚠️ データ反映は未実施＝次セッションの残作業）

ユーザーが `C:\Users\akayu\Documents\ucm1\マニュアル\` に資料を追加した。**照合はすべて完了・判断もユーザー確定済みだが、`torque-data.json` への反映はまだ行っていない。** 再開はここから。

### 9.1 追加されたファイルと判定

| ファイル | 正体 | 判定 |
|---|---|---|
| `FIAT500_WorkshopManual.pdf` 87p | **Haynes『FIAT 500 1957-73』日本語版**（原書 Haynes 1992／ヴィンテージ・パブリケーションズ 1997） | **新規の一次資料。採用** |
| `FIAT126OwnersWorkshopManual.pdf` 84p | **Haynes『Fiat 126 Owners Workshop Manual』英語版**（1990・J H Haynes & P G Strasman） | **新規の一次資料。採用** |
| `F500_Manual.pdf` 128p | 既存の Autobooks 英語版WSM と**同一** | ⚠️ **再走査しない** |
| `MANUALFIAT126.pdf` 131p | 既存の 126 Autobook 853 と**同一** | ⚠️ **再走査しない** |
| `FIAT500_MasterBook.pdf` 110p | 『FIAT500 マスターブック』STUDIO TAC CREATIVE（日本語ムック） | ⛔ **トルクの出典にしない** |
| `partslistvol.7.pdf` 72p | パーツリスト（部品番号図） | ⛔ 同上（ねじサイズの参考どまり） |

> ⚠️ PDF は repo に置かない方針（§3）を維持。上記も `ucm1\マニュアル\` に置いたままにする。

### 9.2 新資料2冊の性格とトルク表の所在

| 資料 | 対象 | 単位 | トルク表のページ |
|---|---|---|---|
| **Haynes 500 日本語版**（`haynes500`） | 479 / 499 / **594cc**。p19 の整備情報に **1973年型＝エンジン 126.000・594cc・23bhp・4.0kgf·m** と明記＝**500 R をカバーする初めての資料** | kgf-m | エンジン **p22**／クラッチ **p83**／ミッション **p90**／ブレーキ **p107**／リアサス **p137**／フロントサス・ステア **p143** |
| **Haynes 126 英語版**（`haynes126`） | 126 全モデル 594 & 652cc | **lb f ft と Nm の2列** | エンジン **p13**／ギアボックス **p53**／ブレーキ **p66**／サス・ステア **p89** |

- ⚠️ **Haynes の Nm 列は丸め**（29 lb·ft→40 Nm）。Bucheli と同じく正確な換算ではないので、**当ページの N·m（kgf·m×9.80665）と食い違うのは正常**。
- ⚠️ Haynes 500 日本語版の**燃料系（p67）にキャブ・インマニのトルク指定は無い**＝§5.3 の no_spec は維持で正しい。

### 9.3 照合結果

**値の食い違いはほぼ皆無で、既存110項目の正しさが第3の独立資料で裏付けられた。** 主な収穫は3つ。

**① §6.8 の「残っている穴」が埋まった（重要保安・最大の収穫）**
独語版だけが出典で「N/D 限定では」と留保していた2項目が、**R 世代までカバーする Haynes500 p107 と完全一致**した。

| 項目 | 既存（oem のみ） | Haynes500 p107 |
|---|---|---|
| ブレーキ バックプレート → ナックル（フロント） | 2.0 | **2.0** |
| ブレーキ バックプレート → トレーリングアーム（リア） | 6.0 | **6.0** |

→ **§6.8 の「次にやること②（照合先探し）」はクローズしてよい。**

**② Bucheli の誤植が3資料目で確定**：`pitman-arm-nut-126` は Haynes126 p89 が **Drop arm nut 72 lb·ft／100 Nm**＝Autobook の 10.0 kgf·m 側。Bucheli の `10`（＝1.0）は誤植で確定。§3 の判断は正しかった。

**③ 126 の資料間対立4件すべてで Haynes が Autobook（＝現在の主値・低い方）を支持**

| 項目 | Autobook（主） | Bucheli（cross） | Haynes126 p89 |
|---|---|---|---|
| ホイール取付ボルト | 5.0 | 7.0 | **36 lb·ft/50 Nm＝5.0** |
| 板バネ ゴムマウント | 3.0 | 5.0 | **22/30＝3.0** |
| ロアアームシャフト→ボディ | 3.0 | 5.0 | **22/30＝3.0** |
| ロアアーム→キングピンハウジング | 6.0 | 7.0 | **44/61＝6.0** |

**④ そのほかは全項目一致**（500側27項目・126側30項目）。**126のシリンダーヘッドナットも Haynes126 p13 で 29 lb·ft／40 Nm ＝既存 4.0 kgf·m と一致。**

### 9.4 ⛔ ユーザー確定の判断（2026-08-13・これに沿って実装する）

1. **Haynes 2冊を `sources` に第3の一次資料として追加し、一致した項目の `cross` に足す。値そのものは1つも変えない。**
   - `torque.html` は `cross.length + 1` で「✓ ◯資料で一致」を数えている（`torque.html:904-906`）ので、**HTML の改修は不要**。データに足すだけでバッジが「✓ 3資料で一致」に増える。
2. **エキゾーストマニホールド → シリンダーヘッド：`no_spec` を解除し、`2.5 kgf·m`（出典 Haynes500 p22）を実値として登録する。**
   - ⚠️ これに伴い **§3 の「atwiki のエキマニ 2.5 は当方の全資料に無い＝出所不明だから採らない」という記述を書き換える**。出所は **Haynes500 日本語版 p22 だった**と判明した（atwiki はここからの転記と見られる）。
   - Autobooks・Fiat 純正独語版・Bucheli には無い旨は `notes` に残す。
3. **カムシャフトギア（500）は `0.9` を主値に変更**し、独語版の `1.1` を `cross`（不一致）へ回す。
   - 根拠＝英語版WSM 0.9 と Haynes500 p22 の 0.9 で **2対1**。当初「1.1 のまま」で決めかけたが、ユーザーが「haynes に変更しよう」と判断を変更した。**こちらが最終。**

### 9.5 ⚠️ 判断を保留した1件（次セッションで扱う）

**遠心オイルフィルター系の「8」**：§3 で「atwiki の `8.0` は原典 15.0 の半分で緩む。採らない」と切っていたが、**その出所も Haynes500 p22「プーリーのオイルフィルターカバー 8」だった**と判明。
- ただし既存の 15.0 は **Fiat 純正独語版 15000 mmkg・Autobooks 108.5 lb·ft・Haynes126 108 lb·ft/149 Nm の3資料が支持**しており、**15.0 の維持が妥当**。
- 未確認なのは「Haynes500 の言う 8 が**どの部品を指すか**」（中空ボルト本体か、カバーの小ボルト `centrifugal-filter-cover`＝既存 0.8 kgf·m か）。**Haynes500 の該当本文を読んでから決める。** 現時点でデータは触らない。

### 9.6 次セッションの手順（この順で）

1. `torque-data.json` を編集（§9.4 の3件＋一致項目の `cross` 追加）。
2. **`torque.html` の `DATA_VERSION` を上げる**（現在 `'2026-08-14a'`）。⚠️ §6.7 の通り、上げないと SW キャッシュから古い JSON が返る。
3. `python -m http.server` で表示確認（`file://` は CORS で真っ白）。
4. 本 HANDOFF の **§3（atwiki のエキマニ／オイルフィルターの記述）・§5.3（エキマニの no_spec）・§6.8（穴のクローズ）** を更新。
5. `sources` に増える2件の `note` にも「Nm 列は丸め」を書く。

### 9.7 作業環境メモ（PDFの読み方・今回踏んだ落とし穴）

- ⚠️ **`pypdf` の `page.images` は同名 XObject でキャッシュが衝突し、別ページに前のページの画像を返す。** 実際に Haynes500 で誤った内容を読みかけた。**`page['/Resources']['/XObject']` を直接辿って `_data` を取る**こと（DCTDecode ならそのまま JPEG）。MasterBook は全110ページが `/Im23` なので、旧方式だと全滅する。
- **Haynes500 日本語版はスキャンが横倒し**＝`img.rotate(270, expand=True)` で正立する。
- **ページ番号 → スキャン番号の対応**：Haynes500 は **scan14 以降で 左＝2N−6／右＝2N−5**（⚠️ scan10と12、scan11と13が**同じ見開きの二重スキャン**）。Haynes126 は **左＝2N−4／右＝2N−3**（重複なし）。
- **ページ番号を一望する方法**：各ページの左上・右上の隅だけを切り出して並べたコンタクトシートを1枚作れば、全ページのノンブルが一度に確認できる。当たりを付けるのが速い。
- ⚠️ **Windows OCR は日本語エンジンしか入っておらず、欧文は完全に壊れる**（§7 の記載どおり）。「どのページに表があるか」の探索にすら使えない。**サムネイルを目視**して当たりを付け、表だけ高解像度で切り出して読む。
