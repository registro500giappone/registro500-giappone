# 締め付けトルク早見帳 引き継ぎ書

最終更新: 2026-08-13

## 1. これは何か

`torque.html`（1ページ完結の検索型トルク早見帳）と `torque-data.json`（データ本体）の設計・判断の正本。

- **本番デプロイ済みだが導線なし＋`noindex`＝限定公開の作業場**（`https://www.registro500.com/torque` を直接開けば見える）。正式公開の判断はユーザーが出す。`<meta name="robots" content="noindex">` を入れてある（公開時に1行外す）。
- N·m は**データに持たず表示時に計算**する（`kgm × 9.80665`）。二重管理を避けるため。
- 表示は「値」より**手順を優先**（`procedure` があるとそちらを主役にする）。

## 2. ⚠️ 最重要の設計判断：適合はエンジン形式で分ける

**2026-08-13 ユーザー確定。** 車種（500 / 126）で分けてはいけない。

> 古いクルマなので車両によってエンジンを載せ替えているケースが多数。よって判断は車両よりエンジン形式にした方がいい。

これは載せ替え対策であると同時に、**メーカーの素の仕様からして車種で切ると破綻する**。

- **500 R（1972-75）は 126 のエンジン（126.A・594cc）を積んでいる。**
- したがって「対象: クラシック FIAT 500（Nuova / D / F / L / R）」という現在のページ表記は**誤り**。R のエンジンは既存データの対象外。**要修正**。

### エンジン形式の一覧

| コード | 排気量 | 搭載車 |
|---|---|---|
| 110.000 | 479cc | Nuova 500 |
| 110D.000 | 499cc | 500 D |
| 110F.000 | 499cc | 500 F / L |
| 120.000 | 499cc（横倒し） | Giardiniera / Autobianchi |
| 126.A.000 | 594cc | **500 R** / 126 前期 |
| 126.A1.000 | 652cc | 126 後期 / Bambino |

### 二軸で持つ

エンジン形式だけでは足りない。サス・ブレーキ・ステアリング・ボディは車体側の値なので、**エンジン系と車体系を別の軸で持つ**。

- エンジン系（ヘッド・コンロッド・フライホイール・プーリー・プラグ・ロッカー・オイルフィルター・ジェネレーター）→ **エンジン形式**で適合
- それ以外（ミッション・デフ・サス・ブレーキ・ステアリング・ボディ）→ **車体**で適合

これで「500F の車体に 126 エンジンを載せた車」を正しく引ける。

## 3. 資料と、その性格（★ここを読まずに数値を足さない）

現物は **`Documents\registro500-notes\manuals\`**（手持ち）と **`manuals\FD Ricambi\`**（2026-08-13 に取得）。

> ⚠️ **このリポジトリの中に置かないこと（2026-08-13 ユーザー確定）。** 当初は repo 直下の `マニュアル/` に置いていたが `registro500-notes` へ移した。理由＝①**push すると Cloudflare Pages が自動デプロイするので、入れた瞬間に `registro500.com/...` で誰でも落とせるようになる**。Autobooks・Fiat 純正・Bucheli はいずれも著作権のある商業出版物で、再配布は権利侵害 ②5ファイルが Cloudflare Pages の1ファイル 25MiB 上限を超えていてビルドが通らない ③一度コミットすると 338MB が `.git` に残り、履歴の書き換え無しには消せない。

### 使ってよい資料

| 資料 | 対象 | 単位 | 性格 |
|---|---|---|---|
| `FD Ricambi/Fiat 500 Workshop Manual 1957-1973 (EN).pdf` 128p | 110 / 110D / 110F / 120 | lb·ft | Autobooks。**lb·ft の原値**。テキスト層あり |
| `FD Ricambi/Fiat 500 D Reparaturanleitung (DE).pdf` 336p | 110 / 110D | mmkg | **Fiat 純正**。**kgf·m の原値**。トルク一覧表は**章ごとに7つ**＝p57（エンジン）/ p128（ミッション・デフ）/ p146（フロントサス）/ p161（リアサス）/ p179（ステアリング）/ p193（ブレーキ）/ p205-206（シャシー総括）。テキスト層あり |
| `FD Ricambi/Fiat 500 Giardiniera Reparaturanleitung (DE).pdf` 39p | **120（ジャルディニエラ）** | mmkg | **Fiat 純正の補遺**。全ページ**スキャン画像**（テキスト層なし）。トルク表は PDF p16（本 p18・エンジン）/ p28（ミッション・デフ）/ p38（サス・駆動・ホイール）/ p39（ステアリング）。**エンジン表は 500 D の p57 と同一構成・同一部品番号**＝120 に固有のトルク値は無い。したがって現状ここを原値にした項目は無く、**「120 にも同じ値でよいか」の裏取りに使う** |
| `FD Ricambi/Fiat 126 Reparaturhandbuch (DE).pdf` 94p | **126.A1（652cc）のみ** | **N·m** | Verlag Bucheli。純正ではない。トルク表は本 p91-92（PDF p93-94）。スキャン画像。**単位は本 p91 に明記**＝「Angaben in Nm; um mkp-Werte zu erhalten, sind die Nm-Werte durch 10 zu dividieren」 |
| `FD Ricambi/Fiat 126 Workshop Manual Autobook 853 1972-1976 (EN).pdf` 131p | **126.A（594cc）** | lb·ft（kgm 併記） | Autobooks。**500 R のエンジン値を得る唯一の資料**。**巻末 本 p112（PDF p113）に締付トルクの一覧表がある**＝126 の値はここを原値にした |

### ⛔ 使ってはいけない / 注意が要る資料

- **FD Ricambi のサイト上のトルク表**（`/en/tech-center/.../tightening-torques-for-classic-fiat-500/`）
  - 「500用」と題しているが、**中身は 126 Bucheli 整備書 p91-92 の値**（ヘッドナット40・主軸受30・コンロッド35・プーリー150・プラグ30 …が完全一致）。
  - **ページに単位の表記がない**。Bucheli 側に「Angaben in Nm」とあるので N·m。
  - 500 のヘッドナットは実値 3.3 kgf·m＝32 N·m。**この表の 40 を 500 に使うと締めすぎになる。**
  - 脚注 `*` `**` `***` の説明文がページに存在しない（`Cylinder head nuts 40*`、`Hub nuts see ***`）。
  - 一部は Bucheli とも食い違う（Wheel bolts 50 / Bucheli 70）。**出所不明の混成。数値の出典にしない。**
- **atwiki「fiat500-onlinemanual」の締め付けトルク2020**（`w.atwiki.jp/fiat500-onlinemanual/pages/285.html`）— 日本語で読める一覧なので見つかりやすいが、**出典の記載が無く、原典と食い違う行が混ざっている。数値の出典にしない。**（2026-08-13 に全27行を照合）
  - 大半は当ページと同じ原典の転記だが、**プーリーのオイルフィルターカバー `8.0 kgf·m` は原典 15.0（15000 mmkg／英語版 108.5 lb·ft）のほぼ半分**。この値で締めると緩む。**カムチェーンスプロケット `0.9` も原典 1.1（1100 mmkg）と違う。**
  - ⚠️ **「エキマニ<>シリンダヘッド 2.5 kgf·m」の1行だけ、当方が全数走査した5資料のどこにも対応する記述が無い。** 出所不明なので §5.3 の判断（エキマニは原典に指定なし）は変えない。**この行を見つけても採らない。**
  - ⚠️ WebFetch は 403 で読めない。中身を見るならブラウザ（Playwright）で開く。
- **`manuals/FIAT500マニュアル.pdf`（128p）** — 上記 Autobooks 英語版の機械翻訳。原典を入手済みなので**役目を終えている**。訳で数値が崩れている（`1 08.5 Ibft`、`lb`→`Ib`、「プラグ」と「pins」の取り違え）。**これを出典にしない。**
- **イタリア語の取説5冊**（`Fiat 500 D uso e manutenzione` 他）— スキャン画像・テキスト層なし。取説なので整備トルクは期待薄。

### 資料そのものの誤り（判明済み・再調査しないこと）

- **Autobooks 英語版はリレーレバーのピン締付を二重に記載している。** p94「39 to 43 lb ft」／p95「78 to 85 lb ft」（約2倍）。Fiat 純正独語 p179 が `5500—6000 mmkg`＝39.8-43.4 lb·ft なので、**p94 側が正しい**。データは 5.5-6.0 kgf·m を採用済み。
- Autobooks は 120.000 エンジンについて「**シリンダーヘッドナットの締付順序が違う**」と明記（FIG 1:44 と FIG 1:46）。トルク値は共通だが順序が別。
- **Bucheli（126・652cc）のピットマンアームは桁を落とした誤植。** `Lenkhebelbefestigungsmutter 10` は N·m として読むと 1.0 kgf·m だが、594cc の Autobook は **10 kgf·m（72 lb·ft）**、500 も 10〜11 kgf·m。**Bucheli 側が誤り**と判断してデータは 10.0 kgf·m を採った。FD Ricambi のサイトの表もこの「10」をそのまま転載している。**再調査しないこと。**
- **Bucheli は同じ箇所を二重に載せて値が食い違う行がある。** ①ブレーキバックプレート→ナックル＝足回りの表 3.0／ブレーキの表 2.0（Autobook と一致する **2.0** を採用）②クラッチハウジング→エンジン＝一方が 2.5、他方が 2.5〜3.5（Autobook と一致する **2.5** を採用）。

## 4. データの持ち方

- `kgm` / `kgm_max` … **Fiat 純正独語版の記載値**（mmkg ÷ 1000）
- `lbft` / `lbft_max` … **Autobooks 英語版の記載値**
- どちらも原典の記載をそのまま入れる。⚠️**原典に書かれていない値を換算で作らない**（2026-08-13 是正済み。片側しか記載が無ければ片側だけ入れ、もう片側は null にする）

### 英語版WSMの lb·ft について（誤解しやすい）

⚠️ **英語版WSM の lb·ft が「kgf·m × 7.23301」と小数1桁までぴったり一致するのは正常。** 元がイタリア語の kg m なので、換算した数字をそのまま印刷しているため。**「換算値だから原典に無い」と早合点しないこと**（2026-08-13 に一度これで誤診した）。

- **章によって書き方が違う**。エンジン〜駆動系は小数1桁（`18.1 Ibft` / `23.9 Ib ft` / `108.5`）、ステアリング章は整数（`14 to 18` / `39 to 43` / `72`）。どちらも原典の記載。
- ⚠️**抽出するときは表記ゆれに注意**。`lb ft` / `Ib ft` / `Ibft` / **`Ib/ft`（スラッシュ）** が混在する。スラッシュ形を落とすとステアリング章がまるごと漏れる。正規表現は `(?:lb|Ib)\s*[/. ]?\s*ft`。
- **全項目を原典と突き合わせ済み**（2026-08-13）。換算で埋まっていたのは4箇所だけで、すべて是正した＝pitman-arm-nut と front-swingarm-hub-nut の lb·ft 上限（原典は単一値）／brake-backplate-front・rear の lb·ft（出典は独語版で kgf·m のみ、英語版に対応記述なし）。

### 単位の書き方（2026-08-13 ユーザー確定）

**注記・手順・差分説明の中でトルク値に触れるときは、必ず N·m を先頭に書く。**

- 理由＝**日本で流通するトルクレンチは N·m が主流**（ユーザー本人のレンチも N·m）。注記の中だけ kgf·m や lb·ft しか無いと、N·m のレンチしか持たない人がその値を使えない。
- ⚠️**併記する N·m は kgf·m × 9.80665 の計算値に統一し、原典が書いている N·m をそのまま写さない。** カード上部の大きい N·m と食い違うため。
- **Bucheli と FD Ricambi の「N·m」は kgf·m を10倍しただけの簡便表記**で、正確な換算ではない（Bucheli 自身が「10で割れば mkp」と書いている）。だから紙面の 70 は当ページでは 69 になる。この説明は `sources.buch126.note` に入れてある。
- 手順項目（`procedure`）は `kgm` が null で**カード上部に単位が何も出ない**ので、手順の文中に lb·ft も残す。値のある項目の `notes` はカードに3単位とも出ているので「N·m（kgf·m）」でよい。
- ⚠️`diff-rolling-torque-126` の **kgf·cm は kgf·m に直さない**（0.13 kgf·m のような読みにくい数字になる）。N·m は併記する。
- `cross` … 2つ目以降の出典と、値が一致したか（`agree`）。画面に「✓ 2資料で一致」バッジが出る。不一致なら橙の「⚠ 資料間で値に差あり」
- `cross[].kgm` / `kgm_max` / `note` … **不一致のときだけ入れる**。相手の資料の値と、なぜ差があるかの説明。画面に「別の資料では ○○ N·m」の枠で出る。**どちらを採るかは読む人が決められるようにし、片方を伏せない**
- `procedure` … 角度戻し等、単純なトルク値で表せない箇所。値より優先表示
- `critical` … 重要保安部位（ブレーキ・ハブ・ホイール・ステアリング）。カード左端に赤帯

## 5. 現状（2026-08-13 時点）

- **110項目**（うち 126 向けが59項目）。値あり98・値なし12＝**この12項目はすべて §5.3 の通り「原典に指定なし」と確定済みで、未調査の項目は1つも無い**。**2資料で照合済みが合計68項目**。
- **【2026-08-13 追記】原典の再照合で未収録だった9項目を拾った（§5.2）。**
- **126 の投入が完了**（下記 §5.1）。これで **500 R もカバーできた**＝エンジン 126.A ＋ 車体 500 を選ぶと 44項目・値32件が出る。**エンジン形式を選んで値が0件になる組み合わせは無くなった。**
- 42項目（値あり28・手順2・値未登録12）だった時点の経緯は以下。
- 2026-08-13 に Fiat 純正独語版と全項目を突き合わせ、**5件を純正の原値に修正**（ステアリングボックス 2.0-2.5／タイロッド 2.5-3.0／リレーレバー 5.5-6.0／ピットマン 10.0-11.0／フロントスイングアーム 5.5-6.0）、**項目名の誤り1件を訂正**（「ミッションとボディの結合」→「クラッチハウジングとエンジンの結合ナット」＝独語 `Kupplungsgehäuse am Motor`）、**4項目を追加**（ステアリングホイール 4.0-5.0／クラウンホイール 3.2／ブレーキバックプレート 前2.0・後6.0）。
- 値が無い12項目は**消さない**。「何を締めるか」の索引になり、ねじサイズ表へ誘導できるため。指定が無いことを確認済みだと画面で伝える（§5.3）。
- **【2026-08-13 追記】画面まわりの手当てを2件（§6.1）と、ログイン済みオーナー向けのプリセットを実装（§6.2）。** ページの構成は `torque.html` ＋ `torque-data.json` のままだが、**`config.js` と supabase-js（CDN 2.94.0）を読むようになった**点だけ変わっている。

### 5.1 126 の投入（2026-08-13）

**原値＝126 Autobook 本 p112 の一覧表**（lb·ft 主・kgf·m 括弧併記）。**照合＝Bucheli 本 p91-92**（N·m）。

- **594cc（Autobook）と 652cc（Bucheli）はエンジン・ミッション・デフの全項目で完全一致した。** よってエンジン系10項目は `fits.engine = ["126A","126A1"]` とした。
- **同じ部位でも 500 と 126 で値が違う**ので、項目は分けて持つ（id は `-126` サフィックス）。例＝ヘッドナット 500 は 3.3・126 は 4.0／クラウンホイール 500 は 3.2・126 は 4.5 kgf·m。
- 名前が重複して取り違えるのを防ぐため、**カードに「対象エンジン」「対象車体」を必ず表示する**ようにした（`fitsLabel()`）。
- Bucheli にしか無い項目（パワーユニットマウント5点・レリーズフォーク・ホイールシリンダー・ブレーキホースユニオン・ハンドブレーキ等）は **652cc でしか裏が取れていない旨を `notes` に書いて**投入した。

#### 資料間で食い違った5項目（画面に両方出している）

| 項目 | 594cc Autobook | 652cc Bucheli | 判断 |
|---|---|---|---|
| **ピットマンアーム** | 10.0 kgf·m | 1.0 kgf·m | **Bucheli の誤植**（§3参照）。10.0 を採用 |
| **ホイールボルト** | 5.0 kgf·m | 7.0 kgf·m | 判別つかず。FD Ricambi の表は 50 N·m＝Autobook 側。**低い方を主にして両方表示** |
| 板バネ ゴムマウント | 3.0 | 5.0 | 年式差か誤植か不明。両方表示 |
| ロアアームシャフト→ボディ | 3.0 | 5.0 | 同上 |
| ロアアーム→キングピンハウジング | 6.0 | 7.0 | Bucheli は自己ロックナット指定。ナットの種類が違う可能性。両方表示 |

#### 手順で管理する箇所（数値を主役にしない）

- **フロントハブナット**＝0.7 kgf·m まで締めて約30度戻す。Bucheli も 0.5 kgf·m と極端に低く、**両資料とも「締めてから戻す」手順で一致**。
- **リアホイールベアリング**＝回転抵抗で管理。スペーサーは必ず新品・指定グリス Fiat MR 3。
- **デフのプリロード**＝シム調整。単体 13〜15 kgf·cm、クラウンホイール組付け後 20〜50 kgf·cm。**締付トルクと取り違えないこと。**
- **ミッションのメイン／レイシャフトナット**＝5.0 kgf·m の後、割ピン穴が合うまで**増し締め方向にのみ**追い込む。

### 5.2 未収録項目の拾い直し（2026-08-13）

**きっかけ＝純正独語版 p57 の `ANZUGSDREHMOMENTE DES MOTORS`（エンジン締付一覧）を、前のセッションで見落としていた。** 一覧表は p179 / p193 / p205-206 の3つだと思い込んでいたが、**エンジンは p57、ギアボックス／デフは p128 に別の表がある**。⚠️ **「一覧表は全部見た」と判断する前に、章ごとの表が他に無いか確認すること。**

拾った9項目：

| 項目 | 値 | 出典 |
|---|---|---|
| 主軸受ボディ 固定ボルト | 2.1 kgf·m | 独語 p57 のみ |
| カムシャフトギア 固定ボルト | 1.1 kgf·m | 独語 p57（英語版と不一致） |
| 冷却ファン→ジェネレーター ナット | 3.5 kgf·m | 独語 p57（英語版と不一致） |
| デフ ベアリングハウジング 取付ナット | 1.8 kgf·m / 13 lb·ft | 独語 p128 ＋ 英語 本 p71（一致） |
| スターターモーター 取付ナット | 18.1〜21.7 lb·ft | 英語 本 p74 のみ |
| パワーユニット 下側マウントナット | 18.1〜21.7 lb·ft | 英語 本 p74 のみ |
| リレーレバー サポート→ボディ | 14〜18 lb·ft | 英語 本 p101 のみ |
| ジェネレーター ベアリング外輪ストッパー | 0.80 lb·ft | 英語 本 p114 のみ |
| デフ プリロード（回転抵抗・手順） | 1.01 ± .07 lb·ft | 英語 本 p74 のみ |

- ⚠️ **英語版WSMにしか無い項目が4つ出たため、`kgm` が null で `lbft` だけの項目が初めて発生した。** 表示ロジックが `kgm` の有無で「値あり」を判定していたので、`itemHasValue()` に集約して lb·ft だけの項目も値ありとして扱うようにした（カードの大きい N·m・並び順・件数・「値がまだ無い」警告の4箇所）。`cross` の差分表示も `lbft` を受け付ける。
- ⚠️ **ファンとプーリーは英語版と独語版で扱いが違う。** 英語版 本 p114 は「The pulley and fan-to-generator armature shaft self-locking nuts … 14.5 Ib ft」＝**プーリーとファンを1行にまとめている**。純正独語版はプーリー 2.0（p223）・ファン 3.5（p57）と**別値**。データは独語版に従い項目を分け、プーリー側の `notes` にも「ファンは別項目」と書いた。**再調査しないこと。**
- ⚠️ **英語版WSMの本のページ番号は PDF のページ番号＋8。** 出典を `本 pXX` で書くときは PDF のインデックスをそのまま使わない（今回、下書きの5箇所が全部ずれていて是正した）。印字されたノンブルで確認する。
- ⚠️ **英語版 本 p74 には 18.1〜21.7 lb·ft が2箇所ある。** 片方はパワーユニット下側マウント（4本）、もう片方はスターターモーター取付（2本）。**同じ値なので1つと早合点しない。**

### 5.3 ⛔ 原典に指定が無いと確定した12項目（2026-08-13）＝再調査しないこと

**オイルドレン・ロッカーカバー・エキマニ／マフラー・キャブ・インマニの5項目は、手持ちの全資料に締付トルクの指定が無い。** §6 の残タスク3としてもう一度探した結果なので、**次のセッションで3度目を始めないこと。**

探した範囲（すべて全数走査。「表だけ見た」ではない）：

| 資料 | 調べ方 | 結果 |
|---|---|---|
| 英語版WSM（128p・テキスト層あり） | `(?:lb\|Ib)\s*[/. ]?\s*ft` で**全ページのトルク記述を1件残らず列挙**し、別途 `kg m` / `kgmm` も走査。`drain plug` / `rocker cover` / `exhaust manifold` / `carburett` / `manifold` の全出現も文脈付きで確認 | **5項目とも指定なし。** 組付け手順は詳しく書いてあるが（例 p24「Secure the carburetter using two copper washers and two self-locking nuts」）トルクは付いていない。**巻末に技術データ／トルク一覧の付録は無い**（本文は第12章ボディで終わる）。空白ページ4枚は本当に白紙 |
| 純正独語版（336p・テキスト層あり） | `mmkg` / `mkg` / `kgm` を**全ページ文脈付きで列挙**し、見出し `ANZUGS(DREH)MOMENTE` も全件確認。トルク表は **p57 / p128 / p146 / p161 / p179 / p193 / p205-206 の7つで確定**（p57 と p205-206 は画像で目視） | **5項目とも指定なし。** p57 のエンジン表は11行で、主軸受・フライホイール・コンロッド・ロッカー台座・ヘッド・カム・プーリー・ファン系のみ |
| 純正独語版 ジャルディニエラ補遺（39p・スキャン） | 全39ページのサムネイル一覧を作って表組みページを特定 → PDF p16（本 p18）`ANZUGSDREHMOMENTE FÜR MOTORTEILE` を目視 | **5項目とも指定なし。** 120.000 のエンジン表は **p57 と同一構成・同一部品番号**（1/11003・1/47500・1/25664・1/17016・1/21647・1/40549・1/09794・987109・1/42904・1/25756） |
| 126 Autobook 本 p112 | 一覧表を目視で再確認 | **5項目とも指定なし** |
| 126 Bucheli 本 p91-92 | 一覧表を目視で再確認 | **5項目とも指定なし** |

- ⚠️ **メーカーが指定していないだけで、締めなくていいという意味ではない。** ページ側は「値は未登録です」ではなく **「整備書に締め付けトルクの指定がありません」＋探した範囲**を出し、ねじサイズからの一般値へ誘導している。データ側は `no_spec: true` で持つ（`_readme` に定義あり）。**未調査（`no_spec` なし）と区別するための印**でもある。
- `no_spec` の項目は **「未確認」バッジを出さない**（調べていないのではないため）。
- 副産物＝`sources` の `oem.note` にトルク表のページを **7つ全部**書き直した（従来は p179 / p193 / p205-206 の3つしか書いておらず、§5.2 で見つけた p57・p128 が反映されていなかった）。

#### 残り7項目も同日に確定（＝値が無い12項目すべてが `no_spec` になった）

同じ手順で残りも当たり、**全12項目が「原典に指定なし」で確定**した。**未調査の項目はもう1つも無い。**

| 項目 | 判定の要点 |
|---|---|
| ブレーキパイプ フレアナット | 独語版のブレーキ表（p193）は**2行だけ**（バックプレート→ナックル 2000／→トレーリングアーム 6000）で配管系は無い。英語版はホースの外し方（本 p101）を書いているがトルクは付いていない |
| ホイールシリンダー（500） | 同上。**126 は Bucheli に 9.8 N·m（1.0 kgf·m）があり別項目で登録済み**＝500 も同じ値かは裏が取れていない |
| マスターシリンダー（500） | 同上。**126 は 14.7 N·m（1.5 kgf·m）**。独語版の油圧ブレーキ章（p186-188）はマスター／ホイールシリンダーの構造を詳しく書いているが、取付トルクは一切無い |
| シートベルト アンカー | **当時の純正装備ではない**。英語版 本 p102 に「シートベルトの取付部を床から外す」という手順が出てくるだけ。独語版の `Gurt` ヒットは OCR の誤り（板バネ表の `Belastung`） |
| シートレール／ドアヒンジ | ボディ章（英語 本 p112-120／独語 p284-297）にトルク記述が1件も無い。`Scharnier` の6ページはいずれも mmkg を含まない |
| エンジンマウント（500） | **「マウント全体」の指定が無いだけで、値そのものは既にある**＝英語版 本 p74 の下側4本 24.5〜29.4 N·m（18.1〜21.7 lb·ft）を `powerplant-mount-lower` として登録済み。独語版の `TRIEBWERKSLAGERUNG` 章（p89-90）にトルクの記載は無い（mmkg を含まないページ） |

- ⚠️ **「指定が無い」と「どこにも値が無い」は違う。** ホイールシリンダー・マスターシリンダー・エンジンマウントは**別項目に値がある**ので、`no_spec_note` で行き先を書いた（`_readme` に定義あり）。カードには「指定がありません」の下に補足として出る。
- ⚠️ **英語版WSMのブレーキ章（本 p104-111）とボディ章には、トルク値が1つも無い。** lb·ft の全数走査で該当ページが1件も出てこないので確認済み。**次に「ブレーキのトルクが載っているはず」と思っても開き直さないこと。**

## 6. 残タスク

1. ~~**適合をエンジン形式ベースに作り替える**~~ **＝2026-08-13 実装済み。** `models` を廃止し `fits`（`engine` / `chassis`）に置き換え、`engines`（6形式）と `chassis`（2種）の定義をデータに追加。UI に2つのプルダウンを付けた。**選んだエンジン形式の値をまだ持っていない場合は、黙って空にせず橙の警告を出す**（「黙って0件」だと「この形式は締めなくていい」と読まれかねないため）。エンジン系14項目・車体系28項目。
2. ~~**126.A（594cc）のトルク値を 126 Autobook から抽出する。**~~ **＝2026-08-13 完了。** 巻末 本 p112 の一覧表を目視で全項目確認した。
3. ~~126.A1（652cc）のデータ投入。~~ **＝2026-08-13 完了。** 懸念していた低い値は裏が取れた＝`Radlagermutter 5` は**手順項目で正しい**（Autobook も 0.7 kgf·m で締めて30度戻す）、`Lenkhebelbefestigungsmutter 10` は**Bucheli の誤植**（§3）。
4. ~~出典表記を公開ページに出してよいかのユーザー確認~~ **＝2026-08-13 ユーザー許可。書名をそのまま画面に出してよい。** 数値の出所を辿れることが早見帳の信頼性そのものなので、**出典表示を外さない**。
5. 公開の判断（noindex を外す・導線を付ける）。**どちらもユーザーの明示指示があるまでやらない。**
6. ~~**500 側で値が無い5項目**（オイルドレン・ロッカーカバー・エキマニ・キャブ・インマニ）を本文から拾う。~~ **＝2026-08-13 完了（結果は「原典に無い」）。** 全5資料を全数走査して指定が無いことを確定させ、`no_spec: true` として持った。**再調査しないこと**＝§5.3 に探索範囲を残した。
7. ~~`マニュアル/` を `.gitignore` に入れるかの判断~~ **＝2026-08-13 解決。`Documents\registro500-notes\manuals\` へ移動した**（§3 の警告を参照）。repo の外なので gitignore は不要。
8. ~~画面のさわり心地・見栄えの点検~~ **＝2026-08-13 実施。**指摘3点のうち2点を修正（§6.1）。**未対応で残したのは「対象エンジンの行が3行になる」1点**＝全形式に当てはまる12項目で `110.000 479cc ／ …／ 126.A1.000 652cc` を全部並べるため。畳むこと自体は可能だが、**適合はエンジン形式で判断するページなので、畳むなら全形式を開ける導線が必須**。優先度は低いと判断して見送った。

### 6.1 画面まわりの修正（2026-08-13）

| 直したもの | 何が起きていたか |
|---|---|
| **タブを折り返しに**（`.tq-tabs` の `overflow-x: auto` → `flex-wrap: wrap`） | 390px 幅で **7個中3個しか見えていなかった**（表示幅 347px に対し中身 680px）。`scrollbar-width: none` でスクロールバーも消していたため、**横に動かせること自体が分からない**状態だった。ちょうど「駆動系」で切れるので、そこで終わりに見えてしまう。折り返すと3段になるが操作が要らない |
| **「指定がありません」の説明を1行に** | 12項目すべて同じ4〜6行の説明を出しており、`no_spec` が連続する箇所（オイルドレン→ロッカーカバー→エキマニ）が文字の壁になっていた。**理由の説明は §5.3 相当の内容を「使う前に」へ一本化**し、カードには「整備書に指定がありません。下の『ねじサイズから目安を調べる』をご確認ください。」だけ残した。項目固有の `no_spec_note` はカードに残す |

⚠️ **ダークモードは既に手当て済み**＝`.tq-novalue` などは `var(--rg-*)` 経由で、ベタ塗り色は `@media (prefers-color-scheme: dark)` ブロックで全て上書きしてある。入力欄・セレクトにも `background` / `color` を明示済み。`viewport-fit=cover` は無いが、このページは `env(safe-area-inset-*)` も固定下部バーも使っていないので不具合にならない。

### 6.2 ログイン済みオーナーの諸元プリセット（2026-08-13）

ログインしているオーナーの**登録車両から、エンジン形式と車体の初期値を入れる**。`cars.owner_user_id` = `session.user.id` で自分の車を引く（`owner_email` では引かない＝SELECT 権限が無い前提の設計）。

- **閲覧にログインは要求しない。** 未ログイン・車両未登録・supabase-js 未読込のときは**帯ごと出さずに黙って終わる**（サイトの閲覧系ページの標準）。
- `engine_type_select`（`edit.html` のプルダウン14種）→ 当ページのエンジンID の対応は `engineIdFromCar()` に集約。括弧書き・空白・ドットを落として頭で判定する。**判定順は `126A1` → `126A0` → `110D` → `110F` → `120` → `110`**（`126A1` を `126A` より先に見ないと誤爆する）。
- ⛔ **`126A5.000` は対応づけない**（当ページのエンジン区分に無く、どれと同じか裏が取れていない）。`その他` / `不明` / 未記入も同様で、**車体だけ合わせて理由を画面に出す**。**推測で埋めない**＝§8 の不変条件そのもの。
- 帯には「すべて表示に戻す」／「この車に合わせる」を置き、**押した状態を保存しない**（次に開けば再びプリセットが効く）。複数台のオーナーはプルダウンで切り替えられ、**手放した車（`is_sold`）は後ろに回す**。
- ⚠️ `onAuthStateChange` のコールバック内で `await supabaseClient.xxx` を呼ぶと**認証ロックでデッドロックする**（サイト内の既知の落とし穴）。ここは `getSession().then(...)` の一本道にしてあるので、書き換えるときも同じ形を保つこと。

## 7. 作業環境のメモ

- **OCR 環境がない**。`tesseract` バイナリと `poppler`（pdftoppm）が未インストール。`pypdf` / `pytesseract` / `pdf2image` は入っている。
- スキャンPDFは **`pypdf` の `page.images` でページ画像を取り出せる**（ページ全体が1枚の JPEG）。取り出して画像として読めば内容は追える＝126 の表もこの方法で読んだ。全ページを機械処理したい場合のみ OCR が要る。
- **Windows 標準 OCR は管理者権限なしで使える**（`winocr` は winrt 3.x と非互換なので `winrt.windows.media.ocr` を直接叩く）。ただし**英語の言語パックは管理者権限が要るため入っておらず、日本語エンジンで欧文を読むと数値の列が崩れる**。
- ⚠️**OCR は「どのページにトルク記述があるか」の探索にだけ使う。数値そのものは、絞り込んだページを画像で目視して読む。** 126 の全項目もこの手順で確定させた。
- ローカルでの表示確認は `python -m http.server` を立ててから開く。**`file://` だと `fetch('torque-data.json')` が CORS で失敗して真っ白になる。**

## 8. 不変条件（勝手に変えない）

- **エンジン形式で適合を判断する**（§2）。車種だけで切らない。
- **N·m はデータに持たない**（表示時計算）。**ただし注記の文中には N·m を必ず併記する**（§4 の「単位の書き方」）。
- **DB化した原典の値を書き換えない**。資料間で違うときは片方を消さず、両方を出典付きで残して差を明示する。
- **原典に無い値を換算で作らない。** 片側しか記載が無ければ片側だけ入れて、もう片側は null にする。埋めると原典にある値のような顔をしてしまう。
- **値が無い項目を消さない**。
- **「締めっぱなしにしない箇所」は数値を大きく出さず手順を主役にする**（フロントハブナット・リアホイールベアリング・デフのプリロード）。数字だけ大きく出すと事故になる。
- **同じ部位でも 500 と 126 で値が違うものがある**（ヘッドナット・クラウンホイール等）。項目を1つにまとめず分けて持ち、**カードに対象（エンジン形式／車体）を必ず出す**。名前だけで見分けさせない。
- **導線を付けない・noindex を外さない**（ユーザーの明示指示があるまで）。
