了解しました。
**いまから、あなたが GitHub に置いている「冒頭メモ（プロジェクト概要）」を “最新版の状態に矛盾なく統合し”、今回の「認証・権限まわりの結論」も完全反映した、Markdown 1 本の最新版メモ** を作り直してお渡しします。

以下の点を必ず満たすように再構成しています：

### ✔ 条件

* 「GitHub から落としてきた冒頭メモ（プロジェクト概要・アーキテクチャ・作業ルール）」をベースに再構成
* **今回の認証まとめ（ActiveUser＋Firebase Auth ハイブリッド構成の最終結論）を統合**
* 現在の状況と矛盾する表現は削除

  * 例：iframe 残骸
  * Firebase 一本化が“現状仕様”のように読めてしまう部分
  * owner_edit_gate が Firebase ログイン前提と思われる表現
* 2025-11-23 時点の作業結果を反映
* 体系化した **README 冒頭メモにそのまま貼れる Markdown**

---

# 📌 **Registro500 Giappone – プロジェクト概要・アーキテクチャ・認証仕様（完全最新版｜2025-11-23）**

---

# # Registro500 Giappone

クラシック Fiat 500（110D / 110F / 126 系）オーナー向け
**完全無料・永続運用可能な登録 & 公開プラットフォーム**

---

# 🎯 プロジェクト目的

* 日本国内のクラシック Fiat 500（110D / 110F / 126 系）個体を登録・蓄積し、
  オーナー／オーナー候補が情報共有できるオンラインガレージを作る。
* 一般ユーザーは **一覧・詳細** を自由閲覧。
* 登録・編集は **オーナー本人のみ**（メール認証に基づく）。
* 完全無料の運用を前提とし、長期安定性を最優先とする。

---

# 🏗 アーキテクチャ（最適解｜2025-11-23 更新版）

※旧チャットの内容にあった **iframe の使用** は完全廃止。
現在は **Apps Script WebApp の直アクセスのみ**。

## ■ 使用技術（すべて無料）

| 用途        | 技術                                 |
| --------- | ---------------------------------- |
| データベース    | **Google Sheets**（cars シート）        |
| Webアプリ本体  | **Google Apps Script（GAS） WebApp** |
| 認証（スマホ導線） | **Firebase Auth（Googleログイン）**      |
| 画像保存      | Firebase Storage                   |
| サイト・表示動作  | 純HTML / CSS（Bootstrap 不使用）         |

---

# 🔀 URL ルーティング（GAS /exec）

| 機能          | URL                                                | HTML                     |
| ----------- | -------------------------------------------------- | ------------------------ |
| 一覧          | `/exec`                                            | index.html               |
| 詳細          | `/exec?mode=detail&doc=DOC_xxx`                    | detail.html              |
| 編集（新規/既存）   | `/exec?mode=edit&doc=DOC_xxx` or `/exec?mode=edit` | edit.html                |
| 編集ゲート（PC案内） | `/exec?mode=editGate&doc=DOC_xxx`                  | owner_edit_gate.html     |
| オーナーページ     | `/exec?mode=owner`                                 | owner.html               |
| 規約 / HowTo  | `/exec?mode=policy` / `/exec?mode=howto`           | policy.html / howto.html |

---

# 🔐 **認証・権限まわり（保存版｜2025-11-23）**

> 今後、新しいチャットを開始するときは
> **必ずこのセクションを AI に読ませること。**
> 過去に“認証方針の巻き戻り”が発生したため。

## 🎯 結論（現時点の最適で安定する構成）

### ✔ 1. **書き込み権限の最終判定は GAS 側（OwnerEmail 照合）**

* 保存処理 `saveCarFromForm()` 内で
  **OwnerEmail と実際のユーザーを照合**し、不一致なら書き込み拒否
* このレイヤが最も安定しており、プロジェクトの根幹

### ✔ 2. **Firebase Auth は owner.html（スマホ導線）専用に限定**

* スマホのログイン体験は Firebase Auth（実運用で正常）
* “マイ車両一覧” と “新規登録導線” はここで成立する

### ✔ 3. **PCの editGate（owner_edit_gate.html）では Firebase を使わない**

* PC では Firebase Auth の Googleログイン直後に
  **edit.html へ遷移すると Apps Script が 403 / 真っ白画面**になる
* 原因は「ログイン直後の一発目の GET だけ認証が不安定になる」Google 側のクセ
* よって PC 用の editGate は：

  * **案内ページのみ**
  * 「編集画面へ進む」ボタンで **素の GET** による遷移
* 認証・権限は保存時に GAS が最終判断するため問題なし

### ✔ 4. **最終的な理想は Firebase Auth 一本化だが、“現段階ではやらない”**

理由：

* GAS WebApp × Firebase Auth の併用は PC で 403 を誘発する
* 安定運用を最優先し、**ハイブリッド構成が実証的に最良**

---

## 🧪 この結論に至った検証経緯（重要）

1. Firebase Auth を detail → editGate → edit に組み込んで検証
2. スマホは成功
3. PC（複数Googleアカウント環境のみ）で
   **ログイン直後の /edit リクエストが必ず403になる**ことを確認
4. 自動遷移 → 失敗
5. 手動遷移 → 一部成功だが不安定
6. editGate から Firebase コードを抜いた瞬間に **完全安定**

＝2周分試行した結果、
**「現状はハイブリッド構成が最適」**という結論が固まった。

---

# 📄 各画面の役割（最新版）

## ■ index.html（一覧）

* 公開領域
* 並び替え機能のみ
* 右上に「オーナーページ」ボタン

## ■ detail.html（詳細）

* 公開領域
* PC：右上「オーナーの方は編集」 → editGate
* スマホ：owner ページ経由が前提（Firebase Auth でログイン）

## ■ owner_edit_gate.html（編集ゲート・PC用）

* Firebase ログイン**なし**
* 「この車両の編集について」＋案内文
* ボタン

  * 「編集画面へ進む」 → `/exec?mode=edit&doc=xxx`（同タブ）

## ■ owner.html（オーナーページ・スマホ用）

* Firebase Auth でログイン
* 自分の車両一覧 / 新規登録への導線

## ■ edit.html（編集フォーム）

* 新規 or 既存
* 認証は保存時の GAS 側で最終判断
* PC・スマホ共通

---

# 📝 作業ルール（最新版）

* 一歩ずつ進める（複雑箇所は質疑応答を優先）
* 勝手にコードを前に進めない
* 目の前のバグに小手先修正をしない（俯瞰で整合性を見る）
* コード修正は**可能な限り“全体差し替え”**（部分修正は誤りの温床）
* スクショがあれば**隅々まで確認してから回答**
* 新チャットを開いたら、この冒頭メモを必ず貼る

  * 認証方針が初期化されないようにするため

---

# 🚧 現状の課題（2025-11-23）

* owner_edit_gate.html の Firebase ログインを完全撤去（作業中）
* PC / スマホで認証導線が異なるため、説明文は丁寧な誘導が必要
* 画像アップロード（Firebase Storage UI）は未実装
* データモデル（60項目）は v2025-11 最新仕様に合わせて随時調整予定

---

# 🔚 まとめ

今回のメモは：

* 現状の仕様
* 認証の最終方針
* なぜそう決まったのか（経緯）
* 今後チャットが変わっても巻き戻らないための防止策

をすべて統合しています。

---

必要なら：

* **GitHub README 用に章の並びを変える**
* **編集履歴・更新日付のテンプレを付ける**
* **diagram（構成図）を追加する**

などにも対応できます。
