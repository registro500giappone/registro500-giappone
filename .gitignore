# Environment variables (API keys)
.env
py/.env

# 内部メモ・引継ぎメモ（設計情報が含まれるためgit管理外）
docs/1119memo
docs/1122memo
docs/1123sumahomemo

# セキュリティ是正・内部作業の指示書（未修正脆弱性の詳細を含むため公開リポジトリに出さない）
risk-instructions.md
refactor-instructions.md

# Firebase Admin SDK サービスアカウント（絶対にgit管理しない）
py/firebase-adminsdk.json
firebase-adminsdk.json

# GA4 Data API サービスアカウント（絶対にgit管理しない）
py/ga4_sa.json

# Firebase CLI設定（ローカルのみ）
firebase.json
storage.rules
firebase-cors.json
.firebaserc

# MCP設定
# .mcp.json は 2026-08-03 からgit管理対象（トークンを直書きせず ${SUPABASE_ACCESS_TOKEN} で
# 環境変数から展開する方式に変更したため。クラウドセッションにも引き継ぐ必要がある）。
# トークンを直書きしたローカル専用の設定を置きたい場合は下記の名前を使う（git管理外）。
.mcp.local.json

# Claude Code ローカル設定（権限設定等が含まれるためgit管理外）
# ただしスキル(commands)・エージェント定義(agents)は秘密情報を含まないためgit管理する
.claude/*
!.claude/commands/
!.claude/agents/

# Temporary files
nul
temp_result.json

# 成長レポート生成物（個人情報・ローカル確認用。HTMLは公開しない）
report.html
report-preview.png

# Python
__pycache__/
*.pyc

# Test files (本番環境から除外)
test.html
test_map.html
indexsub.html
config_test.js

# クローラー（GitHub Actions用にgit管理・機密ファイルはpy/.gitignoreで除外）
py/logs/
py/masking_output/
py/*.txt
!py/requirements.txt
py/*.log
py/*.csv
py/*.bat

# GAS (clasp) 設定・コード（ローカルのみ）
.clasp.json
.claspignore
main.js

# Playwright MCP キャッシュ
.playwright-mcp/

# Supabase CLI ローカルキャッシュ（supabase link 実行時に生成・秘密情報ではないがローカル状態のため管理外）
supabase/.temp/

# 開発用スクリーンショット・テスト確認画像（一時ファイル）
test_*.png
mobile_*.png
*_header.png
*_pc_*.png
masking_modal*.png
page-*.png
page-*.jpeg
*-page.png
*_page.png
*-guide.png
*_modal.png
detail-*.png
episode*.png
stories*.png
top-*.png
# 動画ポータル開発中の確認・モック画像
mock_*.png
mock_*.jpeg
preview_*.png
shelves_*.png
shelves_*.jpeg
videos-*.png
video-*.png
*_check.png
*_recheck.png
*_test.png
*_test2.png
yt_*.png
list_engine.png
howto_card.png
points_card.png
https_play_*.png

# 成長レポートのドライラン出力（REPORT_DRY_RUN=1 の生成物）
report_dryrun.html
