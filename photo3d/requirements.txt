# Photo3D dependencies — install into photo3d/.venv (NOT global Python)
# torch/torchvision must come from the CUDA index:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
torch
torchvision
fastapi
uvicorn
python-multipart
gradio_client
rembg
onnxruntime
pillow
numpy
scipy
trimesh
pygltflib
open3d
# 比較ボードのオフスクリーンレンダ用（trimesh windowed backend）。
# open3d の OffscreenRenderer は本機(Windows/EGL非対応)で使用不可と実測済み。
# pyglet 2.x は trimesh.viewer.windowed が非対応なので必ず 1.x を使う。
pyglet<2
