# あるべき姿の再構築検討

**作成日**: 2026-02-18
**ステータス**: 検討待ち
**優先度**: 中〜高（機能優先、将来的に重要）

---

## 📋 背景

### 現在の課題

2026-02-18のセッションで明らかになった問題：

1. **Firebase Auth と Supabase RLS の統合問題**
   - Supabaseの `auth.uid()` が使えない
   - RLSポリシーで `USING (true)` を使わざるを得ない
   - セキュリティ警告9件を許容している

2. **フロントエンド直接アクセスの問題**
   - edit.html/detail.html が Supabase に直接アクセス
   - SUPABASE_ANON_KEY を使用（公開されている）
   - RLSポリシーで保護しきれない

3. **アーキテクチャの複雑性**
   - Firebase Auth（認証）
   - Supabase（DB）
   - GAS（一部のAPI）
   - Firebase Storage（画像）
   - 3つのサービスが混在

---

## 🎯 検討すべき3つの方向性

### 方向性1: Firebase Auth + Supabase RLS 統合

#### 概要
Firebase Auth のトークンを Supabase に渡し、RLS ポリシーで `auth.uid()` を使えるようにする。

#### メリット
- ✅ 現在の Firebase Auth を継続使用
- ✅ Supabase RLS を正しく活用できる
- ✅ セキュリティ警告を完全に解消
- ✅ データベースレベルでセキュリティを担保

#### デメリット
- ❌ 実装が複雑（Firebase JWT → Supabase カスタムクレーム）
- ❌ フロントエンドの大幅な改修が必要
- ❌ メンテナンスコストが増加

#### 実装方法（概要）
1. Firebase Auth のトークンを取得
2. Supabase クライアントに JWT を渡す
3. Supabase の `auth.uid()` でユーザーを識別
4. RLS ポリシーを厳格化

#### 参考リンク
- https://supabase.com/docs/guides/auth/auth-helpers/auth-ui
- https://firebase.google.com/docs/auth/admin/verify-id-tokens

---

### 方向性2: Supabase Auth へ完全移行

#### 概要
Firebase Auth を廃止し、Supabase Auth に統一する。

#### メリット
- ✅ アーキテクチャがシンプルになる
- ✅ Supabase RLS をネイティブに活用
- ✅ Firebase への依存を減らせる
- ✅ セキュリティ警告を完全に解消
- ✅ コスト削減（Firebase の課金を削減）

#### デメリット
- ❌ 既存ユーザーの移行が必要
- ❌ Firebase Storage も移行する必要がある？
- ❌ フロントエンドの全面的な書き換えが必要
- ❌ Google ログインの再設定が必要

#### 実装手順（概要）
1. Supabase Auth の Google ログインを設定
2. 既存ユーザーのデータ移行
3. フロントエンド（全HTMLファイル）の書き換え
4. Firebase Storage の Supabase Storage への移行検討
5. RLS ポリシーの再設定

---

### 方向性3: GAS 経由への統一

#### 概要
フロントエンドから Supabase への直接アクセスをやめ、すべて GAS 経由にする。

#### メリット
- ✅ RLS を気にしなくて良い（GAS で service_role key を使用）
- ✅ フロントエンドに認証情報を公開しない
- ✅ Firebase Auth をそのまま継続使用
- ✅ GAS でビジネスロジックを集中管理
- ✅ セキュリティ警告は無視できる（RLS を無効化）

#### デメリット
- ❌ GAS の実行時間制限（6分/30分）
- ❌ GAS のレート制限
- ❌ すべてのフロントエンドを書き換え
- ❌ GAS のメンテナンスコストが増加
- ❌ レスポンス速度が遅くなる可能性

#### 実装手順（概要）
1. GAS に API エンドポイントを作成
   - `doPost('getCar')` → Supabase から車両取得
   - `doPost('updateCar')` → Supabase に車両更新
   - `doPost('deleteCar')` → Supabase から車両削除
2. フロントエンドを GAS 呼び出しに変更
3. GAS で Firebase Auth のトークンを検証
4. Supabase へは service_role key でアクセス

---

## 📊 比較表

| 項目 | 方向性1: 統合 | 方向性2: 移行 | 方向性3: GAS |
|------|--------------|--------------|--------------|
| **実装難易度** | 🔴 高 | 🔴 非常に高 | 🟡 中 |
| **コスト** | 現状維持 | 削減 | 現状維持 |
| **セキュリティ** | ✅ 改善 | ✅ 改善 | ✅ 改善 |
| **パフォーマンス** | ✅ 変わらず | ✅ 変わらず | ⚠️ 低下 |
| **メンテナンス性** | ⚠️ 複雑 | ✅ シンプル | ⚠️ 複雑 |
| **ユーザー影響** | なし | 🔴 再ログイン必要 | なし |
| **開発期間** | 2週間〜 | 1ヶ月〜 | 2週間〜 |

---

## 🎯 推奨アプローチ

### 段階的な移行（現実的）

#### Phase 1: 現状維持 + 部分改善（優先度 🟢 低）
- 現在の警告9件を許容
- BACKUP_POLICIES.md に記録済み
- **今すぐ実施する必要なし**

#### Phase 2: GAS 経由への部分移行（優先度 🟡 中）
- 重要な操作（車両削除、イベント削除など）のみ GAS 経由に
- フロントエンドの影響を最小限に抑える
- セキュリティリスクが高い部分から対処

#### Phase 3: 全面的な見直し（優先度 🔴 高、ただし時期は未定）
- ユーザー数・アクセス数が増えたタイミングで検討
- 方向性1（統合）または方向性2（移行）を選択
- 大規模な改修として計画

---

## 🔍 次回のミッションで検討すべきこと

### 1. 現状の整理
- [ ] 各HTMLファイルがどのように Supabase にアクセスしているか
- [ ] Firebase Auth の使用状況
- [ ] GAS の使用状況
- [ ] セキュリティリスクの洗い出し

### 2. 優先順位の決定
- [ ] ユーザー数・アクセス数の確認
- [ ] セキュリティリスクの評価
- [ ] 開発リソースの確認
- [ ] コスト対効果の分析

### 3. 実装計画の策定
- [ ] 選択した方向性の詳細設計
- [ ] 実装スケジュール
- [ ] テスト計画
- [ ] ロールバック計画

### 4. プロトタイプの作成
- [ ] 小規模な機能で実装を試す
- [ ] パフォーマンスの検証
- [ ] セキュリティの検証

---

## 📝 参考資料

### 技術ドキュメント
- [Supabase Auth Helpers](https://supabase.com/docs/guides/auth/auth-helpers)
- [Firebase Admin SDK](https://firebase.google.com/docs/admin/setup)
- [Google Apps Script Best Practices](https://developers.google.com/apps-script/guides/services/quotas)

### 関連ファイル
- `BACKUP_POLICIES.md`: セキュリティポリシーの現状
- `SESSION_2026-02-18.md`: 今回のセッション記録
- `security_fix_function.sql`: 実施したセキュリティ修正

---

## ⚠️ 重要な注意事項

### やってはいけないこと
1. ❌ 本番環境で直接テストしない
2. ❌ バックアップなしで大規模な変更をしない
3. ❌ ユーザーに影響する変更を予告なしに実施しない
4. ❌ 一度に複数の方向性を試さない

### やるべきこと
1. ✅ 小規模な機能でプロトタイプを作成
2. ✅ 段階的に移行する
3. ✅ 各段階で動作確認とロールバックテストを実施
4. ✅ ユーザーへの影響を最小限にする

---

**最終更新**: 2026-02-18
**次回検討**: 次のセッション
