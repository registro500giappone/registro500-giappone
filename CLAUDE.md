# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

今後はすべての説明、プラン、会話を日本語で行ってください。

---

## 🚫 公開導線を勝手に作らない（2026-08-05 ユーザー確定・全開発に適用）

**ユーザーの明示的な指示がない限り、新しいページへの導線（リンク・ナビゲーション）を既存ページに追加してはいけない。** これは提案するのはよいが、**実装して勝手にコミットするのは明確に禁止**。

### 禁止する対象

- `index.html` のメニュー（`.dropdown-content`）への追加 ← **特に厳禁**
- トップページ本文・ヘッダー・フッターからのリンク追加
- 各ページ間の相互リンクの新設（例: `detail.html` から新ページへ）
- `sitemap.xml` への新規URL追加、`_redirects` での新規パス公開

### 理由（この判断の背景）

**本番環境（Cloudflare Pages）にデプロイされていても、リンクが無ければ実質的に見られない。** この「デプロイ済みだが導線なし」の状態が、**本番データを使いながら安全に開発を続けるための作業場**になっている。勝手に導線を付けると、仕上がっていない画面がオーナーの目に触れる。

導線を付けるタイミング＝**機能が仕上がったとユーザーが判断したとき**。その指示は**必ずユーザーから出す**ので、こちらから判断しない。「一覧ページを作ったのだから入口も要るはず」といった**善意の補完もしない**。

### やってよいこと

- 「導線をどこに付けるか」の**提案・下書きの提示**（実装せず文章で示す）
- 新ページ自体の実装、既存ページ**内部**の改修
- そのページを直接URLで開いての動作確認

### 過去の違反例（同じ轍を踏まないため）

- 2026-08-05: 装備手帳の公開一覧を実装した際、指示なく `index.html` のメニューに「🧰 みんなの装備手帳」を追加してコミットした（`b1560e9`）。ユーザー指示により即時撤回（`14043c9`）。**「一覧ページを作る」というタスクに導線の追加は含まれない**。

---

## MCPサーバー構成

このプロジェクトでは以下のMCPサーバーを利用可能（`C:\Users\akayu\.claude.json` で管理）：

| MCPサーバー | 用途 | 認証 |
|------------|------|------|
| `supabase` | DB直接クエリ・テーブル確認 | SUPABASE_ACCESS_TOKEN |
| `github` | PR作成・コミット・Issue管理 | GITHUB_PERSONAL_ACCESS_TOKEN |
| `playwright` | ブラウザ自動操作・サイト確認 | 不要 |

### `.mcp.json` の方式変更（2026-08-03）

**`.mcp.json` はgit管理対象になった**（旧: gitignore・git add禁止）。**トークンを直書きせず環境変数から展開する方式**に変更したため、ファイル自体に秘密情報が無い。

- ファイル内の値は `"SUPABASE_ACCESS_TOKEN": "${SUPABASE_ACCESS_TOKEN}"` の形。**ファイルに秘密情報は入っていないのでコミットしてよい**。
- 動かすには環境変数 `SUPABASE_ACCESS_TOKEN` の設定が必要。**ローカルPCのWindowsユーザー環境変数にのみ設定する**。
- 環境変数が未設定でも設定ファイル自体は壊れない（`claude mcp list` に警告が出て、そのサーバーだけ認証に失敗する）。
- トークンを直書きしたローカル専用設定を置きたい場合は `.mcp.local.json`（gitignore済み）を使う。

> ⚠️ 秘密情報を`.mcp.json`に直書きして`git add`しないこと。トークンは必ず環境変数側に置く。

### ⛔ GAS は廃止で確定（2026-08-08/09 ユーザー確定・掘り起こし禁止）

- **GAS からSupabaseを叩く手段は存在しない**（UAでブラウザ判定され401。レガシーキーは無効化済で再有効化禁止）。**復旧策を調べ直さない**。
- **GAS に残る機能はすべて廃止**。移設もコード掃除もしない。**残タスクに数えない**（デッドコードなので実害なし）。X投稿も不要＝SNSは手作業。
- 例外：公開キー `sb_publishable_` はUA制限が無く、**公開データの読み取りだけならGASからでも可**。

経緯・根拠の全文はメモリ [[decisions-cancelled-2026-08]] と [[incident-supabase-key-exposure-2026-08]]。

### 🔒 Supabaseの個人アクセストークンをクラウド環境に置かない（2026-08-03 決定）

**claude.ai/code のクラウド環境設定に `SUPABASE_ACCESS_TOKEN` を登録してはいけない。** アカウント全体（プロジェクト削除を含む）に及ぶ権限で、本番DBには実在オーナーの個人情報が入っている。`--read-only` はMCP側の制御にすぎずSupabase側で強制されない。**クラウドセッションでは supabase MCP は接続失敗のままが正常**＝DBを変更する操作（migration適用など）は必ずローカルPCから行う。

クラウドから公開データの確認だけしたい場合は、`config.js` の公開キー（`sb_publishable_...`＝元から全訪問者に配られている値）で REST を叩けばよい。RLSが効くので匿名訪問者に見えるものしか返らない。詳細はメモリ [[incident-supabase-key-exposure-2026-08]]。

---

## コスト最適化方針

**軽い作業は Haiku のサブエージェント（`.claude/agents/`）へ委譲し、設計・実装・バグ修正はメインモデルで行う。**（モデル名を固定して書かない＝切り替えるたびに陳腐化するため）

| エージェント | 用途 |
|-------------|------|
| `batch-runner` | クローラー・AI翻訳の実行 |
| `db-checker` | DB件数確認・統計クエリ |
| `git-ops` | コミット・プッシュ |
| `site-checker` | Playwrightでの本番表示確認 |

DB件数確認・単純検索・ファイル調査も Haiku 委譲の対象。

---

## プロジェクト固有のコマンド

```bash
# クローラー実行（py/ディレクトリから）
python run_all.py              # 全ショップ並列実行 + AI翻訳
python ai_marathon_final_v9.py # AI翻訳のみ（category IS NULL を対象）
```

**デプロイ＝`git push origin main` で Cloudflare Pages が自動デプロイ**（他に手順はない）。

---

## 重要なファイル・パス

- `MEMORY.md` → プロジェクト全体のナレッジベース（自動読み込み）
- `.mcp.json` → MCPサーバー設定（**git管理対象**。トークンは直書きせず環境変数 `SUPABASE_ACCESS_TOKEN` から展開。詳細は上記「MCPサーバー構成」）
- `.mcp.json.example` → 旧方式のテンプレート（github/brevo等の設定例として残置）
- `.mcp.local.json` → トークン直書きのローカル専用設定を置く場合の名前（gitignore済み）
- `py/` → クローラースクリプト群（**GitHub Actions用にgit管理対象**、`*.py` / `*.md` はコミット必須。ログ・CSV・機密ファイルは `py/.gitignore` で除外）
- `126/index.html` → Fiat 126姉妹サイトのトップページ
- `.claude/agents/` → 専用サブエージェント定義（Haikuモデル）
- `youtube-portal/` → YouTube動画ポータル（クラシックFIAT 500/126動画キュレーション）の確定設計。`HANDOFF.md`（引き継ぎ書・全決定事項）＋`schema.sql`（適用済みスキーマ）
- `events-portal/HANDOFF.md` → イベントページ再構成の引き継ぎ書・全決定事項（不変条件は §7）
- `equipment-notebook/HANDOFF.md` → 装備手帳（みんなの車載手帳）の引き継ぎ書・全決定事項
- `event-slugs.json` → イベント個別ページのURL対応表。**一度決めたslugは変更しない**（変えるとリンクが切れる）

---

## 進行中テーマの扱い（先読みしない）

**個別テーマの設計・進捗をこのファイルに書かない。** 再開するときはユーザーから指示が出るので、**そのときに正本（各 `HANDOFF.md`）を読みに行く**。ここに写すと実態とズレたまま毎セッション読み込まれる。

- 各テーマの正本の在り処は上の「重要なファイル・パス」を見る。
- どのテーマも **HANDOFF の「不変条件」を勝手に変えない**（確定済みの設計判断）。

## 残ミッションを聞かれたら

その場で `MEMORY.md` の「## 進行中テーマ一覧 / 残ミッション」を読んで答える（`missions_inject.py` フックが自動で差し込むこともある）。**特定作業の途中経過だけで終わらせず、進行中の全テーマを横断**し、テーマ別の見出しで「現在の状態」と「次の残タスク」を各1〜数行にまとめる。記録は時点情報なので、期限・件数は現在時刻と実データで突き合わせる。

⭐**備忘テーマは件数だけ添える**（2026-08-28 ユーザー確定）。観察・相手待ちしか残っていないテーマは `MEMORY.md` から `MEMORY_watchlist.md` へ分離してある。俯瞰では**進行中テーマだけを答え、末尾に「別に備忘◯本あり・一覧が要れば出します」と件数を添える**＝⛔中身を混ぜない・⛔「次に何をやるか」の候補にもしない。**「備忘テーマの一覧を出して」と求められたときだけ `MEMORY_watchlist.md` を開いて答える。**
⚠️⚠️ただし**次の作業に入る前は、求められなくても `MEMORY_watchlist.md` 冒頭の「⛔着手前に引く判断」を必ず開く**＝**SNS掲載／メール送信の設計／車両名データ／PWA／配色・トップの見た目／Amazon**（うっかり再着手・逆行を防ぐ番人がそこに置いてある）。

**台帳を壊さないための保全ルール**（過去に俯瞰セクションを欠落させ、答えられなくなった事故がある）：

- **俯瞰セクションが空・欠落していたら、各 `project_*.md` から再構築してから答える**。「無いので答えられない」で済ませない。
- メモリディレクトリは**ローカルgit管理下**。台帳を編集したら `git -C "<memoryパス>" add MEMORY.md && git -C "<memoryパス>" commit` でスナップショットを残す（更新したファイルが他にもあれば同様に明示指定する）。**`git add -A` / `--all` は `.claude/settings.json` の deny で禁止**（`.mcp.json` 等の巻き込み防止）。使うと拒否されるので、必ずファイル名を明示すること。
- 台帳系は**全文上書き（Write）を避け、部分Editか `【YYYY-MM-DD 更新】` 形式の追記**で更新する。過去の経緯を物理的に消さない。

---

## フリーズ予防の運用規範（全モデル共通・必ず守る）

長エージェントループ×大コンテキストで「ツール結果受信後の継続ドロップ」フリーズが多発した実績がある（詳細はメモリ project-settings-hardening-2026-06）。設定側の手当ては `autoCompactWindow`（2026-08-11 適用）のみで、**残りは運用でしか防げない**。どのモデルで動いていても以下を守ること：

1. **大ファイルを丸読みしない**。Grep（pattern/-n）で行番号を特定 → Read は offset/limit で該当±数十行のみ → 最小Edit。1000行超のファイルの全文Readは禁止。
2. **1ターンでツールを長く連打しない**。sql→grep→read→…の長連鎖は短く区切り、区切りごとに簡潔なテキストを挟む。
3. **毎往復でコンテキスト肥大を自己点検**する。長スレッド・大きな貼付・ログ大量取り込みの兆候があれば、応答を簡潔化し、区切りのよい所で `/clear` を自分から提案する。
4. **長い作業（クローラー監視・大規模リファクタ等）を始める前に `/clear` を提案**する。
5. 異常の兆候（空のthinking・語句の反復・身に覚えのない言語の混入）を感じたら即停止してユーザーに報告する。捏造出力バグ（GitHub Issue #67606等）の既知トリガーは長コンテキスト＋ノイズ。

---

## コンテキスト圧縮ヒント（/compact 実行時に優先保持）

- **最優先で残す**：進行中テーマと各テーマの残タスク（`MEMORY.md` の「進行中テーマ一覧 / 残ミッション」）／作業中のタスクで確定した判断。
- **捨ててよい**：コマンドやスクリプトの生ログ／プロセス一覧などの調査ダンプ／完了済みタスクの詳細（「どのテーマが完了したか」の一行だけ残す）。