-- ============================================================
-- migration名: equipment_notebook_items_reason_2026_08_05
-- 状態: ✅適用済み（2026-08-05 起案 ／ 2026-08-06 本番データで適用を実地確認）
-- 根拠: id25 クラッチワイヤーの reason が「切れると発進もシフトもできず自走不能」になっている
--
-- id25 クラッチワイヤーの reason が「同上」になっていた。
-- 入力画面ではアクセルワイヤー(id24)の直下に並ぶため通じるが、
-- 結果画面のリコメンドは項目を単独で切り出して表示するため意味が通らない
-- （2026-08-05 実機確認で発覚。画面にもDL画像にも「クラッチワイヤー ／ 同上」と出ていた）。
--
-- 教訓: reason は入力画面の並び順に依存しない、単独で読める文にすること。
-- id は不変・reason は可変なので既存の回答には影響しない。
--
-- ロールバック:
--   update public.equipment_items set reason = '同上' where id = 25;
-- ============================================================
update public.equipment_items
   set reason = '切れると発進もシフトもできず自走不能'
 where id = 25;

-- 確認（結果をそのまま表示。判定は不要）
--   select id, name, reason from public.equipment_items where id in (24, 25) order by id;
