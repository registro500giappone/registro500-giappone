-- ============================================================
-- 車載手帳: 購入先リンクが付いていないパーツ9件の決着（第3弾）
-- migration名: equipment_notebook_purchase_links_parts_rest_2026_08_05
-- 状態: ✅適用済み（2026-08-05 起案 ／ 2026-08-06 本番データで適用を実地確認）
-- 根拠: パーツ系項目に nuova500.com/standard-speed のリンクが入っている
--
-- 背景:
--   HANDOFF「次にやること」5番＝「リンクが付かないパーツ9件の判断（実害なし）」の決着案。
--   対象9件（is_active=true・購入先リンクなし）:
--     20 ローター / 26 ガスケット類 / 27 ボルト・ナット・ワッシャー /
--     28 スペアタイヤ / 66 キャブレター本体 / 67 デスビ本体 /
--     69 タイヤチューブ / 72 シフトリンケージのカップリング / 73 ミッションサポートラバー
--   ※ 非常時対応の44 ロードサービス連絡先・45 保険証券・46 整備マニュアル・49 飲料水も
--     リンクなしだが、これらは第1弾で意図的に除外した「物品でない項目」なので対象外。
--   ※ 34/35（ケミカル）と50〜54（洗車・その他）もリンクなしだが is_active=false（無効）。
--
-- 調査（2026-08-05・スタンダードスピードの実ページをHTTP取得して商品名を確認）:
--   ・parts_electric.html   「ディストリビューターローター＜新品＞」「ディストリビューターASSY
--                            ＜新品リプロ／リビルト品＞」を確認 → 20・67 が該当
--   ・parts_gasket.html     「FIAT純正ガスケットセット」ほかガスケット26件を確認 → 26 が該当
--   ・parts_3carb.html      「キャブレター本体」の記載を確認 → 66 が該当
--   ・parts_transmission.html「ミッションサポートダンパー（ペア）」「シフトリンケージダンパーゴム」
--                            「シフトリンケージシャフトガイドホルダーゴム」を確認 → 72・73 が該当
--   4ページとも HTTP 200 を確認済み。
--
-- リンクを付けない3件（＝この migration では触らない。判断の記録）:
--   27 ボルト・ナット・ワッシャー
--     → 車種専用品ではない汎用品。スタンダードスピードに該当カテゴリが無く（parts_other.html に
--        ボルト・ナットの記載なし）、Amazon検索では「どの径・どのピッチか」が定まらず精度が出ない。
--   28 スペアタイヤ（空気圧点検済み）
--     → 「買う物」ではなく「自車のホイールを積んでいるか」を問う項目。購入先リンクは意味を成さない。
--   69 タイヤチューブ
--     → スタンダードスピードはホイール主体で、チューブ単体の扱いを確認できなかった
--        （parts_ABwheel.html の「チューブ」はチューブレス運用の説明文）。Amazonなら買えるが
--        サイズ依存で誤購入のリスクがあり、リンクで背中を押すべきでないと判断。
--   → 3件とも「リンクなし＝実害なし」のまま確定させる。再検討が要るのは、スタンダードスピードに
--      該当商品が増えたときだけ。
--
-- ⚠️ 既存リンクを壊さないこと:
--   purchase_links は配列。上書きでなく jsonb 連結（||）で追記する。
--   対象6件はいずれも現在 null のため coalesce で空配列を補う（null || x = null の回避）。
--
-- ⚠️ これはアフィリエイトではない（第2弾と同じ）。
--   type='shop' なので描画側は rel="nofollow"（sponsored なし）・PRバッジなしで出力する。
-- ============================================================

-- ------------------------------------------------------------
-- 点火・電装系（parts_electric.html）
--   20 ローター（ディストリビューターローター）
--   67 デスビ本体（ディストリビューターASSY・新品リプロ／リビルト）
-- ------------------------------------------------------------
update public.equipment_items
  set purchase_links = coalesce(purchase_links, '[]'::jsonb) ||
    '[{"type":"shop","label":"スタンダードスピード","url":"https://www.nuova500.com/standard-speed/parts_electric.html"}]'::jsonb
  where id in (20, 67);

-- ------------------------------------------------------------
-- ガスケット・シール類（parts_gasket.html）
--   26 ガスケット類
-- ------------------------------------------------------------
update public.equipment_items
  set purchase_links = coalesce(purchase_links, '[]'::jsonb) ||
    '[{"type":"shop","label":"スタンダードスピード","url":"https://www.nuova500.com/standard-speed/parts_gasket.html"}]'::jsonb
  where id = 26;

-- ------------------------------------------------------------
-- 燃料系・キャブレター（parts_3carb.html）
--   66 キャブレター本体（丸ごと交換用）
--   ※ 71 キャブレターの補修部品は第2弾で parts_3OHkit.html（O/Hキット）に付け済み。
--      本体とO/Hキットで送り先が違うのは意図どおり。
-- ------------------------------------------------------------
update public.equipment_items
  set purchase_links = coalesce(purchase_links, '[]'::jsonb) ||
    '[{"type":"shop","label":"スタンダードスピード","url":"https://www.nuova500.com/standard-speed/parts_3carb.html"}]'::jsonb
  where id = 66;

-- ------------------------------------------------------------
-- ミッション（parts_transmission.html）
--   72 シフトリンケージのカップリング（＝シフトリンケージダンパーゴム等）
--   73 ミッションサポートラバー（＝ミッションサポートダンパー〔ペア〕）
-- ------------------------------------------------------------
update public.equipment_items
  set purchase_links = coalesce(purchase_links, '[]'::jsonb) ||
    '[{"type":"shop","label":"スタンダードスピード","url":"https://www.nuova500.com/standard-speed/parts_transmission.html"}]'::jsonb
  where id in (72, 73);

-- ============================================================
-- 適用後の確認（結果をそのまま表示するだけ。判定は人が行う）
-- ============================================================
-- 期待: 6行が返り、いずれも shop リンクを1本だけ持つ
-- select id, name, purchase_links
--   from public.equipment_items
--  where id in (20, 26, 66, 67, 72, 73)
--  order by id;
--
-- 期待: リンクなしで残る is_active=true の項目が 27・28・69・44・45・46・49 の7件だけ
-- select id, category, name
--   from public.equipment_items
--  where is_active = true and purchase_links is null
--  order by id;
