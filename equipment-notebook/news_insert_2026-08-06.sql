-- ============================================================
-- 車載手帳: 公開のお知らせを news テーブルに投入
-- 状態: ★未適用★（2026-08-06 起案・適用はローカルPCから）
--
-- 文案の正本は announcement_2026-08-06.md（ユーザー承認済み）。
-- ここではそれを news.content の形式に合わせて整形しただけで、文言は変えていない。
--
-- ⚠️ 整形にあたって落としたもの（news.html の実装上の制約）:
--   news.html の linkify() は本文を esc() でHTMLエスケープしてから描画する。
--   つまり **強調** や <b> は効かず、アスタリスクがそのまま画面に出る。
--   そのため文案の ** を外し、既存記事と同じ「■ 見出し」のプレーンテキストにした。
--   改行は .news-body { white-space: pre-wrap } でそのまま反映される。
--   URLだけは linkify() が自動でリンクになるので、生のまま書いてよい。
--
-- ============================================================
-- ⚠️⚠️ メール配信について（実行前に必ず読むこと）⚠️⚠️
-- ============================================================
-- main.gs の getUnsentNewsAll_() が
--     news?sent_at=is.null&created_at=gte.<14日前>&limit=5
-- を拾って**登録オーナー全員にメールを送る**。トリガーで自動実行される。
--
-- 重要な落とし穴: この抽出条件に date は入っていない。
--   → date を NULL にして「サイト上は下書き」にしても、メールは送られてしまう。
--     サイトには出ていないページの告知メールだけが届く、という事故になる。
--
-- したがって選ぶのは次のどちらか。下の A / B を切り替えて実行する。
--   A. 通常どおり告知する（サイト掲載＋メール配信）… 既定。過去のお知らせと同じ流れ
--   B. サイトのお知らせ欄にだけ出す（メールは送らない）… sent_at を埋めて対象外にする
--
-- Xへの投稿は x_posted で管理されているが、こちらは自動投稿の仕組みが
-- 見当たらないため false のままでよい（投稿したら手で true にする運用と思われる）。
-- ============================================================

insert into public.news (date, title, content, target_car_type, x_posted, email_sent, sent_at)
values (
  '2026-08-06',
  '「みんなの車載手帳」を公開しました（車に何を積んでいるかを記録・共有）',
  $NEWS$いつもありがとうございます。メニューに新しいページ「🧰 みんなの車載手帳」を追加しました。

空冷FIAT 500・126のオーナーが、出先のトラブルに備えて車に何を積んでいるかを記録・公開できる機能です。

旧車で出かけるとき、何を持って行けばいいのか。答えは人によって違いますが、すでに走り続けている方々が実際に積んでいるものには、それぞれ理由があります。この手帳は、その中身をお互いに見られるようにするものです。

■ 見る

https://www.registro500.com/equipment

公開されている手帳と、項目ごとの車載率（何%の方が積んでいるか）をご覧いただけます。ログイン不要です。

■ 書く — ぜひ、あなたの手帳も見せてください

ガレージに愛車を登録されている方は、すぐに書けます。項目にチェックを入れていくだけで、途中で保存しても大丈夫です。思い出したときに書き足してください。

私たちが一番読みたいのは、皆さんが「当たり前」だと思って積んでいるものです。ご本人にとっては何でもない一品が、他の方には思いつかなかった備えだったりします。逆に「自分はこれだけしか積んでいないけれど」という手帳も、同じくらい参考になります。多い少ないは関係なく、実際のところが知りたいのです。

迷ったら、まず開いてみてください。項目にはすべて「なぜ積むのか」を添えました。読むだけでも何か見つかるかもしれません。

どうぞ、どんどん書いてください。楽しみにお待ちしています。

■ 公開・非公開について

いつでも切り替えられます。非公開のままでも、全体の車載率には人数として反映されます。

現在、6名の方が手帳を公開してくださっています。ありがとうございます。$NEWS$,
  'both',
  false,   -- x_posted: Xへの投稿はまだ
  false,   -- email_sent
  null     -- ← A. メール配信する（既定）
  --                    B. メールを送らない場合は、上の2行を次に差し替える:
  --                         true,          -- email_sent
  --                         now()          -- sent_at を埋めると配信対象から外れる
);

-- ============================================================
-- 投入後の確認（結果をそのまま表示するだけ。判定は人が行う）
-- ============================================================
-- select id, date, title, target_car_type, x_posted, email_sent, sent_at
--   from public.news order by created_at desc limit 3;
--
-- サイト表示の確認: https://www.registro500.com/news
--   date が今日以前・NULLでない行だけが出る。投稿から7日間は NEW バッジが付く。
--
-- ------------------------------------------------------------
-- 取り消したいとき
-- ------------------------------------------------------------
--   サイトから隠すだけ:   update public.news set date = null where id = <id>;
--   完全に消す:           delete from public.news where id = <id>;
--   ※ ただしメールは送信済みなら取り消せない。先に sent_at を確認すること。
--
-- ------------------------------------------------------------
-- 本文中の「6名」について
-- ------------------------------------------------------------
-- 2026-08-06 時点の実測値（equipment_public_list() の total_public = 6）。
-- 投入までに日が空いたら、投入直前に測り直して本文を合わせること:
--   U="https://ttlttclfovuzafvghvaq.supabase.co/rest/v1"
--   K="sb_publishable_YMQjADUCrD6BytxvcMm-lQ_7n8LMEAt"
--   curl -s -X POST "$U/rpc/equipment_public_list" -H "apikey: $K" \
--        -H "Content-Type: application/json" -d '{"p_limit":24}' | head -c 200
