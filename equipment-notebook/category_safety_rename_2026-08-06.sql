-- ============================================================
-- 車載手帳: カテゴリ「安全装備」→「安全・救援」に改称
-- migration名: equipment_notebook_category_safety_rename_2026_08_06
-- 状態: ★未適用★（2026-08-06 起案・適用はローカルPCから）
--
-- 背景（2026-08-06 ユーザー指摘）:
--   4点のうち半分は「装備（モノ）」ではなかった。
--     39 三角表示板          → モノ・法令由来
--     38 発煙筒・非常信号灯   → モノ・法令由来
--     44 ロードサービス連絡先 → **情報**
--     45 保険証券            → **書類**
--   4点の共通項は「止まった直後に、身の安全を確保し、助けを呼ぶための備え」であり、
--   設計思想の記述も「**法律とレスキュー**の事実として言うから成立する」だった
--   （HANDOFF「型の区分け再議論」§425）。性質は 安全（法令）＋救援 の2本立てで、
--   それを「装備」の一語に押し込んでいたのが実態。呼称を実態に合わせる。
--
-- ⚠️ 項目IDもsort_orderも変えない。変えるのは category の文字列だけ。
--    2026-08-05 の category_safety_split で分割したばかりのカテゴリの、呼称のみの変更。
--
-- ⚠️ デプロイ順序の制約（前回と同じ）:
--    **フロント（CATEGORIES配列）を先にデプロイしてからこのmigrationを適用する**。
--    逆順だと、配列に無いカテゴリ '安全・救援' の4項目が detail.html では
--    「洗車・その他」に落ち、equipment-edit.html では表示されなくなる。
--    フロントが先なら該当0件でセクションが出ないだけで無害。
-- ============================================================

-- ------------------------------------------------------------
-- 適用前の確認（結果をそのまま表示するだけ。判定は人が行う）
-- ------------------------------------------------------------
-- 期待: 安全装備4件・安全・救援0件
-- select category, count(*) from public.equipment_items
--  where category in ('安全装備', '安全・救援') group by category;

update public.equipment_items
   set category = '安全・救援'
 where category = '安全装備';

-- ============================================================
-- 適用後の確認（結果をそのまま表示するだけ。判定は人が行う）
-- ============================================================
-- 期待: 安全・救援が4件（39→38→44→45 の順）、安全装備は0件
-- select category, sort_order, id, name from public.equipment_items
--  where category in ('安全装備', '安全・救援') order by sort_order;
--
-- 期待: 有効な項目のカテゴリが、フロントの CATEGORIES 配列と一致する
--       工具 / パーツ / ケミカル / 安全・救援 / 非常時対応 / 洗車・その他
-- select distinct category from public.equipment_items where is_active = true order by 1;

-- ------------------------------------------------------------
-- クラウドセッションからの独立検算（公開キー・読み取りのみ）
-- ------------------------------------------------------------
--   U="https://ttlttclfovuzafvghvaq.supabase.co/rest/v1"
--   K="sb_publishable_YMQjADUCrD6BytxvcMm-lQ_7n8LMEAt"
--   curl -s "$U/equipment_items?select=id,category,sort_order,name&id=in.(38,39,44,45)&order=sort_order" -H "apikey: $K"
--
-- ⚠️ equipment_category_status には「安全装備」で保存済みの行が残りうる（カテゴリ名がキー）。
--    このフラグは集計にも表示にも使われていない自分用の目印なので実害はなく、
--    次に保存した時点で新しいカテゴリ名の行が書かれる。あえて移行しない。
