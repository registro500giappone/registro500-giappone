-- ============================================================
-- 車載手帳: デスビ本体(id67)のreason文を上位互換の実態に合わせる
-- migration名: equipment_notebook_item_reason_supersedes_2026_08_06
-- 状態: ✅適用済み（2026-08-06 起案 ／ 2026-08-06 本番データで適用を実地確認）
-- 根拠: id67 の reason が「切り分けをせず載せ替えて復帰する構え。…」になっている
--
-- 背景:
--   id67 のreason文は「切り分けをせず載せ替えて復帰する構え。
--   **キャップ・ローター単品とは別の備え方**」だった。後半が
--   「デスビ本体とキャップ・ローターは並列する2つの選択肢」と読める。
--
--   ところが 2026-08-06 のユーザー判断で、判定ロジックは
--   「本体があればキャップ・ローター・ポイント・コンデンサーは不要」＝**上位互換**
--   に変更した（SUPERSEDES）。**説明文と判定が食い違っている状態**になっている。
--
--   実害: デスビ本体を積んでいるオーナーが結果画面を見たとき、判定は「充足」なのに
--   項目の説明は「これとは別にキャップ・ローターも要る」と読める。
--   2026-08-06 の文言総点検で見つけた「実装が伴っていない文言」4件と同じ型。
--
-- ⚠️ 前半（この装備の意義）はそのまま残し、後半だけを実態に合わせる。
-- ⚠️ reason は結果画面のリコメンドや車両ページに出る文言なので、
--    フロントのデプロイ順序に依存しない（DBの文字列がそのまま表示される）。
-- ============================================================

-- ------------------------------------------------------------
-- 適用前の確認（結果をそのまま表示するだけ。判定は人が行う）
-- ------------------------------------------------------------
-- 期待: 現行の reason に「別の備え方」が含まれている
-- select id, name, reason from public.equipment_items where id = 67;

update public.equipment_items
   set reason = '切り分けをせず載せ替えて復帰する構え。これがあれば、キャップ・ローター・ポイント・コンデンサーの予備は無くても復旧できる'
 where id = 67;

-- ============================================================
-- 適用後の確認（結果をそのまま表示するだけ。判定は人が行う）
-- ============================================================
-- 期待: 「別の備え方」が消え、「予備は無くても復旧できる」になっている
-- select id, name, reason from public.equipment_items where id = 67;
--
-- 参考: 上位互換の相方である 66 キャブレター本体のreasonは
--   「不調時に載せ替えて最短で復帰する構え。かさばるが確実」で、
--   もともと補修部品(71)との関係に言及していないため変更不要。
--
-- ------------------------------------------------------------
-- クラウドセッションからの独立検算（公開キー・読み取りのみ）
-- ------------------------------------------------------------
--   U="https://ttlttclfovuzafvghvaq.supabase.co/rest/v1"
--   K="sb_publishable_YMQjADUCrD6BytxvcMm-lQ_7n8LMEAt"
--   curl -s "$U/equipment_items?select=id,name,reason&id=eq.67" -H "apikey: $K"
