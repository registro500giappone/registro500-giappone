# 装備手帳機能 — 実装ハンドオフ（正本）

対象サイト: registro500.com
起票: 2026-07-22

**設計思想の正本＝`design-original.md`**（元ハンドオフ全文）。本ファイルは確定事項・実装ログ・次タスクを管理する。確定済みの設計判断を勝手に変えない。

---

## 確定事項（2026-07-22 ユーザー確認済）

- **スコープの段取り＝土台優先**。最重要制約「項目マスターのIDを確定させてから回答を取る／後からIDが変わると回答が壊れる」を守るため、初手はDBスキーマ＋シードのID確定＋Supabase適用に絞った。UI・リコメンド・結果カードは次セッション以降。
- **7テーブルを最初に全部作成**（構造を先に固定。履歴書込などのロジックはフェーズ2でも器は先に用意）。
- **MVP初期はログイン必須**（Supabaseメール OTP）。匿名回答の器（`equipment_records.user_id`/`vehicle_id` nullable・`is_anonymous`）は最初から保持し、anonロールのINSERT開放はパイロット後のフェーズ2で追加。
- **命名は `equipment_` 接頭辞**。既存 `survey-results.html`（＝パーツ調達アンケート・別物）との混同を回避。

## 既存サイトのパターン（UI実装時に踏襲）

- DB＝Supabase（`@supabase/supabase-js@2.94.0`、`config.js` の `SUPABASE_URL`/`SUPABASE_ANON_KEY`）。認証＝Supabaseメール OTP（`edit.html` の `signInWithOtp` → `verifyOtp({type:'email'})`）。
- 書込の手本＝`garage_notes`（`garage-notes.html` L548-557、insert時に `user_id: currentUser.id`）。
- 車両紐付け＝`cars.owner_user_id`（=auth `user.id`）。`cars` から `.eq('owner_user_id', user.id)` で所有車両・handle_name を引く。
- 入力UIの手本＝`edit.html` の `.opt-section`（details/summary 折りたたみ）＋`.radio-group`（L36-52, L304-438）→「常時/随時」トグルに流用。
- 集計表示の手本＝`survey-results.html` の横棒グラフ `.bar-track`/`.bar-fill`。
- ラッパの手本＝`garage-notes.html` の `.page`+`.back-link`+`.brand-header`（系統B）。
- 結果カード画像化＝html2canvas未導入。`edit.html` の canvas `getContext('2d')`→`drawImage`→`toDataURL/toBlob`（L1197-1308）を自前実装 or html2canvas新規導入。
- GA4は `video.html` L322 の `track()` ラッパ方式（gtag未読込でもクラッシュしない）を踏襲。
- **注意**: `onAuthStateChange` 内の Supabase 呼び出しは `setTimeout(...,0)` / `.then()` でデッドロック回避（`edit.html` L630-632 の教訓）。update時に所有権列（user_id）を再送しない（所有権付け替え事故の回避）。

## スキーマ対応表（ハンドオフ案 → 採用テーブル）

| ハンドオフ案 | 採用テーブル | 役割 |
|---|---|---|
| survey_items | `equipment_items` | 項目マスター（id不変） |
| survey_responses | `equipment_records` | 手帳（回答セッション・親） |
| survey_answers | `equipment_entries` | 個別回答（最新状態・行なし=未搭載） |
| survey_answer_history | `equipment_entry_history` | 変更履歴（器のみ・書込はフェーズ2） |
| survey_category_status | `equipment_category_status` | カテゴリ完了フラグ |
| survey_custom_items | `equipment_custom_items` | 自由追加項目（名品昇格の種） |
| survey_experiences | `equipment_experiences` | 経験談（記事素材） |

集計は関数 `equipment_item_rates()`（SECURITY DEFINER・全体母数の積載率・個人特定なし・anon/authenticated 実行可）。

## 実装ログ

### 2026-07-22 タスク1（DB土台）＝完了
- migration `equipment_notebook_schema` 適用：7テーブル＋インデックス＋RLS＋集計関数 `equipment_item_rates()`。控え＝`schema.sql`。
- migration `equipment_notebook_seed_items` 適用：項目マスター**54件**投入（id 1〜54 明示・不変）。控え＝`seed_items.sql`。
  - 内訳: 工具13 / パーツ15 / ケミカル9 / 非常時対応12 / 洗車・その他5。※元ハンドオフ本文の「45項目」は概数、実データは54で確定。
  - `recommend_priority`・`affiliate_url` は全件 null で投入（後述の未確定事項で確定させる）。
- 検証: 件数・カテゴリ内訳・code全ユニーク・reason欠損0・集計関数動作（母数0でも除算エラーなし）を確認。
- advisor(security): `equipment_item_rates()` の SECURITY DEFINER 警告は「全体集計を公開する」意図通りで受容（個人特定情報を返さない。既存 `link_owner_car()` と同型）。
- **未コミット**（repo `equipment-notebook/` は作成済・push はユーザー指示待ち）。

### 2026-07-23 タスク2（recommend_priority 確定）＝完了
- migration `equipment_notebook_recommend_priority_v3` 適用（v1/v2は同日中に上書き）。控え＝`recommend_priority.sql`。上位**23件**付与。
- v3で追加した第4の軸＝**理想と現実のバランス**。「あった方がよい」が実際には積まれていない物を並べると、推奨リスト全体の現実味が落ちる。
  - 反射ベストを除外（日本では法定義務でなく搭載者がごく少数）／ロードサービス連絡先を除外（装備ではなく事前確認する情報。項目自体は残す）。
  - ヘッドライト・軍手ウエスを工具ブロック上位へ（暗所と手の汚れは出先整備の前提条件）。
  - 項目マスターの表記修正2件（**id不変・name/reasonは可変**）: id38「発煙筒・非常信号灯（LED可）」＝LED式は期限切れがない現実解／id23「燃料ホース（応急の短尺）＋ホースバンド」＝全長交換の装備という誤解を避け、50cm〜1mで裂けた区間を差し替える用途と明示。
- **序列の設計軸（ユーザー確定・以後この3軸で見直す）**:
  1. **非常時対応が最上位**。積むだけで手間ゼロ・三角表示板は法定義務・夜間の路肩は二次事故が命に直結。
  2. **次に工具**。パーツを積んでいても工具が無ければ現地で交換できない＝パーツ先行の序列は役に立たない。
  3. **パーツ／ケミカルは「現地修理の簡便性」順**。路肩で数分で終わるものほど積む価値が回収される。ワイヤー通しのような手間のかかるものは下位。
- 上位25件のみ付与、残29件は null（＝推奨対象外・統計フェーズの積載率に委ねる）。`recommend_priority` は id と違い**可変**（回答を壊さない）ため運用中の見直し可。
- 推奨から意図的に外した判断: デスビキャップ/ローター（あった方がよいが推奨までは不要）／スペアタイヤ・車載ジャッキ・携帯空気入れ（積んでいて当たり前＝指摘の新規性が薄い）／車検証・自賠責（法定だが推奨で返すのは失礼）。
- **派生要件**: 車載ジャッキは「何を積んでいるか」のバリエーションが興味を引くため、推奨とは別に**メモ収集の対象**とする（実装方針は下記「未確定」）。

### 2026-07-23 メモ収集の器＝完了
- migration `equipment_notebook_item_notes` 適用。控え＝`schema.sql`（該当列にmigration名を注記）。
- `equipment_items.note_prompt text` ＋ `equipment_entries.note text` を追加。**note_prompt に値がある項目だけ UI にメモ欄を出す**（全項目に出すと「照合に疲れて離脱する」設計原則に反する）。プロンプト文はプレースホルダを兼ねる。名品（`item_class='meihin'`）の収集路も兼ねる。
- 初期プロンプト2件: id9 車載ジャッキ「どんなジャッキ？（純正パンタ／油圧フロア／パンタ社外 など）」／id1 プラグレンチ「銘柄・仕様は？（薄肉／首振り／エクステンション付き など）」。反応を見て増やす。
- 表記修正: id13「軍手・ウエス／ペーパータオル」（出先では使い捨てが実態に近い）。

### 2026-07-23 タスク3（入力UI `equipment-edit.html`）＝実装完了・未デプロイ
- repoルートに `equipment-edit.html` を新規作成。`noindex`（個人の入力画面のため。集計ページ `equipment.html` は別途インデックス対象）。
- 構成: ログインOTPオーバーレイ（`edit.html` と同型・Googleログイン併設）／車両情報（所有車が2台以上のときだけ車両セレクタを表示・年式は `cars.year` からプリフィル）／カテゴリ5つを `details` 折りたたみ＋進捗 `n/m` 表示／項目行＝名前＋一行理由＋「常時・随時」トグル（同じボタン再押下で解除＝積んでいない）／`note_prompt` を持つ項目のみメモ欄（項目行の下に全幅）／カテゴリ末尾に自由追加行＋完了チェック／経験談／固定保存バー。
- **入力画面に編集部推奨バッジは出さない**（設計判断）。推奨を先に見せると「推奨されているから常時にしておこう」というバイアスで積載率データ自体が歪むため。気付きの動線は一行理由が担い、推奨は保存後の結果画面（次タスク）で返す。
- 保存: records は insert時のみ `user_id` を送り、**update では所有権列を送らない**（`edit.html` の事故経路の教訓）。entries は選択分を upsert（`onConflict: 'record_id,item_id'`）＋非選択分を delete、category_status は upsert、custom_items は総入れ替え、experiences は1件運用（空なら delete）。`onAuthStateChange` 内は `setTimeout(...,0)` でデッドロック回避。
- 派生migration `equipment_custom_items_category`: 自由追加欄はカテゴリ末尾に置く設計なのに `equipment_custom_items` にカテゴリ列が無く、どのカテゴリの穴かを記録できなかったため `category text` を追加（名品昇格時の配置先判断に使う）。
- 検証: JS構文チェック（node --check）／ローカルHTTPサーバ＋Playwright 390px幅で描画確認（項目54件の読み出し・トグル・メモ欄・自由追加・完了チェック・カテゴリ進捗・横スクロールなし=375px）。コンソールエラーはローカルの Cloudflare Analytics CORS のみで実装起因なし。**ログイン後の実データ保存はブラウザ実機での確認が必要（未実施）**。

### 2026-07-23 リコメンド設計の再定義（2層＋タイプ連動）＝ユーザー確定
- **問題認識（ユーザー起点）**: カーライフスタイルは「全部自分で直す〜ロードサービス丸投げ」のグラデーションであり、1本の推奨リスト（1〜23）は「自走復帰できるべき」という一点の価値観の押し付けになる。タイプ分類が最初から複数価値観を認めているのと矛盾するのはフェーズ1編集部推奨のみ。
- **確定した方式＝推奨を2層に分割**:
  - **第0層＝スタイル非依存の安全層**（三角表示板・発煙筒＋モバイルバッテリー・飲料水程度）。「路肩で救援を安全に待つ」はどのタイプにも共通に発生するため全員に返してよい。
  - **第1層＝スタイル相対の自走復帰層**（recommend_priority 3〜23 の工具/パーツ/ケミカルの梯子）。本人の回答バランス（工具・パーツの厚み）で表示を条件分岐。機械系が厚い人には梯子の欠けを返し、ほぼ空の人には**返さない**（「もし自走復帰に興味があれば→」の招待に留める）。
  - 判定材料は本人の回答＝統計不要＝**フェーズ1から実装可**。タスク3（リコメンド）とタスク4（タイプ分類）は自然に合流する。
  - **スキーマ変更不要**。recommend_priority=「自走復帰の梯子の順序」と再定義、安全層か否かはカテゴリ（非常時対応か否か）から導出。変わるのは結果画面の表示ロジックと文言のみ。
- **身軽型への態度＝肯定から入る（ユーザー確定）**。「あなたの割り切りは合理的です。最低限これだけ」と、割り切りを尊重する文言にする。推奨の口調は全タイプで「積むべき」でなく「あなたのスタイルなら」。

### 2026-07-23 タスク3+4（編集部推奨リコメンド＋タイプ分類）＝実装完了・未デプロイ
- 上記「リコメンド設計の再定義」どおり、`equipment-edit.html` に保存後の結果セクション（`#result-section`）として合流実装。新規ファイルは作らず、既存の入力画面に追記（保存後に自動スクロール表示・既存手帳がある場合は読み込み時にも表示）。
- タイプ判定 `classifyType()`：カテゴリ別搭載率から4タイプを判定（工具・パーツ平均＝mechScore、ケミカル比、非常時対応比、全体搭載数）。全体搭載数2以下は無条件で身軽型。統計閾値は使わず本人の回答のみで判定（フェーズ1はこれで十分・design-original.mdの「同型オーナー比較」はフェーズ2の統計リコメンドで実装）。
- リコメンド `renderResult()`：`recommend_priority` 昇順で未搭載項目を抽出し、非常時対応＝第0層（誰にでも常時表示）／それ以外＝第1層（工具+パーツの搭載数が3以上のときのみ「梯子」を最大5件表示。3未満は身軽型なら肯定文言のみ、それ以外はライダー1件だけを招待として提示）。
- `equipment_items` の select に `recommend_priority` を追加。
- 検証: 6スクリプトブロックすべて構文チェックOK（node --check相当）。分類ロジックをNodeで単体シミュレーション（工具/パーツ厚め→現地修理型、ケミカル厚め→予防整備型、非常時対応厚め→救援前提型、ほぼ空→身軽型の4パターンとも意図通り分岐）。**ブラウザでの実データ確認・実機Playwright確認は未実施**。
- 閾値（`MECH_THICK_THRESHOLD=3`など）はHANDOFF記載どおり実データが貯まってから調整可能な暫定値。

### 2026-07-23 タスク5（結果カード画像化）＝実装完了・未デプロイ
- html2canvas新規導入は見送り、`edit.html`のマスキングツールと同型のCanvas 2D自前描画を採用。理由＝`#result-section`はDOMそのままのスクショではなく「SNS共有用に再構成したカード」として設計する方が見栄えが良く、外部依存も増やさないため。
- タスク3+4の`renderResult()`から表示分岐（見出し文言・件数）を`computeResultData()`（本人回答から算出する共通データ）と`buildResultSections()`（安全層／梯子／招待文言のどれを出すかの分岐、DOM描画とCanvas描画の両方が参照）に切り出し、文言の二重管理・食い違いを防止。
- Canvas描画は2段階：まず計測用の一時canvasで文字幅を測りながら描画コマンド列と総高さを算出（`layoutResultContent`/`layoutSection`）→ その高さで実canvas（2倍スケールでRetina対応）を作って一括描画（`buildResultCanvas`）。日本語は分かち書きが無いため文字単位で折り返す簡易wrap（禁則処理はしない）。
- 「結果を画像で保存」ボタン押下で`canvas.toDataURL('image/png')`をdownload属性のaタグ経由でダウンロード（iOS Safariはdownload属性が効かず新規タブ表示になる想定・edit.htmlのマスキングツールと同じ制約を許容）。
- 検証：Playwright実機でSupabase実データ・実ログイン（OTP）→工具4点保存→現地修理型判定→画像ダウンロードまで確認。**初回実装ではセーフティ層最後の理由文と次見出し「自走復帰の備え、次の一手」が重なるレイアウトバグを発見**（セクション間の余白を`y+10`にしていたのが原因。テキストのbaseline間隔として不足）→`y+26`に統一して修正・再検証で解消を確認。身軽型（安全対応揃い＋招待文言のみ）・長文理由の2〜3行折り返しもモックデータ注入で追加確認、重なりなし。

### 2026-07-23 タスク6（集計表示 `equipment.html`）＝実装完了・未デプロイ
- リポジトリルートに新規ファイル `equipment.html` を作成。ログイン不要・全員向けの公開集計ページのため `equipment-edit.html` と異なり **noindexを付けず検索インデックス対象**（canonical=`https://www.registro500.com/equipment`）。`sitemap.xml` にも追加。
- `equipment_item_rates()` を `supabaseClient.rpc('equipment_item_rates')` で取得し、`CATEGORIES`（工具/パーツ/ケミカル/非常時対応/洗車・その他）でグルーピングしてカテゴリ別セクション表示。項目は積載率(`load_rate`)降順（0%の項目も「まだ誰も挙げていない」情報として表示）。
- 手本は `survey-results.html` の `.bar-track`/`.bar-fill`。常時/随時の内訳が分かるよう1本の`.bar-track`内に2色（常時=accent／随時=`--occasional`）のスタックバーに拡張。
- `total_records=0`（母数ゼロ）のフォールバック実装：「まだ記録がありません。最初の1件はあなたです」＋`equipment-edit.html`への導線。
- 検証: ローカルHTTPサーバ＋Playwright実機で本番Supabase実データ（検証時点 `total_records=1`）を390px幅で表示確認、コンソールエラーはローカル特有のCloudflare Analytics CORSのみ。母数ゼロパスは`renderRows([])`を直接呼び出して検証し、初回実装ではlegendと直前のカテゴリ描画がゼロ件表示時に残存するバグを発見・`renderEmpty()`でlegend非表示＋categories空化するよう修正して解消を確認。
- **これでMVP残タスク（次のタスク1〜6）が全て実装完了**。以後はコミット・デプロイ判断のみ。

### 2026-07-23 画面レビュー反映（ユーザー実機チェック）＝実装完了・未デプロイ
公開後にユーザーが初めて実画面を通しで確認し、出た指摘を反映。控え＝`items_review_2026-07-23.sql`（migration `equipment_notebook_items_review_2026_07_23` 適用済）。

- **冒頭に対象車を明示**。1台オーナーには車両セレクタを出さない設計だったため、「この手帳がどの車のものか」が画面のどこにも出ていなかった。`#app` 先頭に `.target-car` 帯を追加（例: `Nuova500 F ／ 1971年式`＝`model_display_a` + `model_display_b` + `year`）。2台以上のセレクタ変更にも追従（`renderTargetCar()`）。**登録車両と紐づいていないユーザーには「紐づいていません（このまま記録できます）」と明示**（後述の未ログイン層の件）。
- **年式の手入力を廃止**。年式帯別集計はニーズが想像できないという判断。UI から `#model-year` を削除し、カード説明文も「遠出の多さ別」のみに修正。`model_year` 列は残し、**選択中の車の `cars.year` から自動で控える**（匿名フェーズ2で車が無いケースの器として維持。UI上のコストはゼロ）。
- **id11 のリネーム**: 「ヘッドライト（頭部装着型）」→「作業用ライト（懐中電灯・ヘッドランプなど）」。**ライトの種類を固定化しない**（形状を1つに決める必要がない）。reason も両手が空くことを推奨に留める表現へ。
- **id52 車検証・自賠責 / id53 ETC・小銭 / id54 スマホホルダー・充電ケーブル を is_active=false**。「積んでいて当然＝指摘の新規性が薄い」＝recommend_priority の判断軸と同じ。有効項目 54→**51件**、`洗車・その他` は2件（マイクロファイバークロス・ガラスクリーナー）に減った。**カテゴリの存続自体は要判断（下記「未確定」）**。
- **CSV書き出しを追加し、結果画面の主ボタンに昇格**。画像保存は残すが副ボタン（`.btn-secondary`）へ降格。ユーザー判断＝1枚画像より「Excelで開ける・印刷して車に積める」CSVの方が使い道が想像できる。搭載分＋自由追加分のみを `カテゴリ,品名,積み方,メモ,なぜ積むのか` で出力（未搭載行はチェックリストの用途上ノイズになるため出さない）。**BOM付きUTF-8**（無いとExcelで文字化けする）。GA4 `equipment_csv_download`。
- 検証: インライン2ブロックとも構文チェックOK。ローカルHTTPサーバ＋Playwright 390px で対象車帯・年式撤去・ボタン2種の描画を確認、横スクロールなし（scrollWidth 375）。コンソールエラーはローカル特有の Cloudflare Analytics CORS のみ。**ログイン後の実データでの通し確認（保存→CSV中身）は未実施＝要ユーザー実機**。

### 2026-07-23 追加2件（link_owner_car呼出＋CSVヘッダ行）＝実装完了・未デプロイ
- **`link_owner_car` 呼出を追加**。上記「未確定」にあった未ログイン63台の車両未紐付け問題を解消（詳細は下の「未確定」欄の取り消し線参照）。
- **CSVの先頭に対象車・出力日の行を追加**。`装備手帳,<対象車>,出力日,<YYYY-MM-DD>,` を見出し行の前に挿入。印刷して車に積んだとき、どの車のいつ時点のリストか分かるようにする目的。空判定の閾値も2行（対象車行＋見出し行のみなら空）に修正。
- 検証: `csvCell`/`buildCsv` をNode単体実行で抽出しモックデータで検証（BOM先頭・対象車行・空判定の3点とも意図通り）。構文チェックOK。ブラウザ描画は追加分に見た目の変化なし（結果画面表示後のみ関わるロジック）。

## マイガレージ連携について（方針の確認）

**予定どおり。器は最初から入っている**（新規のスキーマ変更は不要）。

- `equipment_records.vehicle_id`（= `cars.document_id`）で車両に紐づく＝`schema.sql:29`。
- `equipment_records.is_public boolean not null default false`＝`schema.sql:31`。**開示/非開示はレコード単位で選べる設計で、既定は非公開**。
- 未実装なのは「公開トグルのUI」と「ガレージページ側の表示」のみ＝design-original.md のフェーズ2「ガレージページでの手帳公開・展示」。
- **トグルUIだけを先に出さない**方針。ガレージ側の表示が無い状態で「公開する」を押せると、公開したのにどこにも出ない状態になる。**表示の実装とトグルは同時に出す**。パイロット期間中は全レコード非公開のまま（RLSで本人以外読めない・集計は `equipment_item_rates()` の全体値のみ）。

## RLS 早見（現状＝MVP・ログイン必須）

- `equipment_items`：全員 SELECT 可。書込は service_role のみ。
- `equipment_records` ほか6テーブル：本人（`user_id = auth.uid()`、子は親record経由）のみ SELECT/INSERT/UPDATE/DELETE。他人の手帳は読めない。
- 集計の公開読み取りは `equipment_item_rates()` 経由のみ。生の他人レコードは非公開。
- 匿名 anon INSERT は未付与（フェーズ2で `is_anonymous=true` 限定ポリシーを追加）。

## 次のタスク（MVP残り・次セッション以降）

1. ~~**`recommend_priority` の編集部推奨順を確定**~~ ＝**2026-07-23 完了**（上記実装ログ参照）。
2. ~~入力UI `equipment-edit.html`~~ ＝**2026-07-23 完了**（上記実装ログ参照・未デプロイ）。
3. ~~編集部推奨リコメンド（フェーズ1）~~ ＝**2026-07-23 完了**（上記実装ログ「タスク3+4」参照・未デプロイ）。
4. ~~タイプ分類の表示~~ ＝**2026-07-23 完了**（同上・未デプロイ）。
5. ~~結果カード（本人保存用の1枚画像）~~ ＝**2026-07-23 完了**（上記実装ログ「タスク5」参照・未デプロイ）。
6. ~~集計表示 `equipment.html`（`equipment_item_rates()` を横棒グラフ表示）~~ ＝**2026-07-23 完了**（上記実装ログ「タスク6」参照・未デプロイ）。

**MVP実装は全6タスク完了**。残るはコミット・本番デプロイの判断のみ。

**フェーズ2**: 統計リコメンド自動切替／変更履歴の書込＋過去比較／ガレージ公開（is_public）／管理画面の名寄せ・名品昇格／匿名anon INSERT開放／セグメント別リコメンド。

## 未確定（実装前に確認）

- **【2026-07-24 起票・別途相談】購入先リンク（アフィリンク）の見せ方**。仕組みは実装済み（`purchase_links` 全null＝現状不可視）だが、**結果画面でどう見せるかは未決でユーザーと別途相談する**。検討用プレビューを本番と同じCSS・同じHTML構造で作成済＝`registro500-notes/preview-equipment-purchase-links.html`（画像 `equipment-purchase-links-preview.png`。**リポジトリ外＝非公開**）。論点＝1項目あたりのチャネル数（Vベルトの例で4つ並ぶと多いか／Amazon＋専門店の2つに絞るか）、安全層と自走復帰層で出し分けるか、そもそも入力直後の結果画面に出すのが適切か。
- **【2026-07-24 保留＝本番リリース時に実装】装備手帳とマイガレージの相互リンク**。装備手帳側に「マイガレージへ」（`edit.html?type=500`）、マイガレージ側に装備手帳への導線を**ペアで**置く。**テスト（パイロット）が終わって本番リリースするタイミングで実装する**と決定。それまでURL直案内のまま。
- **`洗車・その他` カテゴリの存続**（2026-07-23 レビューで3項目を非表示化し、残り2件＝マイクロファイバークロス・ガラスクリーナーのみ）。①2件のまま残す ②項目を足す ③カテゴリごと畳んで2件を他カテゴリへ移す（※移動は `category` の変更＝id は不変なので回答は壊れない）のいずれか。タイプ分類は工具/パーツ/ケミカル/非常時対応のみを見ているため、**どれを選んでも分類ロジックへの影響は無い**（`classifyType()` の `overallLoaded` に洗車は含まれない）。
- ~~未ログインのオーナーは車両が紐づかない~~ ＝**2026-07-23 対応済み**。`onAuthStateChange` の `loadAll()` 呼び出し前に `await supabaseClient.rpc('link_owner_car')` を追加（edit.html と同型・失敗時もconsole.errorのみでUIは止めない）。
- パイロット2〜3名の先行記入は id 確定後にユーザーが実施（本番データに繰り入れ）。

### 2026-07-23 パイロット実地テストで発見した3件の不具合＝2件修正済・1件は要確認事項として保留

パイロット2名からOTPログイン関連の不具合、1名から機能面のフィードバックが届いた。

- **不具合A: 自由追加欄など未保存の入力がログイン中に消える（実装バグ・修正済）**。`onAuthStateChange` が `USER_UPDATED` 以外の全イベント（Supabaseがバックグラウンドで発火する `TOKEN_REFRESHED` 等を含む）のたびに `loadAll()` を再実行し、DBの値で全項目・自由追加欄・経験談を上書きしていた。51項目を時間をかけて入力する画面のため、長時間の入力中にトークン自動更新が挟まると入力が丸ごと消える。**`hasLoadedOnce` フラグで初回ログイン時のみ読み込むよう修正**。同型の脆弱性が `edit.html`（車両編集）にもあったため同時に修正（下記「認証まわりの横断修正」参照）。
- ~~不具合B: SoftBank等キャリアメールでOTPコードが読めない~~ ＝**2026-07-24 根治・実機確認済み**。原因はテンプレートの装飾だけでなく、送信元（`news@registro500.com`）がニュース一斉配信と共用でキャリア側に「メーリングリスト」誤分類されていたこと・Supabase標準送信がHTML単体でtext/plain代替を持たなかったことの複合。**Supabase標準送信をSend Email Hook経由の自前実装（`supabase/functions/send-auth-email`・multipart化・送信元を`auth@registro500.com`に分離）へ全面置き換え**して根治。詳細な原因調査・実装・詰まった点（Hook secretの`v1,whsec_`prefix除去必須／Hook有効化でメール送信レート制限が自動的に30→2/時間へ下がる）は認証メモリ（`project_auth_flatten.md`）に集約。**2026-07-24 push・本番デプロイ済み（コミット`8da4231`）。大塚さん（nifty.com）が再ログイン成功を本人確認済み**。napdesignさん（softbank.ne.jp）側は再テスト待ち。
- ~~不具合C: プラグレンチの表記「14mm」がレンチの対辺サイズと誤解を招く~~ ＝**2026-07-23 ユーザー確認により修正済**。id1「プラグレンチ（14mm・深さのあるもの）」→「プラグレンチ（20.8mm・六角二面幅／21mm表記のもの）」（migration `equipment_notebook_plug_wrench_size_fix`・控え`items_review_2026-07-23.sql`）。14mmはスパークプラグのネジ径であり、レンチの対辺サイズとは別物だった。
- 「途中で保存できない」という指摘は不具合ではなく仕様どおり（画面下部の固定保存バー1つで全項目を一括保存する設計）。ただし不具合Aと組み合わさると「保存し忘れたら消える」体感になっていたため、不具合Aの修正で実質的なリスクは解消。

### 2026-07-24 購入先リコメンド（アフィリエイト）＝仕組み実装完了・リンクは全null＝非表示

- **チャネル序列＝国内調達優先で確定（変更しない）**：①amazon（アフィリエイト・PR表記必須）②monotaro（定番代替品・素のリンク）③shop（専門店直リンク。スタンダードスピード等＝登録オーナーでもあり応援したい・国内は届くのも速い）④kurabetaro（自サイトの比べ太郎＝parts.htmlへの内部リンク。海外通販は「やりたい人向けのサブツール」という位置付けで、届くまで日数がかかる旨を添える）。
- **`equipment_items.affiliate_url`（全件null）→ `purchase_links jsonb` へ置き換え**（migration `equipment_notebook_purchase_links`）。全件nullのため既存データへの影響なし。控えは `schema.sql` に反映済み。
- **点灯は本番リリース時**：`purchase_links` を投入するmigrationを別途流すまでボタン行自体が描画されない（`recoItemHtml()` が `purchase_links` が null/空なら従来と完全に同一のHTMLを出力）。パイロット中の今は不可視のまま安全にデプロイ可能。
- **入力画面には出さない**（既存の「推奨バイアスを避ける」設計判断と同じ。購入先リンクは結果画面のリコメンド項目のみ）。
- **集計ページ `equipment.html` には初期は付けない**（反応を見てから判断）。
- **Amazonリンクは装備手帳専用のトラッキングタグを発行してから使う**（ユーザー作業・未実施。goods.html/prime-saleで共通タグ`registro500-22`を使い回すとページ別の売上・報酬が切り分けられない教訓＝プライムデー特集の反省を踏まえる）。
- PR開示文（`#affiliate-disclosure`）は「表示中のリコメンドにamazonリンクが1つでも含まれる場合のみ」表示する制御（goods.htmlの開示文と同型）。GA4計測は `equipment_buy_click`（channel/item_name）をイベントデリゲーションで実装（`#result-section` に1つclickリスナー・inline onclickは使わない）。
- 副産物：`parts.js` に `?q=キーワード` のディープリンク対応を追加（比べ太郎ボタンから直接検索結果を出すための汎用改修。既存の `?type=126` 処理とは独立で影響なし）。

**次のタスク**：リリース判断後、51項目（有効項目）のチャネル選定（ユーザーレビュー必須）→ `purchase_links` 投入migration。Amazon専用トラッキングタグの発行（ユーザー作業）。**⚠️その際、`#affiliate-disclosure`の開示文がgoods.htmlの文言をそのまま流用しており末尾に共通タグ`registro500-22`が固定で書かれている（2026-07-24レビューで発見）→専用タグ発行後、開示文のタグ名も合わせて差し替えること。**
- **パイロットで見つかった3件（A・B・C）はすべて対応完了。OTPログインで詰まるリスクは解消し、パイロット依頼を再開できる状態**。

### 2026-07-24 パイロット回答の分析＝項目マスター見直し（有効51→63項目）＋入力欄改善

パイロットを先行依頼した目的＝**回答内容から項目の妥当性を検証すること**。記録6件（実質5名。DOC_1は4項目のみの部分入力）を分析した結果と対応。

**分析でわかったこと**
- 自由追加30件のうち**19件（63%）が「工具」に集中**。一方**非常時対応（12項目）は自由追加ゼロ**＝マスターが十分。つまり**工具13項目という粒度が実態に足りていなかった**のが最大の欠落。
- **複数人が独立に同じ物を追加＝最強の欠落シグナル**：ラチェット＋ソケットセット(2名)／グリス各種(2名)／ドライブシャフトジョイント(2名・両者とも「走行不能になる」と明記)／トルクレンチ(2名)／キャブレター関連(2名)。
- **粒度の設計ミス**：デスビとキャブは「マスターは部品単位、オーナーはアッセンブリ単位で積む」ズレ（スシオ氏「なるべく早く復帰できるよう、丸ごと交換するため」）。→ **単品も一式も両方イキ**で併存させる方針に決定。
- **点火系の論理的な穴**：ポイント・コンデンサー・プラグ・プラグコード・デスビキャップ・ローターと網羅しているのに**イグニッションコイルだけ抜けていた**。
- **安全項目の穴**：車載ジャッキ(9)・輪止め(10)はあるのに**ジャッキスタンドが無い**（MEX氏「ジャッキアップ時の安全確保に必須」）。
- **回答0件の項目**＝液体ガスケット・マフラーパテ（ガレージ作業用で車載向きでない）→ 非活性化。**パンク修理剤も0件だがチューブレスのオーナーが多いためユーザー判断で残す**（0件の理由はチューブタイヤ勢に効かないためで、項目自体は妥当）。
- **note_promptは成功**：2項目だけで5件のメモ（回答機会7回中5回＝高回答率）。車載ジャッキで**「マサダ油圧パンタジャッキ」が2名から独立に**挙がり、**誰も純正パンタを使っていない**というそのままコンテンツになる発見。→ 8項目へ拡大。
- **発見性の問題（マスターではなくUI）**：fiat_takeshi氏が「水」を工具カテゴリに自由追加したが、**マスターには飲料水(49)が非常時対応に既に存在**。63項目をカテゴリ折りたたみで探す構造の限界。→ 検索/サジェストはフェーズ2。
- **重複登録バグを発見・修正**：`equipment_custom_items` に同一 record_id+name が2行。原因は**OTP修正前に `loadAll()` が二重実行され `customRows` に同じ行が2回積まれた**名残（`hasLoadedOnce` ガードで根因は解消済）。保存時にも同名重複を弾く防御を追加し、既存の重複行はDBから削除。

**適用内容**（migration `equipment_notebook_items_pilot_review_2026_07_24`・控え `items_pilot_review_2026-07-24.sql`）
- **新規16項目（id 55〜70）**：工具10／パーツ5／ケミカル1。**id 1〜54は不変**、不要項目も削除せず `is_active=false`。
- **「洗車・その他」カテゴリを廃止**（残2項目を非活性化）。主題＝出先トラブル備えから外れ回答も低調だったため。**項目0件のカテゴリは両画面とも描画をスキップするのでHTMLの `CATEGORIES` 配列は変更不要**＝2行のUPDATEを戻せば復活できる。
- **recommend_priority を1〜33へ再割当**。**v3で確定した既存23件の相対順はそのまま維持し、新規・未設定の項目だけを間に挿入**している（v3の編集部判断を壊さないため）。無料で積める（ロードサービス連絡先・保険証券）と安全（輪止め・ジャッキスタンド）を上位へ繰り上げ。
- 適用後＝**有効63項目**（工具23／パーツ20／ケミカル8／非常時対応12／洗車0）、note_prompt 8項目、recommend_priority 33項目。

**画面側**（commit `03a120e`・main push済）
- **乗る頻度に「毎日」を追加**。
- **遠出の定義に数値目安を明示**＝「片道50km以上、または高速道路を使う移動」。napdesign氏から「長距離とは何kmか」と問い合わせがあり、**主観に寄らない基準が必要**という判断。「随時」の定義がこの数値に紐づくため、選択肢の直上にヒントとして置いた。
- 自由追加の保存時に同一カテゴリ内の同名重複を弾く。

**この分析で残った検討事項**
- 検討したが今回は見送った自由追加＝ノギス・ピッチゲージ・タイミングライト（MEX氏のみ・専門性が高い）、マスキングテープ／ビニールシート（napdesign氏・雨天用）。次のパイロットで再出現したら追加を検討する。
- 母数の扱い：DOC_1（4項目・全カテゴリ未完了）のような部分入力が公開集計の母数に入る。回答者が増えるまでは影響が大きいので、集計から除外する条件（例：1カテゴリも完了していない記録は除く）を将来的に検討。

### 2026-07-24 タイプ判定ロジックの検証＝誤判定を発見（要修正・提案中）

パイロット6件に現行 `classifyType()` を実データで適用して検算したところ、**2件が明らかな誤判定**だった。

| 回答者 | 積載 | 工具率 | パーツ率 | ケミカル率 | 非常時率 | mechScore | 現行判定 |
|---|---|---|---|---|---|---|---|
| MEX | 41 | .57 | .55 | .75 | .92 | .558 | **予防整備型 ← 誤り** |
| スシオ | 41 | .57 | .70 | .63 | .75 | .633 | 現地修理型 |
| ぱそた | 37 | .83 | .50 | .38 | .42 | .663 | 現地修理型 |
| otsuka | 32 | .52 | .45 | .38 | .67 | .486 | 現地修理型 |
| fiat_takeshi | 9 | .13 | .05 | .00 | .42 | .090 | 救援前提型 |
| napdesign | 5 | .17 | .00 | .00 | .08 | .087 | **現地修理型(fallback) ← 誤り** |

**根因1＝カテゴリ間で項目数が違うのに「率」を直接比較している**。有効項目は工具23／パーツ20／ケミカル8／非常時12と偏りが大きく、**項目数の少ないケミカルは率が跳ねやすい**。MEXは工具42項目＋自由追加16の最重装備なのに、ケミカル率.75 > mechScore.558 で `yobou`（予防整備型）に落ちた。

**根因2＝身軽型の閾値 `overallLoaded <= 2` が厳しすぎ、最後のfallbackが `genchi`**。napdesignは5項目（工具のみ）なのに「現地修理型」と判定される。**判定に失敗したとき最も重い型に落とすのは失礼**であり、fallbackは身軽型にすべき。

**【2026-07-24 ユーザー承認・実装/デプロイ済 commit `286c389`】率でなく実数で判定する決定木に置き換えた**：
```
mech = 工具数 + パーツ数 (最大43) / emg = 非常時対応数 (最大12) / chem = ケミカル数 (最大8)
1. mech >= 15                  → 現地修理型
2. chem >= 5 かつ mech < 15    → 予防整備型
3. emg >= 4 かつ mech <= 8     → 救援前提型
4. それ以外                     → 身軽型（fallback＝最も控えめな型に落とす）
```
この式を上表に当てると6件すべて納得のいく型になる（MEX/スシオ/ぱそた/otsuka＝現地修理型、fiat_takeshi＝救援前提型、napdesign＝身軽型）。**閾値はn=6の暫定値なので、回答が増えたら見直す前提**。

**「合理的レッカー利用タイプ」＝救援前提型そのもの（ユーザー提起・重要）**：何でも自分で対応するのでなく、軽く対処できる装備だけ積んで、あとは割り切ってロードサービスを呼ぶ層。**パイロット5名は全員フル満載型に寄った極端なサンプルで、実際にはこの層が最も多い可能性が高い**。現行の説明文「ロードサービスと保険で構える。非常時対応の装備が、その安心を支えています。」は受け身に読めるため、**本人の能動的な合理判断として書き直す**（身軽型の「割り切り」と語彙が重複しているので、身軽型＝ほとんど積まない／救援前提型＝軽い装備は積んで使い分ける、と書き分ける）。
→ **実装済**：救援前提型 desc =「直せる範囲は自分で、無理はしない。手当てと救援の使い分けが、いちばん現実的な構えです。」

### 2026-07-24 結果画面に「4つの型」を並べて表示＝実装・デプロイ済（commit `286c389`）

- **それまでは本人の型の名前と説明しか出しておらず、他に何型あるのか一切見せていなかった**（ユーザー指摘）。型は対比があって初めて意味を持つため、**4型すべてを短い説明つきで並べ、本人の型に「あなた」バッジを付ける**表示を結果画面に追加（`#result-type-map`／`TYPE_ORDER`＋`TYPE_INFO.short`）。
- 短い説明＝現地修理型「工具とパーツで、自分で直して帰る」／予防整備型「消耗品と点検で、そもそも止まらせない」／救援前提型「軽い手当てと救援を、使い分ける」／身軽型「近距離中心。最低限で割り切る」。
- **結果カード画像（Canvas）には型マップを入れていない**（カードは1枚絵として簡潔さを優先）。必要なら後から追加可能。
- これは**未実装の「目指す型を選ばせて型を超えたリコメンドを返す」構想（`goal_type` 列を足すだけ）への下地**でもある。4型が見えていないと「移行したい型」を選ばせようがないため、まず一覧を出した。

### 2026-07-24 項目名の見直し 第2弾＝適用済（migration `equipment_notebook_items_naming_2026_07_24`）

**原則：項目名は幅を持たせ、細かい内訳は `note_prompt` で集める。ユニット丸ごと積む人と部品単位で積む人は別の行動なので必ず別項目にする。**

- **id 21 ヒューズ（ガラス管）→「ヒューズ」**。**空冷Fiat500の実物は欧州車用セラミック（砲弾型）でガラス管ではない**（ユーザーが実物の商品画像で提示）。ブレード型へ換装した車両もあるため名前で規格を限定しない。note_prompt「規格とアンペア数は？（欧州車用セラミック／ブレード型 など）」で内訳を拾う。
- **id 22 電球一式 →「予備の電球」**。「一式とは限らず、切れやすい球だけ積む人もいる」（ユーザー指摘）。note_prompt「どの球を積んでいますか？（ヘッドライト／ウインカー／テール／メーター など）」。
- **id 66 キャブレター（本体または補修部品）→「キャブレター本体（丸ごと交換用）」＋ 新規 id 71「キャブレターの補修部品（ガスケット・Oリング・ニップル・フロート など）」に分割**。旧66は「載せ替えて帰る」と「現地で開けて直す」という別の行動を1項目に押し込んでおり集計が意味を失っていた。MEX氏の自由追加「キャブレターの蓋部分（ニップル抜け対策）」は71に該当。
- **id 67 デスビ一式 →「デスビ本体（丸ごと交換用）」**（同じ原則の明示。キャップ19・ローター20は単品のまま併存）。
- 有効項目は **64項目**（工具23／パーツ21／ケミカル8／非常時対応12）。

### 2026-07-25 タイプ分け再設計＝「止まり方一覧」タタキをテスター公開（commit `e953778`）

- タイプ分けを「積んでいる数」でなく**「どんな止まり方に対応できるか」**で表す再設計に着手。土台として**止まり方22件のタタキ**を整備wiki・オーナー記録・海外フォーラムから組み立て、`equipment-review.html`（noindex/nofollow・サイト内未リンク・sitemap未登録の限定URL）でテスター確認用に公開。
- 単一HTML完結・localStorage自動保存・「回答をコピーしてLINE/メールで返信」方式。装備はIDでなく装備名で提示、出典は整備wikiへ直リンク。
- 構成: 6系統（火花F／混合気M／圧縮C／電気E／駆動D／その他X）×難易度L1〜L3＋救援。質問4つ（①足りない止まり方＝最重要／②難易度の違和感／③パンク修理剤の実効性／④その他）。
- ※本エントリはv0.2反映時（2026-08-02）に遡って記録（公開当時HANDOFFへの記載が漏れていた）。

### 2026-08-02 止まり方一覧レビュー第1回（回答1件）＝v0.2反映

公開から約1週間で回答はMEX氏の1件のみ。他テスターの返答を待たず、MEX氏回答をベースに進める方針（ユーザー判断）。回答の内訳＝22件中20件「これで合っている」・D1のみ「直したい」・M3は用語保留。難易度の体系自体には「違和感なし」。

**決着した迷いポイント（v0.1で「迷っている」と明示していた5点）**
- F4（熱でコイル）とM3（ベーパーロック）＝**分けたまま確定**（「ほぼコイルが原因。分けた方がよい」）。
- M4の難易度＝違和感なし→現状維持（点火時期の大幅な狂いでも同症状の実例ありをnoteに追記）。
- C2＝**1件のまま両にらみ**（実例もプレッシャースイッチ故障=誤報側／オイルポンプ故障=即焼き付き・救援に分かれるが、症状の入口は同じ）。
- D2（シフトリンケージ）＝「走れなくはない・予備があれば路上修理する」→**L2/L3確定**。
- D3（ドラシャ）＝「安全な場所なら路上でやる」→**L3/R確定**。

**D1（唯一の「直したい」）を全面改訂**：破損箇所で対処が分かれる。①ペダル固定ピン破断（L以前）＝長さ25mm以上のM5高強度ボルトで応急=L2／②ワイヤー切れ＝**針金では補修できなかった実証**→その場交換=L3／③Rペダル（シャフト溶接）の溶接外れ・ベルハウジング内のクラッチアーム接続ボルト折損＝救援。lvを L2/L3/R の3段に変更、needから針金・タイラップを撤去。**押しがけ（セル/スターター利用）は事故リスクがあるため掲載しない**（MEX氏も「書けない」と同意見）。

**新規7件を追加（22→29件）**＝最重要質問「足りない止まり方」への回答（全て実体験）：
- B1 ブレーキ踏み抜き（**制動**・救援。ハンドブレーキは制動距離が倍で危険・路上修理不可）
- E5 スターターワイヤー断線（L1・スターター本体レバー直押し・火傷注意。フック外れだけのことも）
- D4 ギアチェンジ時のダダダ振動（ミッションサポートラバー劣化。酷ければ路上修理か救援）
- X3 リーフスプリングのセンターボルト脱落（そーっと帰宅→自宅で応急の実例。路上不可扱い）
- K1 クーリングファン破損（**冷却**・救援。走り続けると焼き付き）
- K2 冷却ダクトの脱走（気づかず走りオーバーヒート→走行不能の実例。MEX氏自身「書くか否か」と迷った件だが、脱走系の致命傷代表として掲載）
- K3 サーモスタット破損（閉固着型は夏場オーバーヒートの恐れ。路上対処の可否は未確認）
- 系統に**制動・冷却を新設（6→8系統）**。新規7件は「【v0.2追加】」を明記し難易度未レビューであることをページ上に注記。

**その他の反映**
- 凡例に**「路上復旧は知識・技量・工具と工夫が前提／応急処置はあくまで応急でリスクが伴う」の注釈**を追加（MEX氏提案）。切り分けの出発点として整備wikiトラブルシュート1（222.html）へのリンクを凡例に明記（「wikiにリンクを貼ると理解が早い」提案の反映）。
- F1に「切り分けの入口＝プラグに火花が飛ぶか否か」を追記（飛ばない→点火系／飛ぶ→燃料系・点火時期ズレ）。**本格的な診断フロー化はフェーズ2候補**（F1/F5共通の「原因探求の悩ましさ」はwiki立ち上げ時からの課題とのこと）。
- note拡充: F2（キャブ二次エア・プラグ接触不良）／F3（プラグターミナル外れ・コード咥え部の緩み、スプリングで押し付けて凌いだ実例）／M5（キャブリンケージ固定金具の損傷）／E1（ダイナモ/オルタのコイル不良・レギュレーター）／E3（端子外れ・断線・手信号の再確認）／E4（充電不良が最多・キースイッチ）／X2（キングピン摩耗・ベアリングナット緩み、オーバートルクで折った実体験）。
- localStorageキーは`eq_review_v1`のまま＝再訪テスターの既回答は保持され、新規7件だけが未回答で増える。

**未決事項（ユーザー対応・次の判断待ち）**
1. M3の用語（ベーパーロックかパーコレーションか、同一現象か）→**服部さん（整備wiki）に確認する**というMEX氏の提案。
2. パンク修理剤の実効性→MEX氏も未使用で不明のまま（質問③は据え置き）。
3. M2に消火器を加えるか（MEX氏の提案・半ば冗談。項目マスターにも無いため今回は見送り、記録のみ）。
4. 新規7件の難易度・採否の最終確認（特にK2の「外れた直後に戻せるか」・K3の路上対処可否）。**v0.2をMEX氏に再確認依頼するか**。
5. 項目マスター追加候補: シフトリンケージのカップリング（D2・既出）／ミッションサポートラバー（D4）→止まり方一覧の確定後にまとめて判断。
6. 本番反映（mainへのマージ＝限定URLの差し替え）のタイミング。現時点は作業ブランチ `claude/equipment-notebook-continued-uj39ww` のみ。
