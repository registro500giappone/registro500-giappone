{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@executeautomation/playwright-mcp-server"
      ],
      "env": {}
    },
    "supabase": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@supabase/mcp-server-supabase@latest",
        "--project-ref",
        "ttlttclfovuzafvghvaq"
      ],
      "env": {
        "SUPABASE_ACCESS_TOKEN": "（Supabase個人アクセストークン: https://supabase.com/dashboard/account/tokens）"
      }
    },
    "github": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "env": {
        "GITHUB_TOKEN": "（GitHub Personal Access Token: https://github.com/settings/tokens）"
      }
    },
    "brevo": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "https://mcp.brevo.com/v1/brevo/mcp",
        "--header",
        "Authorization: Bearer （BrevoのMCP API key: Settings → SMTP & API → API Keys → Create MCP server API key有効化）"
      ]
    }
  }
}
